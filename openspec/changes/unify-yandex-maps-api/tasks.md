## 1. Подготовка локальной приёмки

- [ ] 1.1 Подтвердить подключение безрекламного продукта JavaScript API для используемого ключа и доступ с локального origin; проверка — подтверждение владельца о настройке продукта и успешная загрузка настоящего SDK локально.
- [ ] 1.2 Снять до изменения локальный baseline новости с одним и несколькими событиями, карточки места и desktop-карточки поселка; проверка — сохранённые скриншоты, список страниц и условий замера, количество запросов, переданные байты и рекламные цепочки для сравнения после реализации.

## 2. Общий механизм превью

- [ ] 2.1 Выделить из `PlacePreview` app-owned механизм неподвижного превью и перевести на него карточку места с сохранением общего `lib/yandex-maps/runtime.ts`; проверка — тесты runtime и превью подтверждают совместную загрузку SDK, повторный вход, cleanup, fit всех областей, часы работы и передачу фокуса со ссылки на штатное действие.
- [ ] 2.2 Добавить простую однократную инициализацию превью через `IntersectionObserver`; проверка — короткий тест поведения подтверждает отсутствие запуска до приближения, независимый старт двух превью, отсутствие запуска у скрытого контейнера и cleanup при удалении во время загрузки.

## 3. Подключение фоновых карт

- [ ] 3.1 Перевести `NewsEventCard` на общее превью с обычным маркером, удалить iframe и `buildNewsEventMapEmbedUrl` вместе с ненужным сдвигом координат; проверка — тесты событий сохраняют координаты, исходный `mapUrl`, ссылку места и ICS, не передают иконку/расписание/контуры в превью, а поиск по исходникам не находит оставшихся вызовов iframe-builder.
- [ ] 3.2 Подключить общее превью в desktop-шапке поселка и удалить ставшие ненужными ветки декоративного режима `SettlementMap` после проверки всех вызовов; проверка — тесты и локальный браузер подтверждают тарифный маркер, исходную внешнюю ссылку и кадр шапки, а интерактивное сравнение сохраняет фильтры, fit, попапы и восстановление после ошибки.
- [ ] 3.3 Восстановить фоновую композицию через customization подложки и географическое кадрирование, сохранив штатные элементы Яндекса вне перекрытий; проверка — сравнение с baseline на узком и широком экране, доступность логотипа, условий и OpenMaps указателем и клавиатурой, вертикальная прокрутка над всеми превью.

## 4. Интеграция и документация

- [ ] 4.1 Проверить потребителей разрешения iframe Яндекса и актуальные запросы JS API, скорректировать CSP и её пояснения; проверка — согласованный diff `ops/nginx/security.conf`, `ops/nginx/CSP.md` и затронутых настроек сервера, настоящие карты работают локально с соответствующей CSP без нарушений.
- [ ] 4.2 Добавить ADR об общем подключении карт, связать его с ADR-028 и индексом решений, уточнить фоновые карты в дизайн-гайде; проверка — документы соответствуют `design.md`, описывают оба режима JS API и не вводят нового визуального макета.

## 5. Проверки и завершение реализации

- [ ] 5.1 Адаптировать существующие тесты превью, событий и visual fixtures к общей реализации, включая событие без места; проверка — `pnpm typecheck`, затронутые Vitest-тесты через `pnpm --filter @shelkovo/www test`, `pnpm --filter @shelkovo/www test:visual:news-event`, `pnpm --filter @shelkovo/www test:browser:compare` и `pnpm build` проходят.
- [ ] 5.2 Проверить локально настоящий SDK на `/map/`, карточке места, `/815/compare/`, карточке поселка и новости с несколькими событиями; проверка — записаны результаты отсутствия рекламы и независимого iframe, единственной успешной загрузки SDK в документе, корректных маркеров/контуров, resize, Astro-навигации и сохранения действий; сетевые показатели сопоставлены с baseline в одинаковых условиях.
- [ ] 5.3 Проверить локально отсутствие JavaScript, блокировку API и отложенный старт карт; проверка — сведения, исходные внешние ссылки и ICS доступны, скрытая шапка не инициирует загрузку, прокрутка работает, появление карт не сдвигает контент. Результаты вместе с визуальной приёмкой завершают проверку без обязательного production-этапа.
- [ ] 5.4 Провести ревью полного актуального diff, выполнить `pnpm openspec:validate`, синхронизировать основные specs и архивировать change после выполнения задач; проверка — все требования сопоставлены с результатами, повторная валидация после архивирования проходит, итоговый PR реализации содержит результаты локальной приёмки и `Closes #762`.
