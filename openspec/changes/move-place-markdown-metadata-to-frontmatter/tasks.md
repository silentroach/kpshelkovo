## 1. Публичный frontmatter карточки

- [ ] 1.1 Добавить readonly DTO и явный adapter Markdown-метаданных по `design.md`; переиспользовать экспортированный mapping интервалов и константу московского времени, добавляя timezone только для Markdown. Проверка: `pnpm typecheck` и существующий `places/tests/map-public.test.ts` проходят без изменения JSON-контракта.
- [ ] 1.2 Подключить frontmatter к `buildPlaceMarkdown` через AST API; убрать generated-сведения, часы, пояснение и собственные ссылки из children, сохранив H1, summary, редакционный body и backlinks. Проверка: тесты Markdown из группы 2 подтверждают поля, отсутствие generated-дублей и сохранение текста; diff `buildPlacesMarkdown` и HTML-страницы не меняет их поведение.

## 2. Регрессии публичного контракта

- [ ] 2.1 Обновить `apps/www/src/lib/places/tests/markdown.test.ts`: разобрать YAML существующими средствами, проверить его через Zod, зафиксировать небольшой snapshot метаданных вместо полного документа. Проверка: адрес, числовые координаты, коды категории/lifecycle (включая `underConstruction`), canonical и остальные абсолютные URL соответствуют месту; optional-поля отсутствуют при отсутствии данных, внутренние поля не просачиваются; targeted-тест проходит.
- [ ] 2.2 Заменить проверки печатной Markdown-недели проверками frontmatter: расписание с пояснением и без него, перерыв, выходные, ежедневные часы, скрытое с карты место и полное отсутствие расписания. Проверка: `periods` сохраняют все интервалы, `timezone` равен `Europe/Moscow`, отсутствующее `description` не сериализуется, body не получает generated-часы/пояснение, вычисленное open/closed не публикуется; targeted-тест проходит.
- [ ] 2.3 Сохранить и уточнить проверки body: H1, summary, форматирование и раскрытые ссылки редакционного текста, сгруппированные backlinks с датой/видом/выдержкой, пустое состояние; добавить случай авторского заголовка «Сведения», который не удаляется. Проверка: targeted-тест проходит и отделяет frontmatter от body, не полагаясь на большой snapshot полного Markdown.

## 3. Discovery и приёмка

- [ ] 3.1 Уточнить описание карты в `apps/www/src/lib/llms.ts` по `design.md`, отредактировав публичный русский текст по `humanizer-ru`. Проверка: готовый путеводитель направляет к метаданным/часам во frontmatter карточки и сохраняет ссылку `/map/index.md`; текущий реестр маршрутов и каталог остаются согласованными, новый endpoint не добавлен.
- [ ] 3.2 Выполнить `pnpm --filter @shelkovo/www exec vitest run src/lib/places/tests src/lib/public-surface/index.test.ts src/lib/discovery.test.ts src/lib/root-discovery-routes.test.ts`, затем `pnpm typecheck`, `pnpm build` и `pnpm openspec:validate`. Проверка: все команды успешны; существующие регрессии HTML-недели, пояснений, валидации интервалов, московского статуса и JSON проходят с прежней семантикой. Не обновлять их ожидания ради маскировки регрессии.
- [ ] 3.3 Разово проверить готовую сборку: Markdown-карточки с расписанием/пояснением и без optional-данных, HTML-карточку, `/map/index.md`, `/map/data/places.json` и discovery. Проверка: зафиксированы примеры и результаты по сценариям specs; индекс, HTML и JSON сохранили контракт. Постоянный smoke-обход сайта не добавлять, production-приёмка не требуется.
- [ ] 3.4 Провести correctness- и `ponytail-review` всего актуального diff, включая staged, unstaged и новые файлы; сверить все сценарии с реализацией и устранить замечания в объёме change. Проверка: список результатов ревью и проверок готов для PR #755; пользовательский `skills-lock.json` не включён в собственные изменения.

## 4. Завершение после реализации

- [ ] 4.1 После успешной приёмки синхронизировать specs и архивировать change штатными workflows в этой же ветке. Проверка: `pnpm openspec:validate` проходит; новая `place-markdown-card` имеет Purpose, все сценарии трёх MODIFIED-требований перенесены, остальные требования `place-opening-hours` сохранены.
- [ ] 4.2 При явном разрешении на публикацию обновить существующий PR #755 в продолжение issue #751: указать breaking Markdown-формат, результаты проверок и человеческую приёмку. Проверка: опубликованный diff соответствует согласованному change, ссылки действительны; при отсутствии разрешения результат остаётся локальным с явной отметкой о непубликации.
