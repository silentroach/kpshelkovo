# Tasks

## 1. Исходные проверки

- [x] 1.1 После разрешения реализации сохранить baseline production-сборки: запросы и суммарные raw/compressed размеры JS начальной страницы, первого открытия поиска и сравнения; проверить, что результаты позволяют сравнить одинаковые сценарии до и после, включая общие чанки.
- [x] 1.2 По skill `site-search` снять production baseline поисковой выдачи и исходные сбои quality-проверок; сохранить результат для последующего сравнения без подгонки snapshots.

## 2. Загрузка и восстановление

- [x] 2.1 Перевести поиск на обычный динамический импорт и согласованное сообщение из design при отказе, удалить кнопку повтора модуля без добавления кнопки обновления; адаптировать существующие runtime-тесты, проверив отсутствие автоматического reload, закрытие/фокус, ввод до успешной загрузки и игнорирование завершения после закрытия или Astro-перехода. Сверить видимый текст с согласованной редактурой `humanizer-ru`.
- [x] 2.2 Перевести explorer на обычный динамический импорт, разделить ошибки компонента/hydration и JSON; при отказе кода показывать согласованное сообщение из design без кнопки восстановления. Проверить существующими bootstrap-тестами успешную гидрацию, локальный повтор только данных, оба порядка двойного отказа и отсутствие действий после dispose.
- [x] 2.3 Удалить standalone-интеграции, их регистрации, virtual declarations, счётчики retry и вспомогательные типы/минификатор без оставшихся потребителей; поиском по проекту подтвердить отсутствие рабочих ссылок на удалённый путь, затем выполнить `pnpm typecheck` и `pnpm build`.

## 3. Приёмка поведения

- [x] 3.1 Переписать существующие browser-сценарии отказа JS поиска и сравнения: блокировать реальный lazy chunk при рабочем bootstrap, увидеть ошибку с подсказкой и без кнопки восстановления, снять блокировку, обновить документ средствами браузера и подтвердить восстановленную работу. Проверить, что сайт сам не обновляет документ, а сохраняющийся отказ не создаёт цикл.
- [ ] 3.2 На локальной production-сборке проверить ленивый поиск, синхронный фокус, SSR-карточки и disabled controls до гидрации, CSS без визуальной регрессии, отсутствие hydration warnings, фильтры/URL-state и повторный вход через Astro-навигацию. Сохранить результаты и сравнить JS-загрузку с baseline без заранее заданного обещания экономии.
- [ ] 3.3 Прогнать существующие проверки локального восстановления `explorer.json`, Pagefind и API карты; подтвердить, что сбой данных не требует обновления документа и не ломает работающие части интерфейса.
- [ ] 3.4 Проверить передачу доступности Pagefind через обычный pipeline с dev snapshot и без него, используя ограниченный тестовый harness либо отдельно разрешённый dev-server; повторить search-quality по skill `site-search` и сравнить с baseline, зафиксировав оставшуюся ручную приёмку, если она есть.
- [ ] 3.5 Выполнить `pnpm test`, `pnpm typecheck`, `pnpm build` и подходящие browser-команды проекта; записать результаты, реальные ограничения и исходные несвязанные сбои.

## 4. Документы и завершение

- [ ] 4.1 Дополнить ADR-002 решением о восстановлении после отказа JS через обновление средствами браузера по текстовой подсказке, без отдельной кнопки, и принятой потерей состояния поиска; проверить согласованность с delta spec и отсутствие изменений общей стратегии ClientRouter.
- [ ] 4.2 Проверить весь staged/unstaged/new diff, убрать лишние абстракции и выполнить `pnpm openspec:validate`; сопоставить каждый сценарий spec с результатом приёмки.
- [ ] 4.3 После успешной приёмки повторить отбор долгоживущих требований, синхронизировать spec и архивировать change штатным workflow; проверить `pnpm openspec:validate` и наличие согласованного контракта в основных specs.
- [ ] 4.4 При разрешённой публикации обновить направление #743, связать согласование с коммитом артефактов и открыть PR с `Closes #743`, проверками и оставшейся ручной приёмкой; проверить опубликованные ссылки. Если публикация не разрешена, явно зафиксировать локальный результат и отсутствие этих ссылок.
