# Tasks

## 1. Агент получает новые правила на нужном этапе

- [ ] 1.1 Добавить семь пунктов из `design.md` в существующие списки `openspec/config.yaml`. Сверить diff с отредактированными формулировками и убедиться, что прежние инструкции сохранены.
- [ ] 1.2 Проверить `pnpm exec openspec instructions <artifact> --change refine-openspec-quality-guidance --json` для `proposal`, `specs`, `design` и `tasks`: поле `rules` содержит дополнения только для выбранного артефакта. Для `specs` ожидается `skipped: true`. Тем же вызовом для `apply` проверить новые пункты в `operationGuidance`, а для `archive` убедиться, что их там нет.

## 2. Приёмка изменения

- [ ] 2.1 Выполнить `pnpm openspec:validate` и проверить форматирование изменённых файлов через `pnpm exec oxfmt --check`; устранить ошибки.
- [ ] 2.2 Выполнить проверки агентских инструментов из проектного CI: `pnpm test:agent-tooling` и `pnpm skills:check`.
- [ ] 2.3 Проверить весь diff, включая staged, unstaged и новые файлы: правила сохраняют согласованный смысл, покрывают объём proposal и не противоречат `AGENTS.md` или штатным инструкциям. Исправить найденные проблемы и упростить лишние формулировки.
