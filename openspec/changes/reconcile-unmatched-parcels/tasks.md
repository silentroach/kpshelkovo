# Tasks

## 1. Подтверждения без исходной ссылки

- [ ] 1.1 Расширить `ParcelMatchesSchema` вариантом `{ absent: true }` в `source_cadastral_references`, сохранив обязательный ключ для каждого кода и существующие строки. Проверить небольшими fixtures чтение обоих вариантов, отказ при пропущенном ключе и несовместимость маркера с произвольным объектом.
- [ ] 1.2 Обновить сравнение исходных ссылок и диагностику в `resolve.ts`. Проверить применение связи при сохранённом отсутствии, конфликт при появлении номера (в том числе совпадающего с целевым), изменении и исчезновении строки; отдельно проверить группу со строкой и отсутствующей ссылкой и исчезновение одного алиаса.
- [ ] 1.3 Проверить совместимость существующих подтверждений и воспроизводимость обновления на компактных fixtures в `tests/sources.test.ts` и `tests/update.test.ts`. Проверка двух исходных подтверждений не должна требовать, чтобы весь реестр навсегда состоял только из них; тесты должны сохранять защиту от дублей, коллизий и автоматического сопоставления по положению.

## 2. Первый пространственный разбор

- [ ] 2.1 Сверить принятые снимки рабочей ветки с текущими источниками и получить фактический остаток через существующее разрешение связей. Зафиксировать в отчёте даты и количество по частям и причинам; при обновлении использовать штатную процедуру, не приписывая пропуск выгрузки ошибке генплана.
- [ ] 2.2 Получить необходимые метки, изображения, CSS и площади; подготовить привязку каждой части по прямым совпадениям. Проверить её на независимых контрольных участках, включая края и районы кандидатов; зафиксировать контрольные коды, локальную ошибку и ограничения для повторения проверки.
- [ ] 2.3 Разобрать обозначения Ривера и Фореста с указанным, но отсутствующим в снимке номером. Для каждого сохранить подтверждение с основаниями в `matches.yaml` либо причину нерешённости и недостающие сведения в отчёте; при необходимости проверить номер и расположение непосредственно в НСПД.
- [ ] 2.4 Аналогично разобрать обозначения Парка и Вилладжа с указанным, но отсутствующим в снимке номером. Проверить целевые контуры на уже связанные обозначения, общие земли и объединения; каждая рассмотренная запись должна иметь установленную связь либо объяснённый остаток.
- [ ] 2.5 Разобрать обозначения Ривера без исходной кадастровой ссылки. Проверить границы, окружение и доступную площадь; сохранить только обоснованные группы, явно фиксируя отсутствие ссылки, остальные коды перечислить с причиной.
- [ ] 2.6 Разобрать обозначения Парка без исходной кадастровой ссылки по районам с достаточным контролем. Сверить, что весь исходный остаток учтён в подтверждениях или списках нерешённых случаев; статус продажи и попадание метки в общий контур не должны подменять проверку.

## 3. Применение и повторяемая инструкция

- [ ] 3.1 Применить подтверждения через `pnpm parcels:update genplans --snapshot apps/www/src/data/parcel-sources`; новые контуры предварительно принять только в проверенном НСПД-снимке по действующей процедуре. Проверить каждую новую или изменённую группу: нынешний номер и геометрия из НСПД, все коды, основание, выбор основной записи и сохранность заметок/истории; цены частей не считать ценой целого.
- [ ] 3.2 Повторить импорт тех же снимков и реестра. Проверить отсутствие повторных изменений файлов и истории, а также объяснимость оставшихся несопоставленных обозначений и конфликтов. Если проверку ограничивает доступ к НСПД, явно указать затронутые коды и причину вместо объявления полного покрытия.
- [ ] 3.3 Добавить в `docs/parcel-updates.md` процедуру и готовый промпт по разделам 1–5 дизайна. Сверить инструкцию с фактически выполненным разбором: агент может повторить получение остатка, независимую проверку, запись оснований и импорт без сведений из памяти этой сессии.

## 4. Приёмка перед завершением реализации

- [ ] 4.1 Выполнить `pnpm openspec:validate` и проверить delta spec по правилам отбора долгоживущих требований: разовые количества и результаты остаются в отчёте, требования и сценарии согласованы с реализацией.
- [ ] 4.2 Выполнить затронутые тесты участков, `pnpm typecheck` и `pnpm build`; сопоставить сценарии delta spec с тестами либо сохранёнными результатами проверки реальных источников. Одноразово проверить созданные записи и их попадание в собранную карту без добавления постоянного обхода сайта в CI.
- [ ] 4.3 Провести ревью всего актуального diff, включая снимки, подтверждения, записи, документацию, staged/unstaged и новые файлы. Устранить ошибки и лишнюю сложность, проверить сохранность исходных кадастровых координат и обоснованность каждой новой связи; записать результаты проверок и оставшиеся вопросы в PR.
