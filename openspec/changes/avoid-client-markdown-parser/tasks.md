# Tasks

## 1. Подготовка реализации в следующей сессии

- [ ] 1.1 Сверить #842, согласованный change, чистоту/состояние ветки и отдельное разрешение владельца на реализацию; подтвердить эти условия перед правкой кода.
- [ ] 1.2 До изменения импортов запустить `pnpm bundle:analyze` и зафиксировать состав, размеры и запросы страниц `/map/`, карточки места, превью события и сравнения; проверить, что исходная цепочка к парсеру воспроизводится.

## 2. Развязать схему мест и упоминания

- [ ] 2.1 Убрать константы упоминаний из `places/schema.ts`, перевести `places/view.ts` и тест обратных ссылок на `SITE_MENTION_SECTIONS` из `mentions/types.ts`; проверить тест групп обратных ссылок и отсутствие runtime-импорта упоминаний в схеме мест.
- [ ] 2.2 Сохранить неизменными вычисление часов, HTML-карточку и общее превью; проверить существующие тесты часов, карточки и карт с расписанием и без него, а также `pnpm typecheck`.

## 3. Приёмка результата

- [ ] 3.1 Повторить `pnpm bundle:analyze`, сохранить актуальные `docs/bundle/client.yaml` и `docs/bundle/assets.yaml`; сравнить с исходным состоянием готовые JS/Brotli, точки входа и состав чанков, убедиться, что парсер не доставляется из-за часов.
- [ ] 3.2 На собранном сайте проверить сетевые запросы и сохранённые сценарии `/map/`, карточки места с часами и без них, превью события и сравнения; подтвердить работу карт и статуса, зафиксировать результаты до/после для PR.
- [ ] 3.3 Запустить `pnpm test` и `pnpm openspec:validate`; сверить результат с #842 и действующими `place-opening-hours`, `place-card-presentation`, `site-maps` и `news-event-places`.
- [ ] 3.4 До перевода draft PR в готовый вызвать reviewer по всему актуальному diff, включая коммиты, staged/unstaged и новые файлы; разобрать замечания, исправить подтверждённые и повторить затронутые проверки. Если reviewer недоступен, выполнить самостоятельный проход по регрессиям и отдельный `ponytail-review`, указав причину и отсутствие независимого ревью в PR.
