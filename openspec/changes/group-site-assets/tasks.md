## 1. Сопоставимый baseline

- [ ] 1.1 После отдельного разрешения реализации сохранить отметку разрешения в #629 со ссылкой на коммит артефактов; проверить, что это именно разрешение реализации обновлённого change, а не только публикации draft PR.
- [ ] 1.2 Собрать baseline на исходной конфигурации через `LIGHTHOUSE_DISABLE_ANALYTICS=true pnpm build` и зафиксировать условия измерений из `design.md`. Сохранить во временном каталоге сборку, категории существующих inline-вставок, размеры и зависимости ресурсов, скриншоты представительных страниц всех корневых разделов. Проверка: обе стороны будущего A/B используют ту же ревизию, контент, время, timezone и LCP-кандидатов; охвачены `/`, `/news/`, статья и архив, `/status/`, `/kb/`, `/map/`, `/sarafan/`, `/reviews/`, страницы `/people/` и `/meetings/`, `/815/compare/`, карточка поселка, `/815/regulation/` и 404.

## 2. Ограниченные эксперименты

- [ ] 2.1 Проверить простой CSS-кандидат «общий + разделовой» при штатном JS, начиная с правил `design.md` и общего пакета `815`. Проверка: состав и повторное использование пакетов, внешний CSS/JS без новых оптимизационных inline-блоков, предварительные серии минимум из трёх одинаковых cold mobile-прогонов проблемных и тяжёлых маршрутов.
- [ ] 2.2 Отдельно измерить влияние клиентской vendor-группы с `entriesAware: true` на том же CSS-кандидате. Проверка: повторяемые показатели и выходные импорты подтверждают отделение библиотек без ранней загрузки отложенных зависимостей, включения workspace-кода в vendor или изменения самодостаточных retryable-ресурсов.
- [ ] 2.3 Если простой кандидат проигрывает из-за лишних стилей или зависимости между пакетами, проверить один более выборочный вариант штатной группировки по выявленной причине. Проверка: в `design.md` зафиксированы сравнение, выбранная конфигурация и причины отказа от остальных; при близких результатах выбран более простой вариант, а при недоказанном выигрыше цель ускорения не объявлена выполненной.

## 3. Приёмка реализации

- [ ] 3.1 Выполнить `pnpm typecheck`, `pnpm test` и итоговый `pnpm build`; проверить ресурсы всех корневых разделов и внешний CSS/JS, отдельно сверив существующие служебные вставки Astro. Проверка: пакеты доступны, корректно переиспользуются и не включают CSS посторонних разделов, а production-аналитика сохраняется по ADR-004.
- [ ] 3.2 Выполнить финальные серии по пять одинаковых cold mobile-прогонов baseline и победителя на шести текущих Lighthouse-маршрутах и `/map/`. Проверка: сводка содержит медиану и диапазон Performance/FCP/LCP/TBT/CLS, сетевые CSS/JS/HTML-байты, число и время завершения блокирующих стилей; подтверждает критерии ускорения и отсутствия регрессий из specs. При спорном различии перепроверить серию, сохранив LHR-отчёты; проверить соответствие вывода реальным HTTP/2 и сжатию.
- [ ] 3.3 Сравнить тёплую загрузку, прямое открытие, переходы и возврат между разделами на мобильном и настольном viewport. Проверка: кеш переиспользуется, интерфейс не становится медленнее и сохраняет оформление; поиск, меню, календарь статуса, карта, Compare и калькулятор работают без новых ошибок консоли. Выполнить существующие browser-проверки `pnpm --filter @shelkovo/www test:browser:search-recovery` и `pnpm --filter @shelkovo/www test:browser:compare` для отложенной загрузки и восстановления.
- [ ] 3.4 Выполнить `pnpm openspec:validate`, проверить форматирование и весь актуальный diff, включая новые файлы; устранить ошибки и лишние усложнения. Проверка: реализация остаётся компактной штатной конфигурацией без нового сборочного слоя; заявленное улучшение относится к сопоставимым замерам приложения, а не к прежним пробам в памяти или другому контенту.

## 4. Завершение

- [ ] 4.1 После подтверждения критериев синхронизировать основные specs и архивировать change в той же ветке; повторить `pnpm openspec:validate`. Обновить #629 ссылкой на архив и существующий draft PR результатами A/B и проверок, затем передать PR на ревью. Проверка: `Closes #629` и готовность PR выставлены только после успешной реализации; до этого используется `Refs #629`.
