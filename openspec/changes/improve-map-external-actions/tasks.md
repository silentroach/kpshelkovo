# Tasks

Задачи реализации начинаются после ревью опубликованного change и явного разрешения владельца в [#797](https://github.com/silentroach/kpshelkovo/issues/797) или в разговоре с последующей отметкой в issue. Публикация planning-артефактов не означает такого разрешения.

## 1. Короткая штатная кнопка

- [x] 1.1 Добавить общий helper штатного контрола с подписью «Яндекс Карты» и подключить его в `map-preview.ts`, `PlaceMap.svelte` и `SettlementMap.svelte` с существующими позициями. Проверить целевыми тестами добавление контрола, передачу позиции, освобождение и отсутствие добавления после ухода со страницы; реальную автоматическую замену проверить в задаче 4.2.
- [x] 1.2 Согласовать готовность короткой кнопки с существующим handoff резервной ссылки; сократить верхнюю fallback-подпись события. Проверить в `components/places/tests/place-preview.test.ts` загрузку, ошибку, перенос фокуса и обычное восстановление после навигации; при отказе импорта контрола fallback сохраняет исходный URL. По уточнению владельца повторная загрузка после закэшированного SDK отказа допускает полную перезагрузку страницы.

## 2. Маркеры-ссылки сущностей

- [x] 2.1 Сделать корень маркера `PlacePreview.astro` обычной ссылкой на `place.mapUrl` с доступным именем и подсказкой. Проверить целевыми render/runtime-тестами исходный адрес, координатный fallback, сохранение корня-ссылки после клонирования и работу иконки, геометрии и статуса часов.
- [x] 2.2 Сделать маркер `MapCardShell.astro` ссылкой на уже переданный `mapUrl`, передать из `EventWidget.astro` контекст имени места или события. В `components/events/tests/event-widget.test.ts` проверить связанное место и inline-координаты, одинаковое назначение в новости и на странице мероприятия, отсутствие выдуманного маркера при неизвестных координатах и сохранение календарных действий.
- [x] 2.3 Сделать маркер шапки поселка ссылкой на тот же адрес, что у самостоятельных внешних ссылок страницы. В `compare/lib/tests/settlement-map-preview.test.ts` проверить сохранённый URL, в том числе внешний адрес не из Яндекса, и координатный fallback; проверить, что существующие desktop/mobile ссылки и отсутствие отдельного overlay-fallback сохраняются.
- [ ] 2.4 Настроить область нажатия, `title`, доступное имя, курсор и видимый фокус маркеров; согласовать кликабельность с фоновой композицией и жестами. В браузере проверить нажатие, Enter, копирование адреса, открытие в новой вкладке, отсутствие перехода после drag/pinch/прокрутки и сохранение географической позиции; если для подавления завершающего клика потребуется логика, покрыть её коротким поведенческим тестом.

## 3. Согласованность документации

- [ ] 3.1 Уточнить разделы карт в `docs/design/design-code-shelkovo.md` и внешние действия в ADR-039: короткая штатная кнопка, ссылка маркера и одна отдельная кнопка события. Сверить с delta `site-maps` и `event-widget`; общие требования не дублировать в спецификациях отдельных сущностей.

## 4. Приёмка и завершение

- [ ] 4.1 Выполнить целевые тесты карт, `pnpm typecheck` и `pnpm build`; зафиксировать команды и результаты. Проверки должны подтверждать URL и поведение, а не полный HTML или снимки CSS-классов.
- [ ] 4.2 На собранном сайте с настоящим SDK проверить карточки места с координатным и сохранённым URL, виджет события в новости и на странице мероприятия, шапку поселка, `/map/` и `/815/compare/`. Подтвердить одну короткую штатную кнопку без текстовой стрелки, сохранение логотипа и условий, переход кнопки на текущий кадр, ссылки маркеров сущностей и прежние действия обзорных маркеров, в том числе при одном результате фильтра. Зафиксировать исходящие URL; если внешняя страница недоступна для просмотра, отдельно указать оставшуюся визуальную проверку.
- [ ] 4.3 Проверить узкий экран, resize, клиентские переходы, задержку и отказ SDK/контрола, режим без JavaScript и клавиатурный handoff. Подтвердить доступность маркеров поверх фонового контента, отсутствие дубликатов после возврата на страницу и сохранение резервных ссылок. Сверить сетевые запросы с действующей CSP; при необходимости синхронно обновить разрешения по проектным правилам. Закрыть браузерные сессии после проверки.
- [ ] 4.4 Выполнить `pnpm openspec:validate` и ревью полного staged/unstaged/new diff по требованиям и сценариям; исправить найденные проблемы и лишнюю сложность. После проверки требований синхронизировать delta в основные specs штатным sync workflow, архивировать change и повторить структурную валидацию. Проверить, что оба общих правила появились в `openspec/specs/site-maps/spec.md`, а `event-widget` согласован с ними.
- [ ] 4.5 Опубликовать реализацию и открыть PR с `Closes #797`, ссылкой на архив change, результатами проверок и оставшейся ручной приёмкой. Обновить ссылку на change в issue и передать PR владельцу на человеческое ревью; проверить ссылки и label `ai`.
