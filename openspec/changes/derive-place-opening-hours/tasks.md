## 1. Согласование реализации

- [ ] 1.1 Получить явное разрешение владельца на реализацию после просмотра артефактов и оставить в issue #751 ссылку на коммит согласованного change. Проверка: доступна отдельная отметка разрешения; публикация PR со спецификацией сама по себе её не заменяет.

## 2. Данные и публичный контракт

- [ ] 2.1 Сделать `description` необязательным в raw-схеме, доменных типах и mapper-е places. Проверка: тесты `raw-schema.test.ts` и `load.test.ts` принимают расписание без пояснения, сохраняют заполненное пояснение и отсутствие всего расписания.
- [ ] 2.2 Добавить Zod-проверку пересекающихся и соприкасающихся периодов одного дня с контекстом места и дня. Проверка: короткие тесты отклоняют частичное пересечение, вложение, дубль и соприкосновение независимо от порядка записей; принимают настоящий перерыв и одинаковые часы в разные дни; ошибка загрузки содержит место и день.
- [ ] 2.3 Провести optional-пояснение через Public DTO, JSON-адаптер и клиентский адаптер карты; пересмотреть связанные discovery-описания. Проверка: `map-public.test.ts` валидирует контракт через Zod с пояснением и без него, сериализованный JSON сохраняет `periods` и опускает отсутствующий `description`; `PlaceMap.test.ts` подтверждает работу статуса без пояснения.

## 3. Единое представление расписания

- [ ] 3.1 Добавить общее форматирование недели в `lib/places/opening-hours.ts` для HTML и Markdown. Проверка: небольшие snapshots производных строк покрывают ежедневные часы, выходной посреди недели, соседние выходные, разделённые одинаковые часы, несколько интервалов и независимость от порядка исходных записей; дни с разными полными наборами интервалов не объединяются.
- [ ] 3.2 Проверить соответствие статуса отображаемым интервалам. Проверка: `opening-hours.test.ts` покрывает включённое начало, исключённый конец, перерыв, выходной и время закрытия текущего интервала в `Europe/Moscow`, в том числе при другом часовом поясе окружения.
- [ ] 3.3 Подключить строки расписания и необязательное пояснение в `PlaceOpeningHours.astro` и `lib/places/markdown.ts`. Проверка: тесты HTML- и Markdown-представлений подтверждают соответствие одному набору интервалов, наличие расписания без пояснения и вывод пояснения отдельно под ним; не сохранять snapshot целой страницы ради этого блока.

## 4. Редакционные данные и инструкция

- [ ] 4.1 Удалить дублирующие `description` из мест `burzhuyka`, `village-checkpoint` и `green-dreams`. Проверка: diff сохраняет исходные интервалы и отдельный текст о завтраках; сборка успешно загружает все три места.
- [ ] 4.2 Заменить ручной график в контакте Буржуйки ссылкой `[Часы работы — в карточке места](@burzhuyka)` и обновить правило часов в `apps/www/src/data/places/AGENTS.md`. Проверка: разово проверить HTML и Markdown контакта — ссылка ведёт на `/map/burzhuyka/`, ручной копии часов нет; инструкция описывает optional-пояснение и запрет конфликтов.

## 5. Приёмка и завершение

- [ ] 5.1 Выполнить `pnpm openspec:validate`, `pnpm typecheck`, `pnpm test` и `pnpm build`. Проверка: все команды завершаются успешно; разово проверить собранные HTML, Markdown и `/map/data/places.json` по сценариям спецификации.
- [ ] 5.2 Проверить карточки Буржуйки, КПП Вилладжа и Green Dreams в браузере на узком и широком экране, включая режим без JavaScript; на тестовых данных проверить перерыв и пояснение. Проверка: вся неделя читается без раскрытия и горизонтального скролла, вторник Буржуйки явно обозначен, интервалы не обрезаются, пояснение находится под графиком, динамический статус соответствует московскому времени. Результаты записать в PR; браузерные сессии закрыть.
- [ ] 5.3 Сверить весь diff с требованиями и убрать лишние изменения; после готовности реализации синхронизировать основной spec и архивировать change штатным workflow. Проверка: повторная `pnpm openspec:validate` успешна, в PR перечислены результаты приёмки, а ссылка из issue ведёт на актуальное расположение change.
