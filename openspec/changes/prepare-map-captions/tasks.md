# Tasks

## 1. Передача согласованного плана и исходные замеры

- [ ] 1.1 Перед редактированием кода сверить #845, #841, актуальный change, рабочую ветку и незакоммиченные файлы; найти явное разрешение владельца на реализацию после ревью draft PR и указать ссылку на него в задаче или issue. Без разрешения реализацию не начинать.
- [ ] 1.2 Зафиксировать исходные размеры и состав картографических JS из `docs/bundle/` и сетевые запросы представительных страниц; проверить, что сравнение относится к текущей базе, а не только к историческому срезу #845.

## 2. Подготовленные данные подписей (#845)

- [ ] 2.1 Добавить в `@shelkovo/markdown` безопасное форматирование обычного текста по действующим правилам Typograf; проверить тестами кавычки, неразрывные пробелы, буквальные entity, псевдотеги, Markdown и строки без преобразований.
- [ ] 2.2 Подготовить подписи точек в независимом display-представлении для HTML Markdown-карт и превью места; проверить сохранность исходной коллекции, скрытых строк, HTML-экранирование, проверку конечного payload до публикации и фактический текст маркера тестами.
- [ ] 2.3 Подключить ту же подготовку к `/map/data/places.json` через публичный адаптер без второго поля; проверить тестом, что `properties.iconCaption` подготовлен, исходная запись не изменена, остальные публичные свойства/координаты сохранены, а невалидный конечный payload останавливает сборку.

## 3. Лёгкая клиентская цепочка карты (#845)

- [ ] 3.1 Убрать типографику и HTML decoder из общего renderer-а: выводить полученный `iconCaption` только через `textContent`; проверить тестами буквальные символы разметки, отсутствие исполняемых элементов, отсутствие подписи у неподписанной точки и работу всех трёх потребителей.
- [ ] 3.2 Проверить фактические остаточные пути Typograf/YAML на `/map/` и остальных страницах; если их удерживает форматирование статического статуса через `places/view`/`status/view`, готовить display-текст при сборке данных карты и убрать тяжёлый клиентский импорт. Подтвердить состав чанков и отсутствие регрессии динамического отображения часов/состояния, включая сопутствующий Luxon-путь #843.

## 4. Код редакционных карт только на материалах с картами (#841)

- [ ] 4.1 Условно подключать bundled script карты в шаблонах новостей и KB по уже подготовленному результату Markdown-render-а; проверить на собранных страницах с картой и без неё отсутствие карты/её JS на текстовых страницах и наличие готовых карт на материалах с несколькими вставками.
- [ ] 4.2 Проверить тестом и в браузере переход без карты → с картой → без карты → с картой через Astro ClientRouter, работу всех вставок ниже экрана, печать, сообщения об ошибке и освобождение старого экземпляра; исправить способ условного подключения, если Astro всё же включает код карты на страницах без неё.

## 5. Документация и общая приёмка

- [ ] 5.1 Обновить применимые инструкции по редакционным картам и публичному `iconCaption`, отметить несовместимое изменение поля без изменения исходников/Markdown companion; проверить согласованность с `map-geodata`, `site-maps` и границами #858/#859.
- [ ] 5.2 Выполнить `pnpm typecheck`, затронутые тесты и `pnpm build`; затем `pnpm bundle:analyze`, сравнить запросы и суммарный вес JS на `/map/`, месте с геометрией, статье с картой, новости/KB без карты и обычном превью, сохранить обновлённые YAML-отчёты и не считать `rendered` сетевым весом.
- [ ] 5.3 Выполнить `pnpm openspec:validate`, сопоставить требования и сценарии delta specs с результатами тестов, браузерной проверки и сборки; не отмечать готовность по одной структурной валидации.
- [ ] 5.4 Перед переводом draft PR в готовый вызвать reviewer для всего актуального diff относительно точного SHA базы, включая staged/unstaged/новые файлы, требования, результаты проверок и ограничения; разобрать и исправить замечания, повторить проверки изменённых мест. При недоступности reviewer провести явную самопроверку регрессий и отдельный ponytail-review с указанием отсутствия независимого ревью.
