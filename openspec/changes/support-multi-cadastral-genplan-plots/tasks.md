# Tasks

## 1. Подтверждения и импорт

- [ ] 1.1 Расширить Zod-схему и чтение `matches.yaml` явной составной группой кадастровых номеров с проверкой уникальности и исходной ссылки; проверить тестом, что двойное назначение номера, неполная форма и изменённая ссылка не принимаются.
- [ ] 1.2 Научить `resolve.ts` и `reconcile.ts` обрабатывать одну группу с несколькими нынешними контурами, сохранять её идентичность при повторе и сообщать об отсутствующей составляющей или коллизии вместо усечённой записи; проверить тестами E35-подобный случай, повтор импорта и сохранение прежнего поведения одиночных записей, алиасов, заметок и истории.

## 2. Редакционная запись и публичные представления

- [ ] 2.1 Добавить составную форму frontmatter с отдельными кадастровыми номерами, геометриями и площадями, нормализацию доменной записи и индекс каждого номера без массового переписывания одиночных файлов; проверить тестами загрузку, суммы доступных площадей, уникальность номеров и отсутствие выдуманного общего номера.
- [ ] 2.2 Собирать один публичный элемент карты из всех геометрий группы с сохранением внутренней межи, отверстий, внутренней точки подписи и признака нескольких кадастровых участков; проверить тестом структуру feed, отличие группы от одного `MultiPolygon` и неизменность формата одиночных записей.
- [ ] 2.3 При выделении составной позиции любым контуром, клавиатурой или прямой ссылкой показывать на пять секунд доступную экранному диктору подсказку «Разбит на несколько участков»; проверить поведение карты для E35, одиночного участка и снятия выделения.
- [ ] 2.4 Убедиться, что Pagefind получает один результат на подтверждённую группу, отличает одинаковые короткие коды разных частей и ведёт к её общей ссылке; проверить сборкой индекса и ручным сравнением затронутых запросов до/после по правилам `site-search`.

## 3. Данные и терминология

- [ ] 3.1 Повторно сверить E35 по `docs/parcel-updates.md`: снимки, исходную ссылку и площадь генплана, местную привязку, независимые контрольные участки и увеличенное наложение обоих контуров; зафиксировать основания для `SHR-E35` → `:3385` + `:3386` в реестре без утверждения о юридической истории номера.
- [ ] 3.2 Применить подтверждение через штатный импорт принятых снимков и повторить его; проверить одну запись `SHR-E35` с двумя кадастровыми составляющими, одну подпись, совместное выделение и временную подсказку на карте, отсутствие повторного изменения файлов и истории, независимость `SHV-E35` и неизменность исходных координат НСПД.
- [ ] 3.3 Уточнить `GLOSSARY.md`, ADR-041 и необходимые рабочие пояснения в `docs/parcel-updates.md`: отличать кадастровый участок от редакционной группы и зафиксировать проверяемую процедуру её обновления; сверить формулировки с delta specs и фактической записью.

## 4. Приёмка

- [ ] 4.1 Проверить структуру и корректность change командой `pnpm openspec:validate`; устранить замечания до передачи на ревью.
- [ ] 4.2 Пройти сценарии четырёх delta specs по тестам, отчёту импорта, `pnpm typecheck`, `pnpm test`, `pnpm build` и ручной проверке карты/поиска; зафиксировать результаты и ограничения ручной приёмки для PR.
- [ ] 4.3 Провести ревью всего актуального diff, включая staged, unstaged и новые файлы; проверить сохранность кадастровой границы, отсутствие двойного учёта сведений, ссылок на источники и лишних изменений, исправить найденное до sync/archive и PR.
