# Tasks

## 1. Жесты интерактивных карт

- [ ] 1.1 Добавить учёт активных touch-контактов в `map-gestures.ts` и включать штатный `drag` только при двух пальцах либо основной кнопке мыши; проверить компонентными тестами второй контакт, отпускание/отмену одного касания, смену ввода, колесо и очистку слушателей.
- [ ] 1.2 На локальной странице с реальным SDK проверить Markdown-карту и интерактивное превью места: сдвиг двумя пальцами, pinch, вертикальную прокрутку и отсутствие сдвига одним пальцем, жест с началом на ссылке маркера, mouse drag и сохранение/повторный fit кадра по правилам каждого представления. Убедиться, что фоновые превью не получили drag; записать результат в итоговой приёмке.

## 2. Документация и приёмка

- [ ] 2.1 Обновить уточнение о touch-жестах в ADR-039 без переписывания истории решения; проверить, что действующее описание соответствует общему helper-у и не противоречит `site-maps` и `place-card-presentation`.
- [ ] 2.2 Выполнить `pnpm openspec:validate`, подходящие тесты карт и `pnpm typecheck`; сверить результат со всеми сценариями delta specs и зафиксировать оставшуюся ручную проверку, если нужна.
- [ ] 2.3 Просмотреть весь актуальный diff (staged, unstaged и новые файлы) на ошибки и лишнюю сложность, исправить найденное и повторить затронутые проверки.
