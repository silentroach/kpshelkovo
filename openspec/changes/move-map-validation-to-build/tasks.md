# Tasks

## 1. Исходное измерение

- [ ] 1.1 До изменения клиентских импортов выполнить `pnpm bundle:analyze`, сохранить исходные `docs/bundle/client.yaml` и `assets.yaml` для сравнения и зафиксировать вклад трёх клиентских схем, общего полигонального модуля и Zod. Проверить, что baseline относится к текущей базе реализации.

## 2. Проверенные данные без клиентских схем

- [ ] 2.1 Перенести `EditorialMapDataSchema` в `apps/www/src/lib/geometry/editorial-map-data-schema.ts` и применять её перед сериализацией в `lib/markdown/editorial-maps-html.ts`, передавая контекст источника и номер вставки. Через `editorial-maps-html.test.ts` проверить корректный payload, отказ на недопустимом конечном представлении с источником, номером вставки, полем и определимым ID объекта, а также сохранение script-safe экранирования; использовать существующую схему без переписывания ограничений.
- [ ] 2.2 Убрать runtime-импорт схемы и повторную проверку из `editorial-map-element.ts`, типизировать результат JSON-разбора существующей `EditorialFeatureCollection`. Проверить успешный показ и fallback на отсутствии данных, ошибке разбора JSON и SDK связанными тестами редакционной карты; проверки семантических ограничений должны выполняться на build-границе.
- [ ] 2.3 Убрать повторную проверку `EditorialPublicGeometrySchema` из `PlaceMap.svelte`, сохранив публичный DTO и public → domain адаптер. Проверить тестами геометрии, публичного представления мест и `PlaceMap`, что подготовленные координаты, оформление, ID `0` и цифровые метки сохраняются; отрицательное покрытие конечной геометрии оставить у build-адаптера.
- [ ] 2.4 Убрать ленивый импорт `ParcelMapPublicSchema` и клиентскую проверку участков, использовать существующий type-only `ParcelMapPayload`. Обеспечить новый запрос после обнаруженной ошибки применения ответа без повреждения актуального состояния поздним ответом. Проверить в `PlaceMap.test.ts` отложенную загрузку, кеш успешных данных, выключение/unmount, прямую ссылку, HTTP/JSON-ошибки и существующий сценарий неправильного wrapper с успешным вторым ответом и сохранением `p` до восстановления.
- [ ] 2.5 Сверить и выполнить связанные suites генераторов HTML/публичной геометрии, редакционных карт, мест и участков. Подтвердить отказ build-границ на ошибке в подготовленном представлении и сохранение публичных данных; добавить только недостающее адресное покрытие. Не подменять схемную проверку тестом случайного `TypeError` и не удалять проверки восстановления при переносе semantic rejection из клиента.

## 3. Итоговая приёмка

- [ ] 3.1 Выполнить `pnpm typecheck` и повторный `pnpm bundle:analyze`. Подтвердить успешную production-сборку с build-потребителями схем, отсутствие клиентских runtime-зависимостей схем и Zod через все три пути, включая включение участков, и уменьшение объёма затронутого JS. Сравнить JS/Brotli/gzip с baseline с учётом общих чанков; сохранить актуальные YAML-отчёты и фактические результаты для PR.
- [ ] 3.2 Выполнить `pnpm openspec:validate` и проверку форматирования затронутых файлов. Сверить результат со всеми сценариями delta `site-maps` и затронутыми требованиями `map-geodata` / `parcel-map`; повторить отбор долгоживущих требований перед последующим sync/archive.
- [ ] 3.3 Вызвать `reviewer` для всего актуального diff с issue, согласованным change, применимыми specs, целевой веткой, точным SHA базы и результатами проверок, включая staged, unstaged и новые файлы. Разобрать замечания, исправить подтверждённые и повторить ревью исправлений; при недоступности reviewer явно провести основной проход и отдельный `ponytail-review`, указав отсутствие независимого ревью. Итоговая сводка должна включать проверки, замеры и фактические ограничения приёмки.
