# Tasks

## 1. Контракт геоданных

- [ ] 1.1 Принять `iconContent` только как одну цифру у `Point` в raw-схеме, перенести в доменную модель; проверить тестом успешный разбор `0`–`9` и ошибки с источником, ID и полем для длинного текста, разметки и линии.
- [ ] 1.2 Провести значение через HTML-схему, публичную схему и адаптеры геометрии места; проверить регрессией raw→domain→HTML и raw→domain→public→map, включая отсутствие поля у обычной точки.

## 2. Отрисовка и материал

- [ ] 2.1 Показать одну цифру по центру существующего круглого маркера без изменения подписи `iconCaption`; проверить DOM-тестом маркированные и обычные точки и корректное применение цвета, затем сверить карту визуально на узком и широком экранах.
- [ ] 2.2 Перенести номера `1`, `2`, `3` из `move.geojson` в блок `map` новости об объезде, убрать временный файл после сверки; проверить, что остальные данные вставки и ссылка совпадают с источником, а Markdown companion сохраняет номера.
- [ ] 2.3 Обновить `docs/editorial-maps.md` правилом одной цифры и коротким примером; проверить, что инструкция согласуется со схемой и delta spec.

## 3. Приёмка реализации

- [ ] 3.1 Выполнить `pnpm openspec:validate`; проверить успешную структурную валидацию change.
- [ ] 3.2 Выполнить `pnpm typecheck`, профильные тесты и `pnpm build`, а также сверить сценарии delta spec на карте новости и в публичной геометрии места; записать результаты в существующий PR.
- [ ] 3.3 Проверить весь актуальный diff (staged, unstaged и новые файлы), исправить найденные проблемы и повторить только затронутые проверки; убедиться, что PR содержит итоговый объём изменения.
