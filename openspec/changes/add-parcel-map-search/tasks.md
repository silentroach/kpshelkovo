# Tasks

Реализация разрешена владельцем в [#804](https://github.com/silentroach/kpshelkovo/issues/804#issuecomment-5783998699). Рабочая ветка — `feat/parcel-map-search`, общий PR — [«Участки на карте и поиск по номерам»](https://github.com/silentroach/kpshelkovo/pull/810). Исследовательское демо не означает выполнения продуктовых задач ниже. По указанию владельца от 23 сентября 2026 года PR переводится в ready после проверок. Замечания к текущему этапу обсуждены, решения внесены в design.md и задачи ниже. Синхронизация specs и архивирование выполняются после реализации и проверки согласованных уточнений.

## 1. Модель и рабочие источники

- [x] 1.1 Добавить коллекцию `parcels`, raw-схему, отдельные domain types, mapper и loader по разделу 1 дизайна. Проверить тестами один нынешний контур с алиасами, соответствие пути основному коду, конфликт кодов/кадастрового номера, отсутствие площади и отсутствие выдуманного статуса при первичной неоднозначности; выполнить typecheck приложения.
- [x] 1.2 Подготовить schemas и чтение рабочих снимков `parcel-sources/`, реестр подтверждений и перенос исходного кадастра с его известной датой и основаниями из `capture.json`. Проверить Zod, сохранение EPSG:3857 в снимке, перевод всех колец Polygon/MultiPolygon в WGS84 без потери вершин и подтверждённые случаи L43/L44 и V43; сравнить с принятым исследовательским набором, не закрепляя его количество постоянным тестом.
- [x] 1.3 Реализовать явные сопоставления исходных обозначений, статусов, признаков и полной цены по разделу 4 дизайна. Проверить все поддержанные формулировки компактными примерами, остановку на неизвестных значениях и отсутствие вывода признаков по отдельным словам; использовать Zod на внешней границе.

- [x] 1.4 Перенести записи в `<part>/<first-letter>/<name>.md` по уточнённому § 1 дизайна и свести построение пути к одной функции для коллекции, mapper-а и обновлятора. Обновить чтение вложенных файлов, проверки путей, затронутые fixtures и примеры в документации. Однократно сверить побайтовую сохранность каждого перенесённого файла; тестами проверить чтение буквенных каталогов, несоответствие каталога основному коду и переименование со сменой буквы при сохранении тела и истории. Повторить обработку принятого снимка без новых изменений, выполнить typecheck, затронутые тесты и сборку.

## 2. Обновление и сохранность редакционных записей

- [x] 2.1 Реализовать вычисление действующего набора по двум снимкам и подтверждённым связям. Проверить на небольших fixtures прямую связь, подтверждение, конфликт изменившейся кадастровой ссылки, новый несопоставленный номер и отсутствие угадывания по близости.
- [x] 2.2 Реализовать обновление геометрии, объединение, разделение и выбор основной записи по `parcel-records`. Проверить сохранность заметок и истории выбранной записи, явный выбор при новом кадастровом номере, создание новых записей при разделении и отсутствие автоматического смешения сведений старых частей.
- [x] 2.3 Реализовать удаление алиаса, переименование по оставшемуся обозначению и удаление файла при исчезновении всех обозначений или кадастрового контура. Проверить каждый исход тестами обновления, включая дословное сохранение Markdown-тела и истории при переименовании.
- [x] 2.4 Реализовать объединение признаков, сохранение поля при конфликте, подтверждение цены целого и накопление истории. Проверить первую цену, повтор, изменение и возврат, ноль/отсутствие, проданный/недоступный статус, возврат в продажу, разные статусы частей и отсутствие новых наблюдений из старого генплана при кадастровом обновлении.
- [x] 2.5 Добавить CLI `pnpm parcels:update` с режимами genplans/nspd и воспроизведением сохранённого снимка. Проверить извлечение JSON без исполнения JavaScript, независимость от НСПД в режиме genplans, остановку всей партии до записи при ошибке страницы/формата, атомарную замену отдельного файла и ненулевой exit code при ошибке; включить скрипт в typecheck и тесты приложения.
- [x] 2.6 Добавить отчёт изменений и краткую `docs/parcel-updates.md` с получением кадастра, проверкой области/ответа, повтором, разбором конфликтов и восстановлением после прерывания записи. Проверить отчёт на примерах добавления, удаления, переименования и неоднозначности; выполнить одну повторную обработку принятого снимка и убедиться в отсутствии новых изменений записей и истории.
- [x] 2.7 Получить актуальные сведения всех четырёх генпланов, сформировать первоначальный Markdown-набор и проверить весь его diff. Сверить основной SHR-L43 и алиас SHR-L44, площадь V43 по НСПД, даты источников, список несопоставленных номеров и сохранность оснований; повторить обработку тех же входов без новых записей истории.

- [x] 2.8 Перевести поиск элементов `<script>` в `genplan-page.ts` на `parse5` по уточнённому § 3 дизайна, сохранив строгую проверку единственного присваивания, `JSON.parse` и Zod на границе. Короткими примерами проверить тегоподобный текст в HTML-комментариях, `>` внутри атрибутов, корректный источник, несколько присваиваний, неподдерживаемую форму скрипта и повреждённый JSON. Подтвердить отказ всей партии до записи и отсутствие исполнения JavaScript; выполнить typecheck, затронутые тесты и проверку разбора актуальных генпланов.

## 3. Отображение и статические данные

- [x] 3.1 Перенести настройку в `apps/www/src/config/parcel-map.yaml`, использовать один конфиг в демо и приложении и перенести проверенную функцию поправки. Проверить нулевой и ненулевой сдвиг, направление и величину на земле, согласованность подписей/контуров, отсутствие накопления и сохранность конфига после обоих режимов обновления; выполнить typecheck и сборку демо.
- [x] 3.2 Добавить отдельные public adapters и статические `/map/data/parcels.json` и `/map/data/parcel-search.json` по разделу 6 дизайна. Проверить Zod-контракты, один результат на основной код, отсутствие геометрии в поисковом payload и отсутствие коммерческих/редакционных полей в обоих; проверить сборку и компактную сериализацию.
- [x] 3.3 Зарегистрировать геометрический feed в slice карты и актуализировать `/map/index.md`, корневой llms-путеводитель и описание страницы. Проверить существующими contract-тестами стабильные ссылки и отсутствие внутренних путей, вручную сверить объяснение слоя и перехода; публичный Markdown собирать через AST, русские тексты отредактировать по `humanizer-ru`.
- [x] 3.4 Заменить центр bounds для `labelCoordinates` на Mapbox polylabel по уточнённому § 8 дизайна: helper в `packages/geo`, расчёт при сборке в Web Mercator, учёт отверстий и выбор лучшего кандидата MultiPolygon. Короткими тестами проверить внутреннее положение на вогнутом контуре и контуре с отверстием, выбор части с наибольшим отступом от границы и сохранность исходной геометрии. Выполнить typecheck и сборку; на карте проверить K29/K30, согласованность точки с поправкой отображения и отсутствие polylabel в клиентском bundle.

- [x] 3.5 Перенести применение поправки в build-time adapter геометрического feed по уточнённым §§ 6 и 9 дизайна; удалить чтение YAML и расчёт смещения из клиентского слоя. Актуализировать описание координат в `/map/index.md`, затронутом discovery и при необходимости ADR-041. Проверить тестами adapters согласованный сдвиг контуров и подписей, применение изменённой настройки, отсутствие накопления и сохранность исходных координат и площади. Выполнить typecheck и сборку; в production-артефакте и браузере проверить готовые координаты без параметров поправки, отсутствие клиентской зависимости от конфига и функции сдвига, совпадение с принятым демо и правильный фокус участка.

- [x] 3.6 Упростить `/map/data/parcels.json` по уточнённому § 6 дизайна: массив записей без обёртки, `aliases` только для непустого списка, `part` в каждой записи и стандартная структура GeoJSON. Согласованно обновить adapter, публичную Zod-схему, клиентские типы и чтение данных, затронутое описание формата. Проверить контракт массива и трактовку отсутствующего `aliases` как пустого списка, переход по существующему алиасу и сохранность колец Polygon/MultiPolygon; выполнить typecheck, затронутые тесты и сборку.

- [x] 3.7 Сосредоточить преобразования WGS84 ↔ Web Mercator и равномерный сдвиг в `packages/geo` по уточнённым §§ 1 и 9 дизайна. Использовать общие функции в импорте НСПД, расчёте polylabel и подготовке отображения; географические helpers должны использовать собственные типы координат вместо типов Яндекс Карт. Сверить потребителей в приложении и исследовательском демо. Проверить известную контрольную точку преобразования, нулевой и ненулевой сдвиг в метрах на опорной широте, сохранность колец и исходной геометрии, прежнее положение контуров и подписей; выполнить typecheck, затронутые тесты и сборки приложения и демо.

## 4. Слой участков общей карты

- [x] 4.1 Подключить модуль слоя к существующему экземпляру `PlaceMap`, добавить доступный переключатель и отложенный fetch с локальным повтором. Проверить DOM-тестами отсутствие fetch до действия, включение/выключение, повтор без лишнего запроса, выключение во время загрузки, ошибку и успешный retry, уничтожение во время запроса без позднего монтирования.
- [x] 4.2 Реализовать нейтральные контуры, основные короткие подписи при приближении и простой пятисекундный выбор. Проверить один контур для алиасов, переключение выбора и сброс таймера, Enter/Space и cleanup; убедиться, что маркеры мест и штатные действия Яндекса доступны поверх слоя.
- [x] 4.3 Добавить URL-helper и обработку `?p=` с разрешением полного кода/алиаса, фокусом смещённого контура и очисткой параметра. Проверить неизвестный код без смены вида, retry с сохранением намерения, расхождение словаря и геометрии, history state/query/hash, приоритет при смешанном URL, повторную навигацию и reduced motion.
- [x] 4.4 Отделить окончание выделения от сброса камеры: после перехода к участку камера сохраняется также при resize, после перезагрузки очищенного адреса слой выключен. Проверить это тестом жизненного цикла и браузером; прогнать действующие тесты `PlaceMap`, фокуса `h`, видимости мест, геометрии и превью после затронутых общих helpers. Изменённые Svelte-компоненты проверить официальным autofixer и повторить его после исправлений.
- [x] 4.5 Заменить отдельный checkbox кнопкой «Слои» с выпадающим списком по уточнённому § 8 дизайна. Добавить один пункт «Участки» как кнопку-переключатель с иконкой, названием и согласованными состояниями; последующие слои будут дополнять этот список. Проверить DOM-тестами `aria-expanded`/`aria-pressed`, переключение всей строкой и с клавиатуры, сохранение открытого списка при выборе, закрытие снаружи и по Escape с возвратом фокуса. Сохранить проверки ленивой загрузки, включая отсутствие запроса при одном открытии списка, и синхронизации состояния с переходом `?p=`. На desktop/mobile проверить оформление обоих состояний и доступность штатных действий карты; выполнить typecheck и официальный Svelte autofixer с повтором после исправлений.
- [x] 4.6 Убрать отдельный вывод полного кода и алиасов выбранного участка и обслуживающее его состояние по уточнённому § 8 дизайна. Актуализировать проверки выбора: выделение длится пять секунд, короткая подпись следует общему порогу масштаба, отдельный текст с обозначениями не появляется. Проверить выбор указателем, клавиатурой и переходом по алиасу, сохранение полного кода в доступном имени кнопки; выполнить typecheck, затронутые тесты и браузерную проверку.

- [x] 4.7 Сделать границы тоньше и мягче по уточнённому § 8 дизайна; добавить в adapter и публичную Zod-схему карты производный `muted` по § 6. Проверить обычную заметность только для `sold`, приглушение остальных и неизвестного статуса, одинаковое выделение и восстановление правильного оформления после таймера или выбора другого участка. Проверить, что исходный статус и другие коммерческие поля не попали в payload; актуализировать описание слоя и затронутые формулировки ADR-041. На карте с настоящим SDK подобрать прозрачность обычных и приглушённых контуров на desktop/mobile, сверить читаемость и доступность действий; выполнить typecheck, затронутые тесты и сборку.

- [x] 4.8 Оформить номера по одобренному статичному образцу и уточнённому § 8 дизайна: тёмный серо-зелёный текст Fira Sans 12 px/600 с тонким светлым ореолом, без прямоугольной плашки и рамки. На production-сборке с настоящим SDK проверить читаемость на разных участках подложки и выбранном контуре, на desktop/mobile и у порога zoom 17; проверить кликабельную область, Enter/Space и видимый фокус. Выполнить typecheck и официальный Svelte autofixer с повтором после исправлений; визуальное оформление принимать в браузере, без snapshot-тестов CSS-классов.

## 5. Общий поиск

- [x] 5.1 До правки поискового поведения просмотреть затронутые номерные и соседние обычные запросы через существующий интерфейс, по `site-search`. Сохранить краткие наблюдения URL, порядка и контекста для сравнения; проверить свежесть индекса. Это исходная точка приёмки, не постоянная матрица реального контента в CI.
- [x] 5.2 Добавить ленивый точный поиск по компактному словарю и композицию в `lib/search/site-client.ts`. Проверить полные/короткие номера и регистр, разные части, совпадение алиаса без дубля, отрицательные случаи с цифрами/префиксом, сохранение `p` в ссылке и отсутствие загрузки геометрии.
- [x] 5.3 Подключить композицию к общему диалогу, согласовать общий лимит/total и догрузку с Pagefind. Проверить отмену устаревших результатов, ошибку и повтор словаря, сохранение обычных текстовых результатов, догрузку без дублей, переход по Enter и возврат к поиску после Astro-навигации; сохранить действующие проверки подсветки Pagefind и восстановления загрузки.

## 6. Общая приёмка

- [x] 6.1 Выполнить `pnpm typecheck`, `pnpm test`, `pnpm --filter @shelkovo/www test:search-quality` и итоговую `pnpm build`, если после production-сборки поисковой проверки менялись её входы либо не собраны другие приложения. Проверить успешный production-артефакт и индекс; исправить причины всех связанных отказов. Новые постоянные тесты проверяют доменные и UI-контракты на компактных данных, а не полный снимок сайта.
- [x] 6.2 На production-сборке с настоящим SDK проверить desktop/mobile: обычное открытие без запроса участков, ручное включение, подписи, фокус и алиас, повторный переход, пять секунд выделения, сохранение камеры, reload, клиентскую навигацию, keyboard/reduced motion и ошибки данных с повтором. Сверить поправку с принятым демо и работоспособность мест/штатных действий; записать фактические bytes и время первого показа слоя, результат и оставшуюся ручную приёмку.
- [x] 6.3 На актуальном production-индексе вручную сравнить запросы из 5.1, включая L43/L44 и одинаковый короткий номер разных частей. Проверить приоритет точных совпадений, понятное различение, отсутствие геометрии в сетевых запросах поиска и сохранность полезной текстовой выдачи; кратко записать результат для PR.
- [ ] 6.4 Проверить доставку новых JSON под действующим `/map/data/`, content type, кеш и отсутствие новых внешних запросов/CSP-ошибок в браузере. При необходимых изменениях deploy/CSP синхронно обновить workflow/nginx и профильные документы, выполнить соответствующие проверки; не добавлять постоянный обход всех артефактов сайта ради разовой проверки доставки.

## 7. Документация и завершение change

- [x] 7.1 Зафиксировать один ADR в существующей серии об отдельном домене участков и точном поиске рядом с Pagefind, связать его с ADR-013/025/028/039 и текущими specs. Уточнить в `GLOSSARY.md` устойчивое различие участка, основного кода и алиаса; обновить ссылки и указатели в исследовательском README. Проверить согласованность с реализованной моделью и отсутствие второй копии задач или требований в документации.
- [ ] 7.2 Выполнить ревью полного актуального diff ветки, включая staged, unstaged и новые файлы, и `pnpm openspec:validate`. Сверить результат с каждым требованием и существенным сценарием, проверить собственную реализацию по `ponytail-review`, устранить расхождения; зафиксировать выполненные проверки.
- [ ] 7.3 После успешной приёмки синхронизировать основные specs и архивировать change штатными workflows в этой же ветке, повторив отбор долгоживущих требований и структурную валидацию. Обновить существующий PR и ссылки из issue на архив, указать выполненные проверки и оставшуюся ручную приёмку; проверить состав PR и оставить человеческое ревью и merge владельцу.
