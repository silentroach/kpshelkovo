# Tasks

## 1. Сократить поисковые проверки

- [ ] 1.1 Удалить матрицу, rank expectations, полные snapshots и её неиспользуемый код; сохранить четыре самостоятельных интеграционных сценария и сравнение результатов в пределах одного запуска, переименовать `resultSnapshot` в `readResults`. Проверка: ревью оставшихся сценариев и успешный запуск `pnpm --filter @shelkovo/www test:search-quality` в общей приёмке.
- [ ] 1.2 Заменить запросную карту `statusTargets` списком постоянных URL для корпуса; перенести исключение годовых и месячных архивов новостей в существующую проверку полного индекса. Проверка: тест корпуса подтверждает состав статуса, мероприятий и KB, а также отсутствие архивов новостей без перебора поисковых запросов.
- [ ] 1.3 Дополнить существующий fixture-тест отменённого мероприятия проверкой пометки внутри индексируемого body, с фиксированным тестовым временем для opt-in. Проверка: `pnpm --filter @shelkovo/www exec vitest run src/components/events/tests/events-view.test.ts`.
- [ ] 1.4 Перенести проверку состава поискового описания посёлка в один короткий unit-тест `src/compare/lib/tests/seo.test.ts` на искусственных данных; сохранить проверку форматирования тарифа в существующем наборе. Проверка: `pnpm --filter @shelkovo/www exec vitest run src/compare/lib/tests/seo.test.ts src/compare/lib/format.test.ts`.
- [ ] 1.5 Удалить `test:search-quality:update` из корневого и app `package.json`, сохранив обычную команду и config. Проверка: обе записи удалены, `test:search-quality` по-прежнему собирает сайт с индексом и запускает сокращённый набор.

## 2. Обновить рабочие инструкции

- [ ] 2.1 Обновить `site-search`, корневой и app-level `AGENTS.md`: ручная оценка релевантности по затронутым запросам вместо обязательной матрицы, snapshots и rank assertions; проверка пользы алиасов сохраняется. Уточнить актуальные пояснения о проверках в ADR-025 и ADR-035. Проверка: ревью всех изменённых инструкций, отсутствие действующих ссылок на удалённый snapshot/update и требований пополнять матрицу; исторические отчёты сохраняют прежние результаты.

## 3. Приёмка и завершение

- [ ] 3.1 Выполнить `pnpm test`, `pnpm typecheck`, `pnpm --filter @shelkovo/www test:search-quality` и `pnpm openspec:validate`. Проверка: успешные результаты; поисковая команда включает актуальную production-сборку, старые rank failures больше не требуют исключений. Зафиксировать выполненные проверки кратко, без нового отчёта выдачи.
- [ ] 3.2 Выполнить ревью всего staged/unstaged/new diff по `design.md`: все технические проверки учтены, существующие тесты клиента и границ body сохранены, изменения не затронули runtime, контент, веса, алиасы или правила корпуса. Проверка: исправлены замечания; итог описывает пересмотр тестов #747, а не улучшение ранжирования.
- [ ] 3.3 После приёмки архивировать change штатным workflow с `skip_specs: true`, без искусственных delta specs; при разрешённой публикации связать PR и итоговое резюме с #747. Проверка: архивная валидация проходит, задачи соответствуют фактическому результату, опубликованные ссылки указываются только после их создания. Production-приёмка после merge не требуется.
