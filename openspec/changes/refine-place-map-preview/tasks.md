# Tasks

## 1. Подготовка

- [x] 1.1 Получить явное разрешение реализации подготовленного change и сверить рабочую ветку с незакоммиченным переносом календарной кнопки. Проверка: владелец разрешил реализацию и push после завершения в текущую `feat/224-events-calendar`; календарная кнопка сохранена в `9392784a`.
- [ ] 1.2 На production-сборке зафиксировать ширину, высоту и положение адреса у карты места при 1440, 390 и 320 px. Проверка: сохранены измерения и скриншоты для сравнения с рамкой нового оформления.

## 2. Карта места

- [ ] 2.1 Применить к `PlacePreview` рамку, скругление и тень виджета, сохранив высоты `15rem`/`20rem`, breakpoint и ширину колонки; передать штатное `copyrightsPosition: 'bottom right'`. Проверка: размеры совпадают с исходными, логотип и копирайт справа внизу, адрес снаружи, атрибуция и действие карты не обрезаны и не перекрываются на узком экране.
- [ ] 2.2 Добавить явный режим pinch-зума только для карты места. Проверить штатный `pinchZoom` с настоящим SDK на touch- и трекпадном пути, при необходимости добавить минимальный адаптер сигнала масштабирования по design. Проверка: два пальца меняют масштаб карты без масштаба страницы; обычное колесо, свайп и прокрутка трекпада прокручивают страницу. Предметные тесты подтверждают отключённый режим у фоновых карт и освобождение добавленных слушателей при уходе/повторном входе.
- [ ] 2.3 Уточнить дизайн-гайд и ADR-028/039 под рамку, атрибуцию и разрешённый pinch места. Проверка: документы согласованы с обеими delta specs и не распространяют интерактивность на фоновую карту события или поселка.

## 3. Приёмка и завершение

- [ ] 3.1 Выполнить `pnpm typecheck`, подходящие тесты превью и `pnpm build`; проверить production-сборку с настоящим SDK в браузере: жесты и прокрутку, точку и MultiPolygon, начальный охват/resize, атрибуцию, фокус, fallback при ошибке, переходы Astro и фоновые карты. Проверка: записаны результаты, измерения и оставшаяся ручная проверка физического телефона/трекпада; браузерные сессии закрыты. При недоступном SDK проверка жестов остаётся незавершённой.
- [ ] 3.2 Проверить весь актуальный diff, синхронизировать основные specs и архивировать этот change после приёмки; выполнить `pnpm openspec:validate` и `git diff --check`. Проверка: контракты соответствуют результату, отчёт содержит фактические проверки; связанный `add-events-calendar` остаётся активным по указанию владельца. Публикация выполняется только при подтверждённом разрешении.
