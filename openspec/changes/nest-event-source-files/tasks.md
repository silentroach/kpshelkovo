# Tasks

## 1. Согласование и исходное состояние

- [ ] 1.1 Получить явное разрешение на подготовленный change, сохранить артефакты в рабочей ветке и оставить в #224 отметку со ссылкой на коммит; проверить наличие подтверждения и соответствие ветки PR #748.
- [ ] 1.2 До переноса зафиксировать ID, публичные URL, самостоятельные и новостные ICS-адреса и UID существующих мероприятий; результат сохранить для разового сравнения с итоговой сборкой.

## 2. Загрузка и перенос

- [ ] 2.1 Добавить предметную проверку `YYYY/MM/<event-id>.md` и соответствия папки московскому `starts_at` через Zod и существующие helpers; короткими тестами проверить корректный путь, неверное размещение, расхождение месяца и начало около границы UTC-месяца.
- [ ] 2.2 Подключить рекурсивную загрузку с исключением `AGENTS.md`, полным ключом коллекции и нормализацией ID перед построением доменных записей; тестом загрузочной границы подтвердить сохранение ID/ICS при смене папки и ошибку повторного ID в разных месяцах.
- [ ] 2.3 Перенести семь существующих записей в месячные папки и обновить прямые пути в news integration test; проверить в diff неизменность frontmatter/body и прохождение существующих тестов новостных URL и UID.

## 3. Инструкции и приёмка

- [ ] 3.1 Обновить корневой и локальный `AGENTS.md`, skill `news-maker` и `docs/news.md` по соответствующим skills; поиском и ревью убедиться, что действующие инструкции не требуют плоского хранения, а правило переноса файла при смене месяца явно описано.
- [ ] 3.2 Выполнить предметные тесты events, связанных новостей, mentions/backlinks и public surfaces, `pnpm typecheck` и `pnpm build`; сверить все зафиксированные ID, URL и ICS UID с итоговой сборкой и убедиться, что месячные папки не попали в публичный ID.
- [ ] 3.3 Провести ревью всего diff, выполнить `git diff --check` и `pnpm openspec:validate`; записать результаты проверки сценариев и исправить найденные расхождения до публикации.
- [ ] 3.4 По штатным workflows синхронизировать основную spec и архивировать change в той же ветке; повторной OpenSpec validation проверить итоговую структуру.
- [ ] 3.5 Закоммитить и запушить результат в PR #748, ответить в исходном обсуждении о новой структуре и вызвать `@codex review`; проверить актуальный HEAD PR и наличие опубликованных ссылок.
