# Tasks

## 1. Исходная выдача

- [x] 1.1 На актуальной локальной production-сборке зафиксировать в интерфейсе выдачу и холодные ресурсы Pagefind для полного кода, коротких номеров разных частей, подтверждённого алиаса, префикса, одних цифр и «участки», а также соседних текстовых запросов; результатом служат сопоставимые URL, порядок, snippets, highlights и байты до правки.

## 2. Единая выдача

- [x] 2.1 Добавить полный и короткий вариант основного кода и алиасов в `content` custom record, убрать индексируемую подпись раздела и больше не нужные фильтры; проверить на собранном индексе поиск обоих форматов, отсутствие массовых совпадений по «участки» и сохранность единственного результата на участок.
- [x] 2.2 Убрать filter-only запрос и объединение отдельных списков из клиента: для номерных запросов делать один текстовый вызов, для обычных сохранить обработку однословного запроса; нормализовать записи участков внутри общей порции с каноническим URL, частью и пояснением совпавшего алиаса. Проверить тестами клиента порядок и total, разные части, короткий алиас, догрузку, устаревшие ответы и retry.
- [x] 2.3 Актуализировать затронутые поисковые unit/DOM- и integration-тесты по контракту поведения, не закрепляя полную выдачу или случайные ранги; проверить `pnpm test` и `pnpm typecheck`.

## 3. Приёмка и документы

- [x] 3.1 Собрать production-сайт и индекс, выполнить `pnpm --filter @shelkovo/www test:search-quality` и вручную сравнить в интерфейсе запросы из 1.1 после правки: убедиться в доступности страниц, переходе на карту, корректной догрузке, полезности порядка и snippets; сравнить холодные ресурсы, байты и размер индекса с исходной сборкой и замером PR #810, записать существенные результаты в PR.
- [x] 3.2 Обновить ADR-041 по принятому полнотекстовому пути и проверить, что доменные алиасы и карты описаны без изменения смысла; выполнить `pnpm openspec:validate` и ревью всего diff, включая новые файлы.
- [x] 3.3 После прохождения приёмки синхронизировать `search` с основными specs и архивировать change штатными workflow; проверить `pnpm openspec:validate` и отразить итог проверок и оставшуюся ручную приёмку в PR #810.
