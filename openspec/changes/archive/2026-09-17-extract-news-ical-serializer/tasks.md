## 1. Разрешение реализации и baseline

- [x] 1.1 Подтвердить отдельное разрешение владельца на реализацию после ревью planning PR; проверить, что разрешение относится к актуальным артефактам, и оставить в issue #752 отметку со ссылкой на их коммит.
- [x] 1.2 До замены генератора зафиксировать URL, filename, UID и смысл календарных полей существующих событий для разового сравнения; проверить исходный baseline запуском `pnpm --filter @shelkovo/www test --project=node src/lib/news/calendar.test.ts src/lib/news/tests/event-places.test.ts src/lib/news/tests/static-paths.test.ts --update=none` и записать результаты.

Baseline: 25 тестов в трёх файлах прошли; исходная `pnpm build` успешна. Сохранены URL, filename и развёрнутые календарные строки всех пяти реальных `.ics` для разового сравнения после реализации.

## 2. Независимый сериализатор

- [x] 2.1 Создать private `@shelkovo/ical` с отдельными readonly-типами и renderer одного события; перенести UTC, TEXT escaping, folding и календарную оболочку. Проверить package typecheck, отсутствие app-импортов, сохранение явных значений и ошибки невалидной даты.
- [x] 2.2 Исправить parameter encoding по RFC 6868 и десятичную запись координат без потери значения; проверить кавычку, caret, backslash, CR/LF, ноль, отрицательную и очень маленькую координату короткими регрессионными тестами. Проверить, что URL с запятой или точкой с запятой сохраняется как URI.
- [x] 2.3 Добавить четыре `.ics`-эталона из design и проверки сырого вывода через `toMatchFileSnapshot`; выполнить `pnpm --filter @shelkovo/ical exec vitest run src/index.test.ts -u`, просмотреть созданные файлы и выполнить тот же тест с `--update=none`. Отдельно проверить CRLF, завершающий CRLF, лимит 75 UTF-8-байт с пробелом продолжения и сохранность длинной кириллицы/emoji; проверить сохранение CRLF эталонов при checkout.

## 3. Подключение к новостям

- [x] 3.1 Подключить пакет в `apps/www`, обновить lockfile и оставить `calendar.ts` новостным адаптером с совместимым фасадом. Проверить отсутствие оставшегося низкоуровневого сериализатора в приложении и неизменность route/filename/UID на baseline из 1.2.
- [x] 3.2 Заменить повторяющиеся полные app-snapshots точечными проверками правил адаптера: заголовок, выбор описания и уточнение, URL, `DTSTAMP`, явное окончание не через два часа, fallback, стабильность UID при правках и актуальные данные места. Проверить новые и существующие calendar/event-places/static-paths тесты; новые app-тесты размещать в ближайшей `tests/`.

## 4. Приёмка и передача

- [x] 4.1 Добавить README пакета с командами проверки/обновления эталонов, ссылками на четыре файла и ожидаемым результатом ручного импорта. Проверить соответствие инструкции фактическим файлам и командам; передать владельцу эти же снапшоты для проверки в Apple Calendar до merge.
- [x] 4.2 Выполнить `pnpm typecheck`, `pnpm test`, явный package snapshot-тест с `--update=none` и `pnpm build`; сравнить собранные реальные `.ics` с baseline по смыслу, включая имя файла, UTC, UID и географию. Зафиксировать результаты без постоянного smoke-обхода сайта в CI.
- [x] 4.3 Выполнить `pnpm openspec:validate` и ревью всего актуального diff, включая новые файлы, на соответствие `news-event-calendar`, `news-event-places` и design; исправить найденные дефекты и лишнюю сложность, подтвердить затронутые проверки повторно.
- [x] 4.4 После самостоятельной приёмки синхронизировать новую основную spec и архивировать change штатным workflow; повторить `pnpm openspec:validate`. Обновить PR и issue результатами, ссылкой на архив и оставшейся ручной приёмкой; явно указать, что merge реализации ожидает проверки импортированных файлов владельцем.

Самостоятельная приёмка 17 сентября 2026 года: `pnpm typecheck`, `pnpm test` (1399 тестов, включая 18 в пакете и 1287 в приложении), строгий package snapshot-тест, `pnpm build` и `pnpm openspec:validate` прошли. Все пять реальных экспортов совпали с baseline по URL, filename и развёрнутым календарным полям. Четыре эталона сохранили точные байты CRLF при checkout с `core.autocrlf=true`; разовый проверочный тест удалён. Ревью охватило staged, unstaged и новые файлы; lockfile ограничен подключением workspace-пакета. Ручной импорт по `packages/ical/README.md` остаётся владельцу до merge.
