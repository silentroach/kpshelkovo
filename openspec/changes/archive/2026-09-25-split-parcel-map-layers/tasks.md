# Tasks

Реализация запланирована для отдельной сессии в ветке `docs/862-split-parcel-map-layers`. Эти чекбоксы относятся к реализации и её приёмке, а не к публикации планового PR.

## 1. Подготовка реализации

- [x] 1.1 Прочитать артефакты и комментарии issue/PR, проверить состояние той же ветки и подтвердить явное разрешение реализации. Сохранить в issue отметку со ссылкой на коммит одобренных артефактов; наличие опубликованного плана само по себе разрешением не считать.
- [x] 1.2 До изменений сохранить исходные bundle-отчёты через `pnpm bundle:analyze`, замерить запросы участков при обычном открытии и прямой ссылке, записать примеры выдачи полного/короткого кода и алиаса из разных частей. Проверить актуальность production-индекса и сохранить краткую базу сравнения для приёмки; `pnpm dev` без просьбы владельца не запускать.

## 2. Данные частей и поисковый индекс

- [x] 2.1 Добавить подготовку четырёх компактных JSON и статический маршрут `/map/data/parcels/[part].json` с общей поправкой до разбиения. Проверить Zod-контракты конечных массивов, принадлежность кодов и алиасов части и равенство объединения файлов подготовленному общему набору без потерь, дублей или изменения координат; для регрессии расширить прицельные тесты адаптера без снимка всего реального набора.
- [x] 2.2 Перевести `apps/www/scripts/build-search-index.ts` на четыре файла, восстановить общий порядок по коду и общую проверку уникальности кодов и алиасов. Проверить коротким тестом сборщика ошибку при отсутствующем/повреждённом файле и конфликте между файлами, а на собранном индексе — один результат на позицию, алиасы и URL по основному коду без геометрии в записи.
- [x] 2.3 Удалить маршрут общего JSON и его устаревшие URL-helpers, описание в Markdown карты и запись в реестре публичных поверхностей; новые файлы не добавлять как агентские feeds. Обновить затронутые проверки Markdown/реестра и разово проверить свежую сборку: четыре JSON присутствуют, общего файла и его сжатых вариантов нет, агентские документы не ссылаются на удалённый feed, Markdown-индекс доступен.

## 3. Независимые части и панель выбора

- [x] 3.1 Заменить единый флаг и запрос частей независимыми выбором, кешем и выполняющимися запросами в экземпляре карты; расширить существующий рендерер для добавления/удаления частей с одним общим выделением. Поведенческими тестами проверить независимую готовность, повторное использование данных, один запрос при выключении/включении до ответа, отсутствие позднего показа после отмены/ухода, выбор участков между частями и сохранение соседних контуров при переключении.
- [x] 3.2 Сделать общий трёхсостоянийный чекбокс и сразу видимые отдельные строки «Форест / Вилладж / Парк / Ривер» с существующими иконками и короткими названиями. Показывать на кнопке «Слои» маркер активного выбора без числа при хотя бы одной выбранной части, в том числе при закрытой панели. Проверить пустое/смешанное/полное состояние, выбор любой комбинации, появление и исчезновение маркера, сохранение открытой панели и камеры, клавиатуру, Escape и внешнее нажатие; визуально проверить порядок, иконки, отсутствие счётчика и область нажатия от 44 px. Выполнить официальный Svelte autofixer для изменённых компонентов и повторить после исправлений.
- [x] 3.3 Убрать текст/индикатор загрузки участков, видимую строку ошибки и отдельную кнопку повтора. Выделять неудачную часть красным и знаком ошибки, оповещать скринридер с названием части, повторять обычным включением; проверить сетевой отказ, неразбираемый JSON и ошибку применения: выбор сбрасывается только у неудачной части, частичные объекты убраны, успешные части доступны, неудачный результат не кешируется.

## 4. Переход к участку по номеру

- [x] 4.1 Определять файл по префиксу полного кода, разрешать основной код/алиас без учёта регистра и включать нужную часть. Проверить один запрос по прямой ссылке, нынешние масштаб и пятиисекундное выделение всех контуров позиции, reduced motion, сохранение камеры при resize, удаление только `p` с сохранением history state, остальных параметров и hash, повтор исходной ссылки и обычное открытие очищенного адреса.
- [x] 4.2 Сохранить различие неизвестного номера, ошибки и отмены перехода. Проверить неверную форму без запроса, отсутствующий код без обхода других частей и смены выбранных слоёв, сохранение `p` при отказе, выполнение исходного фокуса при следующем включении части и отмену без позднего фокуса при явном выключении; сохранить приоритет `p` над `h`.

## 5. Согласованность документации

- [x] 5.1 Уточнить доставку данных в ADR-041 и ссылку на источник поисковых записей в skill `site-search`, применив предметные skills для ADR и агентских инструкций. Проверить актуальные ссылки и отсутствие обещания общего feed; исторические архивы changes не переписывать. Сверить delta specs с фактическим результатом, не добавляя в основные требования разовые размеры, список текущих позиций или описание миграции.

## 6. Приёмка результата

- [x] 6.1 Выполнить `pnpm openspec:validate`, `pnpm typecheck`, `pnpm test` и `pnpm --filter @shelkovo/www test:search-quality` (включает production build). Выполнить `pnpm search:prepare` для второго выхода сборщика индекса; записать результаты и устранить подтверждённые ошибки.
- [x] 6.2 Проверить карту в браузере на широком и узком экране, включая малую высоту панели: чекбоксы, любую комбинацию, независимое скрытие, отсутствие видимых текста ожидания/ошибки и кнопки повтора, красный знак неудачной части и доступное оповещение, восстановление после отказа, доступность штатных действий и повторную клиентскую навигацию. Проверить фокус и короткие подписи, статусы продажи и позицию из нескольких кадастровых контуров по действующим требованиям; приложить результат ручной приёмки к PR.
- [x] 6.3 Сравнить запросы и байты для одной части, всех частей и прямой ссылки, проверить отсутствие загрузки выключенных частей и повторного запроса готовых данных. Сравнить поисковые примеры с исходным индексом и записать итог; не вводить точные ранги или количества реальных записей как постоянный тест.
- [x] 6.4 Повторить `pnpm bundle:analyze`, сравнить состав клиентских модулей и размеры с базой, сохранить актуальные `docs/bundle/client.yaml` и `docs/bundle/assets.yaml`. Проверить, что новые загрузки не вернули клиентский код Zod-валидации карт; объяснить существенные изменения в PR.
- [x] 6.5 Перед переводом планового draft PR в готовый PR реализации вызвать `reviewer` из основной сессии с issue, артефактами, применимыми specs, точным SHA базы, всей веткой и рабочим diff, результатами проверок и ограничениями. Разобрать замечания, исправить подтверждённые и повторить ревью после правок; при недоступности reviewer выполнить основной проход и отдельный `ponytail-review`, явно указав fallback и его охват в PR.

После этих задач выполнить штатные sync/archive workflows в той же ветке, повторить структурную валидацию и итоговое ревью, обновить существующий PR по проектному жизненному циклу. До реализации и её приёмки change остаётся активным, а PR ссылается на issue через `Refs`.
