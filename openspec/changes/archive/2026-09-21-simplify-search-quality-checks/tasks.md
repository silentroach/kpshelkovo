# Tasks

## 1. Сократить поисковые проверки

- [x] 1.1 Удалить матрицу, rank expectations, полные snapshots и её неиспользуемый код; сохранить четыре самостоятельных интеграционных сценария и сравнение результатов в пределах одного запуска, переименовать `resultSnapshot` в `readResults`. Проверка: ревью оставшихся сценариев и успешный запуск `pnpm --filter @shelkovo/www test:search-quality` в общей приёмке.
- [x] 1.2 Заменить запросную карту `statusTargets` списком постоянных URL для корпуса; перенести исключение годовых и месячных архивов новостей в существующую проверку полного индекса. Проверка: тест корпуса подтверждает состав статуса, мероприятий и KB, а также отсутствие архивов новостей без перебора поисковых запросов.
- [x] 1.3 Дополнить существующий fixture-тест отменённого мероприятия проверкой пометки внутри индексируемого body, с фиксированным тестовым временем для opt-in. Проверка: `pnpm --filter @shelkovo/www exec vitest run src/components/events/tests/events-view.test.ts`.
- [x] 1.4 Перенести проверку состава поискового описания посёлка в один короткий unit-тест `src/compare/lib/tests/seo.test.ts` на искусственных данных; сохранить проверку форматирования тарифа в существующем наборе. Проверка: `pnpm --filter @shelkovo/www exec vitest run src/compare/lib/tests/seo.test.ts src/compare/lib/format.test.ts`.
- [x] 1.5 Удалить `test:search-quality:update` из корневого и app `package.json`, сохранив обычную команду и config. Проверка: обе записи удалены, `test:search-quality` по-прежнему собирает сайт с индексом и запускает сокращённый набор.

## 2. Обновить рабочие инструкции

- [x] 2.1 Обновить `site-search`, корневой и app-level `AGENTS.md`: ручная оценка релевантности по затронутым запросам вместо обязательной матрицы, snapshots и rank assertions; проверка пользы алиасов сохраняется. Уточнить актуальные пояснения о проверках в ADR-025 и ADR-035. Проверка: ревью всех изменённых инструкций, отсутствие действующих ссылок на удалённый snapshot/update и требований пополнять матрицу; исторические отчёты сохраняют прежние результаты.

## 3. Приёмка и завершение

- [x] 3.1 Выполнить `pnpm test`, `pnpm typecheck`, `pnpm --filter @shelkovo/www test:search-quality` и `pnpm openspec:validate`. Проверка: успешные результаты; поисковая команда включает актуальную production-сборку, старые rank failures больше не требуют исключений. Зафиксировать выполненные проверки кратко, без нового отчёта выдачи.
- [x] 3.2 Выполнить ревью всего staged/unstaged/new diff по `design.md`: все технические проверки учтены, существующие тесты клиента и границ body сохранены, изменения не затронули runtime, контент, веса, алиасы или правила корпуса. Проверка: исправлены замечания; итог описывает пересмотр тестов #747, а не улучшение ранжирования.
- [x] 3.3 После приёмки архивировать change штатным workflow с `skip_specs: true`, без искусственных delta specs. Проверка: архивная валидация проходит, задачи соответствуют фактическому результату. Production-приёмка после merge не требуется.

Публикация после архивной валидации выполнена: ветка отправлена, создан [PR #775](https://github.com/silentroach/kpshelkovo/pull/775) с итоговым резюме и `Closes #747`. Учитывается отдельно от архивного чекбокса, чтобы проверка архива до PR не зависела от уже созданного PR.

## Результаты проверок

- 21 сентября 2026: оба точечных прогона прошли — 16 тестов мероприятий и 15 тестов описания/форматирования тарифа.
- `pnpm test`: 7 успешных workspace-задач, в `apps/www` — 208 файлов и 1445 тестов; `pnpm typecheck` прошёл.
- `pnpm --filter @shelkovo/www test:search-quality`: production-сборка и индекс успешны, 4/4 интеграционных теста прошли.
- `pnpm openspec:validate`: 16/16 основных specs и активных changes, 17/17 архивных changes прошли.
- После архивирования: `pnpm openspec:validate` — 15/15 основных specs, 18/18 архивных changes прошли.
- Ревью полного diff подтвердило переносы из `design.md`, сохранность тестов клиента и границ body и соответствие `skip_specs: true`. Действующих ссылок на удалённые snapshot/update нет; исторические отчёты сохранены. Результат закрывает пересмотр проверок #747, не заявляя улучшение ранжирования.
