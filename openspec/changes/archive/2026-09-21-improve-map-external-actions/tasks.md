# Tasks

Задачи реализации начинаются после ревью опубликованного change и явного разрешения владельца в [#797](https://github.com/silentroach/kpshelkovo/issues/797) или в разговоре с последующей отметкой в issue. Публикация planning-артефактов не означает такого разрешения.

## 1. Короткая штатная кнопка

- [x] 1.1 Добавить общий helper штатного контрола с подписью «Яндекс Карты» и подключить его в `map-preview.ts`, `PlaceMap.svelte` и `SettlementMap.svelte` с существующими позициями. Проверить целевыми тестами добавление контрола, передачу позиции, освобождение и отсутствие добавления после ухода со страницы; реальную автоматическую замену проверить в задаче 4.2.
- [x] 1.2 Согласовать готовность короткой кнопки с существующим handoff резервной ссылки; сократить верхнюю fallback-подпись события. Проверить в `components/places/tests/place-preview.test.ts` загрузку, ошибку, перенос фокуса и обычное восстановление после навигации; при отказе импорта контрола fallback сохраняет исходный URL. По уточнению владельца повторная загрузка после закэшированного SDK отказа допускает полную перезагрузку страницы.

## 2. Маркеры-ссылки сущностей

- [x] 2.1 Сделать корень маркера `PlacePreview.astro` обычной ссылкой на `place.mapUrl` с доступным именем и подсказкой. Проверить целевыми render/runtime-тестами исходный адрес, координатный fallback, сохранение корня-ссылки после клонирования и работу иконки, геометрии и статуса часов.
- [x] 2.2 По уточнению владельца оставить маркер `MapCardShell.astro` декоративным под контентом, без ссылки и фокуса. В `components/events/tests/event-widget.test.ts` проверить связанное место и inline-координаты, одинаковое поведение в новости и на странице мероприятия, исходный URL резервной ссылки, отсутствие выдуманного маркера при неизвестных координатах и сохранение календарных действий.
- [x] 2.3 По уточнению владельца оставить маркер шапки поселка декоративным, без ссылки и фокуса. В `compare/lib/tests/settlement-map-preview.test.ts` проверить сохранённый URL самостоятельных desktop/mobile ссылок, в том числе внешний адрес не из Яндекса, координатный fallback и отсутствие отдельного overlay-fallback.
- [x] 2.4 Настроить область нажатия, `title`, доступное имя, курсор и видимый фокус маркера самостоятельного превью места. В браузере проверить нажатие, Enter, доступность контекстного меню для копирования адреса, открытие в новой вкладке, отсутствие перехода после drag/pinch/прокрутки и сохранение географической позиции; если для подавления завершающего клика потребуется логика, покрыть её коротким поведенческим тестом.

## 3. Согласованность документации

- [x] 3.1 Уточнить разделы карт в `docs/design/design-code-shelkovo.md` и внешние действия в ADR-039: короткая штатная кнопка, ссылка маркера и одна отдельная кнопка события. Сверить с delta `site-maps` и `event-widget`; общие требования не дублировать в спецификациях отдельных сущностей.

## 4. Приёмка и завершение

- [x] 4.1 Выполнить целевые тесты карт, `pnpm typecheck` и `pnpm build`; зафиксировать команды и результаты. Проверки должны подтверждать URL и поведение, а не полный HTML или снимки CSS-классов.
- [x] 4.2 На собранном сайте с настоящим SDK проверить карточки места с координатным и сохранённым URL, виджет события в новости и на странице мероприятия, шапку поселка, `/map/` и `/815/compare/`. Подтвердить одну короткую штатную кнопку без текстовой стрелки, сохранение логотипа и условий, переход кнопки на текущий кадр, ссылки маркеров сущностей и прежние действия обзорных маркеров, в том числе при одном результате фильтра. Зафиксировать исходящие URL; если внешняя страница недоступна для просмотра, отдельно указать оставшуюся визуальную проверку.
- [x] 4.3 Проверить узкий экран, resize, клиентские переходы, задержку и отказ SDK/контрола, режим без JavaScript и клавиатурный handoff. Подтвердить доступность маркеров поверх фонового контента, отсутствие дубликатов после возврата на страницу и сохранение резервных ссылок. Сверить сетевые запросы с действующей CSP; при необходимости синхронно обновить разрешения по проектным правилам. Закрыть браузерные сессии после проверки.
- [x] 4.4 Выполнить `pnpm openspec:validate` и ревью полного staged/unstaged/new diff по требованиям и сценариям; исправить найденные проблемы и лишнюю сложность. После проверки требований синхронизировать delta в основные specs штатным sync workflow, архивировать change и повторить структурную валидацию. Проверить, что оба общих правила появились в `openspec/specs/site-maps/spec.md`, а `event-widget` согласован с ними.
- [x] 4.5 Опубликовать реализацию и открыть PR с `Closes #797`, ссылкой на архив change, результатами проверок и оставшейся ручной приёмкой. Обновить ссылку на change в issue и передать PR владельцу на человеческое ревью; проверить ссылки и label `ai`.

## 5. Уточнение после ревью PR #798

Владелец попросил вернуть маркеры фоновых карт в декоративный слой и продолжить работу в том же PR. Пункты 2.2 и 2.3 пересмотрены; результаты первоначальной приёмки в разделе 4 дополняются проверками ниже.

- [x] 5.1 Исправить mock SDK в тестах `SettlementsExplorer`, воспроизведя пять ошибок CI `maps.import is not a function`; проверить связанные тесты.
- [x] 5.2 Согласовать proposal, design, delta specs, дизайн-гайд и ADR-039 с декоративными фоновыми маркерами. Синхронизировать основные specs.
- [x] 5.3 Выполнить полный `pnpm test`, `pnpm typecheck`, `pnpm build` и браузерную проверку событий и hero: карта и непрозрачный маркер образуют единую сцену с `opacity: 0.4`, в hero общий градиент затухания включает маркер; карта не просвечивает через маркер. Фоновый маркер находится за контентом, без фокуса и перехода; штатные действия и fallback работают. Закрыть браузерные сессии.
- [x] 5.4 Провести ревью diff, повторно архивировать change, выполнить `pnpm openspec:validate`, опубликовать исправления в существующем PR и дождаться всех CI-проверок, включая workflow с тестами.
