## 1. Восстановление в новой сессии

- [x] 1.1 Прочитать #724, все артефакты change, отметку разрешения со ссылкой на коммит и фактическое состояние `feat/724-installed-app-shortcuts`, включая незакоммиченные файлы. Проверка: согласованный коммит доступен в истории ветки, разрешение относится к этим артефактам, следующий шаг определён без пересказа прежнего чата владельцем. При отсутствии подтверждения разрешения запросить его до изменения кода.
- [x] 1.2 Оставить в #724 краткий результат настоящей передачи. Проверка: комментарий новой сессии ссылается на change и коммит согласования, сообщает восстановленный следующий шаг и возникшие затруднения, если они были; он не выдаёт передачу контекста за проверку shortcuts.

## 2. Manifest и подключение

- [x] 2.1 Создать статический endpoint `apps/www/src/pages/manifest.json.ts` по `design.md`, используя `createJsonResponse`, `themeColor` и готовые PNG. Проверка: параметры и порядок трёх shortcuts соответствуют спецификации, JSON компактен; новые зависимости, клиентский код и вспомогательные слои для констант не добавлены.
- [x] 2.2 Подключить `/manifest.json` в общем head `apps/www/src/layouts/BaseLayout.astro`. Проверка: в layout одна ссылка `rel="manifest"` с корневым путём, существующие метаданные имени, цвета и иконок согласованы с manifest.

## 3. Локальная приёмка

- [x] 3.1 Выполнить `pnpm typecheck`, `pnpm test` и `pnpm build`. Проверка: команды завершаются успешно, сборка содержит `dist/www/manifest.json`; результаты сохранены для описания PR.
- [x] 3.2 Разово проверить собранный manifest через Zod и раздать production-сборку локальным preview. Проверка: поля приложения, имена и порядок пунктов соответствуют spec; URL принадлежат текущему origin и scope; manifest отвечает HTTP 200 с JSON MIME, обе PNG загружаются с заявленными размерами, `/`, `/news/`, `/status/`, `/kb/` отвечают HTTP 200 с HTML без промежуточной главной. Разовую проверку не превращать в постоянный smoke-обход CI.
- [x] 3.3 Проверить локальную сборку браузером без установки приложения на `/`, `/news/`, `/status/`, `/kb/`, `/815/compare/` и после клиентского перехода между разделами. Проверка: на каждой странице одна корректная ссылка на manifest, обычная навигация работает, загрузка manifest успешна; разрешение same-origin manifest и иконок подтверждено чтением production CSP. Закрыть браузерную сессию и остановить поднятый preview после проверки.
- [x] 3.4 Выполнить `pnpm openspec:validate`, проверить форматирование изменённых файлов и весь актуальный diff, включая staged, unstaged и новые файлы, по требованиям и с `ponytail-review`. Проверка: валидация и форматирование проходят, замечания исправлены, каждому сценарию соответствует результат локальной проверки или согласованная проверка параметров вместо реальной установки; в результатах для PR явно указано, что системное меню на устройствах не проверялось по решению владельца.
