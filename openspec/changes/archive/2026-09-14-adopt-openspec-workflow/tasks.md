## 1. Воспроизводимая интеграция OpenSpec

- [x] 1.1 Закрепить точные версии OpenSpec и установщика skills; проверить frozen-установку зависимостей по lock-файлу.
- [x] 1.2 Зафиксировать параметры штатной генерации и реализовать запуск с изолированным временным профилем; проверить одинаковый состав семи workflows при чистом и отличающемся глобальном профиле.
- [x] 1.3 Сохранить штатную интеграцию OpenCode в Git и исключить её из форматирования; проверить повторную генерацию и formatter с явными путями, как при обработке staged-файлов.
- [x] 1.4 Добавить проверку расхождения генерации; убедиться, что изменение, удаление и лишний generated-файл дают ошибку, а штатный набор проходит.

## 2. Проектный набор skills и уборка

- [x] 2.1 Закрепить Git SHA и проверяемые хеши сохраняемых внешних skills, включая обязательный проектный `agent-browser`; проверить восстановление выбранных ревизий штатным установщиком без использования глобальных skills.
- [x] 2.2 Добавить bootstrap с проверкой состава и исходных хешей; проверить повторную установку и отказ при пропущенном, изменённом или лишнем skill.
- [x] 2.3 Удалить девять управляющих skills и отдельно согласованный `github-actions-docs` из проектной установки и lock-файла; проверить отсутствие их активных вызовов в сохранённых инструкциях.
- [x] 2.4 Удалить проверенные `.superpowers/` и пустые остатки `tailwind-design-system` / `.opencode/skills/news-maker`; записать инвентаризацию и результат удаления ignored-материалов в отчёт приёмки.
- [x] 2.5 Проверить методы на отсутствующие документы, старые планы и конкурирующие хранилища; исключить согласованные с владельцем методы и перечислить конкретные остаточные находки.

## 3. Единая политика и документация

- [x] 3.1 Добавить единый раздел жизненного цикла в `AGENTS.md` и ссылки на него из README и OpenSpec config; сверить владельцев информации, согласование, самостоятельность и завершение со spec `development-workflow`.
- [x] 3.2 Адаптировать `project-issues`; закрепить в проектной политике источники требований, весь актуальный diff, связи issue/change/PR, выбор `Refs`/`Closes` и границу человеческого ревью.
- [x] 3.3 Закрепить использование `GLOSSARY.md`, `docs/decisions/` и текущего change в проектных правилах; сохранить совместимые внешние методы в исходном виде.
- [x] 3.4 Записать ADR о выборе процесса и обновить навигацию; явно отделить исторические планы и отчёты от действующей политики, сохранив относящиеся к ним факты и незавершённую приёмку.
- [x] 3.5 Документировать один путь установки, обновление и откат; проверить инструкции фактическим выполнением команд и отметить необходимость перезапуска OpenCode для загрузки новой интеграции.

## 4. CI и приёмка этапа

- [x] 4.1 Добавить в CI строгую OpenSpec-валидацию, проверку архивов и генерации для соответствующих изменений; проверить workflow подходящим инструментом и выполнить его команды локально.
- [x] 4.2 Выделить OpenSpec из общей dev-группы Dependabot; проверить, что правило оставляет ему самостоятельный PR.
- [x] 4.3 Восстановить предполагаемое изменение в чистой временной копии без ignored-остатков и глобальных skills; выполнить bootstrap дважды, проверки генерации и подходящие проверки конфигурации/скриптов, записать результаты и отсутствие побочных изменений.
- [x] 4.4 Проверить соответствие реализации обеим specs и весь актуальный diff, применить необходимые упрощения; зафиксировать проверки и остаточную ручную приёмку отдельно от структурной валидации.
- [x] 4.5 После выполнения критериев синхронизировать основные specs и проверить их строгую валидацию; сверить все требования и сценарии с дельтами.
- [x] 4.6 Подготовить итоговый отчёт для #736 и описание PR со ссылками на change, результатами установки и уборки; проверить `Refs #734`, выбор закрытия #736 и явную передачу функционального пилота #724 следующему этапу.

После этих проверяемых задач выполняется служебное завершение по `AGENTS.md`: архивирование change, проверка завершённости архива, commit/push и PR. Эти действия завершают тот же объём #736; их результат фиксируется в issue и PR. Они вынесены из чекбоксов, чтобы архивирование не зависело от уже открытого PR, а PR — от уже выполненного архивирования.

## 5. Уточнение владельца в ревью PR

- [x] 5.1 Удалить `code-review`, `domain-modeling`, `idea-refine` и `code-simplification` из установки, lock и patches; проверить отсутствие активных вызовов в оставшихся инструкциях.
- [x] 5.2 Повторить установку сокращённого набора, проверки хешей и OpenSpec; сверить актуальный diff и обновить отчёт и PR.

## 6. Полный отказ от patches

- [x] 6.1 Удалить остальные 12 патченных skills, каталог patches и код их применения; проверить отсутствие активных вызовов удалённых методов.
- [x] 6.2 Проверить повторную установку исходного upstream-набора, тесты tooling и OpenSpec; обновить отчёт и описание PR.

## 7. Сокращение по итогам общего ревью

- [x] 7.1 Удалить выбранные владельцем 12 skills: `grill-me`, `teach`, `resolving-merge-conflicts`, `prototype`, `ponytail-help`, `ponytail-gain`, `ponytail-debt`, `research`, `handoff`, `documentation-and-adrs`, `wizard`, `to-questionnaire`; проверить отсутствие их активных вызовов.
- [x] 7.2 Проверить установку 16 оставшихся внешних skills и валидацию OpenSpec; обновить отчёт и описание PR.

## 8. Worktree только по запросу

- [x] 8.1 Изменить `project-issues` и требования выбора рабочей копии; сверить три сценария: обычный issue, явный запрос отдельного worktree и запрос нескольких параллельных тикетов.
