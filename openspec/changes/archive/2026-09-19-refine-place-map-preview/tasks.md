# Tasks

## 1. Подготовка

- [x] 1.1 Получить явное разрешение реализации подготовленного change и сверить рабочую ветку с незакоммиченным переносом календарной кнопки. Проверка: владелец разрешил реализацию и push после завершения в текущую `feat/224-events-calendar`; календарная кнопка сохранена в `9392784a`.
- [x] 1.2 На production-сборке зафиксировать ширину, высоту и положение адреса у карты места при 1440, 390 и 320 px. Проверка: сохранены измерения и скриншоты; у Green Dreams размеры 481.828125×320, 318×240 и 248×240 px. Положение подписи дополнительно проверено временной DOM-подстановкой и существующим тестом страницы с адресом.

## 2. Карта места

- [x] 2.1 Применить к `PlacePreview` рамку, скругление и тень виджета, сохранив высоты `15rem`/`20rem`, breakpoint и ширину колонки; передать штатное `copyrightsPosition: 'bottom right'`. Проверка: размеры совпадают с исходными, логотип и копирайт справа внизу, адрес снаружи, атрибуция и действие карты не обрезаны и не перекрываются на узком экране.
- [x] 2.2 Добавить явный режим pinch-зума только для карты места. Проверить штатный `pinchZoom` с настоящим SDK на touch- и трекпадном пути, при необходимости добавить минимальный адаптер сигнала масштабирования по design. Проверка: два пальца меняют масштаб карты без масштаба страницы; обычное колесо, свайп и прокрутка трекпада прокручивают страницу. Предметные тесты подтверждают отключённый режим у фоновых карт и освобождение добавленных слушателей при уходе/повторном входе. Capture-обработчик переключает штатные behaviors; отдельный расчёт масштаба не нужен.
- [x] 2.3 Уточнить дизайн-гайд и ADR-028/039 под рамку, атрибуцию и разрешённый pinch места. Проверка: документы согласованы с обеими delta specs и не распространяют интерактивность на фоновую карту события или поселка.

## 3. Приёмка и завершение

- [x] 3.1 Выполнить `pnpm typecheck`, подходящие тесты превью и `pnpm build`; проверить production-сборку с настоящим SDK в браузере: жесты и прокрутку, точку и MultiPolygon, начальный охват/resize, атрибуцию, фокус, fallback при ошибке, переходы Astro и фоновые карты. Проверка: 1539 тестов, typecheck и сборка прошли; проверены настоящий SDK и браузерные touch/wheel-события. Результаты и оставшаяся человеческая проверка записаны в verification.md; браузерная сессия закрыта.
- [x] 3.2 Проверить весь актуальный diff, синхронизировать основные specs; выполнить `pnpm openspec:validate` и `git diff --check`. Проверка: контракты соответствуют результату, отчёт содержит фактические проверки; связанный `add-events-calendar` остаётся активным по указанию владельца. OpenSpec проверил 15 актуальных элементов и 12 архивов перед архивированием без ошибок; diff проверен.

После завершения проверок архивировать этот change штатным workflow и опубликовать изменения в текущую ветку по разрешению владельца. Это завершающая операция над уже проверенными артефактами.
