# Tasks

## 1. Регрессии загрузчика

- [x] 1.1 Добавить в `apps/www/src/lib/status/tests/timeline.lazy.test.ts` проверку одного пересчёта на последовательность `after-swap` → `page-load` после загрузки модуля, включая новый DOM при переходе и возврат назад; до исправления тест должен показать два вызова вместо одного.
- [x] 1.2 Добавить проверки управляемой загрузки: взаимодействие и переход ожидают общий проход, несколько замен DOM обрабатывают текущую страницу, уход на страницу без таймлайнов не вызывает гидратацию и replay удалённого маркера; до исправления воспроизвести дублирующие вызовы.
- [x] 1.3 Проверить восстановление после отклонённого promise и отсутствие преждевременной загрузки при навигации без взаимодействия; после повторной попытки подсказка открывается, модуль не скачивается повторно после успеха.

## 2. Исправление

- [x] 2.1 Оставить `page-load` единственным событием навигационного пересчёта и объединить ожидающую гидратацию для взаимодействия и навигации в `timeline.lazy.ts`; проверить новые регрессии и существующие тесты первого pointer/focus/touch-взаимодействия командой `pnpm --filter @shelkovo/www exec vitest run src/lib/status/tests/timeline.lazy.test.ts src/lib/status/timeline.dom.test.ts`.
- [x] 2.2 Добавить интеграционную проверку начала и конца события между `after-swap` и `page-load` с настоящим DOM-модулем; проверить фазу, доступную подпись и открытую подсказку после `page-load`, а также актуальное время при завершении задержанной загрузки. Прогнать тесты таймлайна командой `pnpm --filter @shelkovo/www exec vitest run src/lib/status`.

## 3. Приёмка и завершение

- [x] 3.1 Выполнить `pnpm typecheck` и `pnpm build`; проверить отсутствие ошибок типов и успешную production-сборку.
- [x] 3.2 На локальной production-сборке проверить `/status/` → страница сервиса → назад: один навигационный пересчёт, сохранение первой подсказки и однократную загрузку модуля. Зафиксировать число проходов и ориентировочную стоимость на странице с несколькими таймлайнами до/после, без постоянного smoke-обхода в CI; закрыть браузер и preview после проверки.
- [x] 3.3 Проверить весь diff, включая новые файлы, на соответствие spec и совместимость области изменения с PR #159; исправить найденные проблемы, проверить форматирование затронутых файлов и выполнить `pnpm openspec:validate`.
- [x] 3.4 Повторить отбор долгоживущих требований, синхронизировать spec и архивировать change штатным workflow; подтвердить результат командой `pnpm openspec:validate`. Подготовить итог для PR с результатами проверок и `Closes #741`; commit, push и создание PR выполнять только при явном разрешении владельца.

## 4. Согласованные замечания ревью PR #770

- [x] 4.1 Воспроизвести потерю pointer/focus/touch-взаимодействия после замены DOM при уже загруженном модуле; добавить захват намерения до `page-load` и replay после единственного финального пересчёта. Проверить сброс намерения при следующем переходе и сохранение ленивой загрузки.
- [x] 4.2 Убрать механику загрузчика из основной и delta spec, сохранив пользовательские гарантии и технические проверки в design, задачах и тестах.
- [x] 4.3 Выполнить тесты статуса, typecheck, production-сборку, браузерную приёмку, проверку diff, форматирования и OpenSpec; подготовить правки и ответы на оба замечания для публикации в PR.

## 5. Отмена устаревшего намерения по повторному ревью

- [x] 5.1 Воспроизвести уход указателя и фокуса до `page-load` и во время загрузки модуля; отменять replay завершённого взаимодействия. Сохранить переход внутри маркера и открытие по касанию.
- [x] 5.2 Уточнить условие актуальности взаимодействия в design и обеих specs, выполнить тесты, typecheck, сборку, браузерную проверку, ревью diff, форматирование и OpenSpec; подготовить ответ на замечание.
