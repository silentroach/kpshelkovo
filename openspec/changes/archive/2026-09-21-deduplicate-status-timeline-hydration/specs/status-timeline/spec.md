# Spec Delta

## Purpose

Таймлайн помогает жителю видеть периоды перебоев и узнавать подробности событий. Его клиентское поведение сохраняет актуальность статусов при навигации и подключает интерактивность по первому взаимодействию.

## ADDED Requirements

### Requirement: Ленивое подключение подсказок

Пока модуль интерактивности не запрошен, таймлайн SHALL сохранять серверное представление до первого наведения, фокуса или касания маркера, не загружая модуль заранее. После успешной загрузки первое взаимодействие SHALL открывать соответствующую подсказку, если маркер ещё находится на текущей странице. Ошибка загрузки SHALL позволять повторную попытку при следующем взаимодействии.

#### Scenario: Первое взаимодействие

- **WHEN** житель впервые наводит указатель, переводит фокус или касается маркера
- **THEN** модуль загружается, таймлайн актуализируется и подсказка открывается без повторного действия

#### Scenario: Уход со страницы во время загрузки

- **WHEN** житель уходит со страницы до завершения загрузки модуля
- **THEN** завершение загрузки не открывает подсказку удалённого маркера и не возвращает прежнюю страницу

#### Scenario: Повторная попытка после ошибки

- **WHEN** загрузка модуля не удалась и житель снова взаимодействует с маркером
- **THEN** система повторяет попытку подключения интерактивности

### Requirement: Актуализация при завершении навигации

После подключения интерактивности таймлайны текущей страницы SHALL актуализировать периоды, фазы, доступные подписи и содержимое подсказок по времени завершения клиентского перехода, включая возврат назад. Если модуль ещё загружается, актуализация SHALL использовать время его готовности и текущую страницу. Уведомления разных стадий одного перехода SHALL не вызывать дублирующую полную пересборку таймлайнов; ожидающие одну загрузку запросы SHALL объединяться.

#### Scenario: Переход к сервису и возврат

- **WHEN** модуль уже загружен и житель переходит к сервису или возвращается назад
- **THEN** таймлайны новой страницы актуализируются при завершении перехода без отдельной полной пересборки на промежуточной стадии

#### Scenario: Временная граница внутри перехода

- **WHEN** событие начинается или заканчивается между заменой страницы и завершением перехода
- **THEN** итоговые фаза, доступная подпись и подсказка учитывают пересечённую границу

#### Scenario: Несколько запросов во время загрузки

- **WHEN** первое взаимодействие и завершение перехода ожидают одну загрузку модуля
- **THEN** после её завершения таймлайны текущей страницы пересчитываются одним общим проходом
