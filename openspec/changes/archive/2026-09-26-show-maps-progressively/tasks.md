# Tasks

## 1. Подготовка реализации

- [x] 1.1 В отдельной сессии прочитать change, отметку в #851 и фактическое состояние `perf/show-maps-progressively`; получить явное разрешение реализации после чтения артефактов и зафиксировать его в issue со ссылкой на коммит согласованного плана. Проверка: разрешение подтверждено, рабочая копия и исходный SHA известны.
- [x] 1.2 Показать владельцу пересмотренные артефакты без резервных действий, получить явное разрешение продолжить реализацию и зафиксировать его в #851 со ссылкой на сохранённый план. Проверка: согласование подтверждено после чтения обновлённого change, незакоммиченные правки сохранены.

## 2. Постепенный показ превью и независимые действия

- [x] 2.1 В `MapPreview` убрать скрытие canvas, ожидание всех тайлов и резервные действия места и события, сохранив отдельное сообщение об ошибке. Проверка: тесты покрывают видимость карты до готовности тайлов/контрола и отсутствие резервной ссылки при ожидании, отказе и без JavaScript; браузер показывает сцену без перекрытия.
- [x] 2.2 Показывать только готовое короткое штатное действие независимо от тайлов, не переводить фокус с других ссылок и сохранить самостоятельные ссылки поселка и маркер места. Проверка: тесты покрывают медленный контрол, частичные тайлы, отказ импорта, фокус и повторную загрузку; браузер подтверждает отсутствие переходной и дублирующей кнопки события при появлении автоматического действия SDK.

## 3. Остальные renderer-ы без перекрывающего ожидания

- [x] 3.1 Убрать загрузочную панель и скрытие canvas у `EditorialMapElement`, сохранив обработку ошибки и раннюю инициализацию всех схем. Проверка: тест с задержанным контролом, проверка ошибок и удаления экземпляра; в браузере несколько схем ниже экрана загружаются без прокрутки и печатаются после готовности с подложкой, объектами, атрибуцией и подписями.
- [x] 3.2 Убрать перекрывающую загрузочную панель `PlaceMap` и независимое от неё ожидание контрола перед добавлением подготовленных маркеров/геометрии. Проверка: тесты задержанного и отклонённого импорта, уничтожения карты во время ожидания; в браузере работают кластеры, выделение по URL, переходы и слой участков без возврата общего лоудера.
- [x] 3.3 Убрать панель и спиннер `SettlementMap`, добавлять маркеры независимо от загрузки штатного контрола и сохранить необходимые состояния управления. Проверка: тесты поздней готовности/отказа/повторной загрузки, маркеров и попапов; в браузере работают фильтры, повторный показ и восстановление после отказа, область резервирования места оболочки сохраняет размеры.

## 4. Документация и общая приёмка

- [x] 4.1 Актуализировать пояснения ADR-039 и разделы о загрузке и действиях карт в дизайн-гайде. Проверка: правила описывают отсутствие резервных действий, самостоятельные ссылки и постепенный показ в соответствии с delta specs.
- [x] 4.2 Повторить после правок `pnpm openspec:validate`, `pnpm typecheck`, `pnpm test` и сборку с обновлением отчётов через `pnpm bundle:analyze`; сравнить состав и размеры с отчётами до изменений. Проверка: команды проходят, изменения бандлов объяснены и актуальные YAML сохранены вместе с реализацией.
- [x] 4.3 Пройти в браузере сценарии обоих delta specs на перечисленных в proposal поверхностях: постепенный показ и готовность контрола без резервного действия, мобильный/десктоп, первое/повторное открытие, Astro-навигация, ошибки, no-JS, указатель/клавиатура, атрибуция, прокрутка и отсутствие сдвигов; скрытая шапка ждёт показа. Проверка: записаны наблюдения и оставшаяся ручная приёмка; к #851 подготовлено короткое резюме подтверждённых причин и дальнейших шагов без объявления полной загрузки ускоренной по одному факту удаления лоудера.

Наблюдения при приёмке: на локальной сборке с реальным SDK видны карта, объекты и атрибуция без перекрытия на `/map/`, карте сравнения, превью места, поселка и события, редакционной схеме; на мобильном превью событие оставляет одну короткую кнопку и независимые ссылку на место и ICS. Прокрутка над превью места работает, фокус на ICS сохраняется при появлении кнопки. Повторное открытие карты сравнения и возврат через Astro-навигацию восстанавливают карту; фильтр, попап, слой участков и подсветка по URL работают. Скрытая мобильная шапка не запускает SDK до появления при расширении экрана. При блокировке SDK сообщения и повтор сравнения доступны, резервные переходы не появляются; при отключённых внешних скриптах превью не создаёт перехода, самостоятельные ссылки остаются. Единственную опубликованную редакционную схему продублировали только в DOM для проверки раннего старта нескольких схем ниже экрана; PDF после готовности содержит подложку, объекты, атрибуцию и подпись. На dev-сервере сравнение не гидратировалось из-за `504` оптимизированных зависимостей Vite, поэтому его проверяли на собранном сайте через `localhost`; production-приёмка и скорость полной загрузки внешнего SDK этим не подтверждены.

- [x] 4.4 Вызвать `reviewer` для всего актуального diff, включая новые, staged и unstaged файлы, требования change и результаты проверок; разобрать замечания, исправить подтверждённые и повторить ревью после правок. Проверка: замечания закрыты или обоснованно отклонены; при недоступности reviewer выполнены и явно отмечены самостоятельный основной проход и отдельный проход `ponytail-review`. После sync/archive итоговое ревью повторяется по проектному workflow перед переводом того же draft PR в готовый к ревью.

После синхронизации и архивации reviewer повторно проверил весь diff. Замечания к формулировке ADR-039 и четырём относительным ссылкам архивного `design.md` исправлены; контрольное ревью подтвердило отсутствие новых проблем и лишней сложности.
