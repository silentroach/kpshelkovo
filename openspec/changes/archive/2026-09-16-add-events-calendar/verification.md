# Проверка add-events-calendar

## Текущий статус после последних уточнений владельца

Последние уточнения выполнены в рамках той же задачи #224 и PR #748. Основная спецификация и архивная delta описывают текущий согласованный объём. Прежняя приёмка ниже сохранена как история; актуальные результаты приведены в этом разделе.

- Canonical при смене месяца начала или slug меняется напрямую; связанные ссылки обновляются в том же изменении. URL-алиасы, редиректы совместимости, генератор `apps/www/dist/events-redirects.conf` и его deploy-интеграция исключены. Усиление deploy-helper отменено: используется его реализация до этой доработки. Прежние проверки редиректов и отката не описывают поставляемые возможности PR.
- Ограничения числовых slug и `list`, уникальность итоговых URL, стабильные ID/DOM-якоря строк дня и опубликованные ICS-адреса/UID сохранены. Тесты прямого изменения месяца/slug и обновления редакторских ссылок прошли. На работающем dev проверены главная, день и текущий адрес сада — 200; незанятый `/events/2026/04/apple-garden/` — 404 без редиректа. После сборки `events-redirects.conf` отсутствует, подключений генератора и deploy-артефакта больше нет.
- Нижняя навигация удалена со дня и detail; тесты подтверждают единственный переход к месяцу/дню в breadcrumbs. На карте мероприятия увеличена видимость подложки, убрана event-only ширина iframe 100%, сохранена общая с новостью геометрия 150%. На 1440/390/320 px видны дороги, текст читается, переполнения нет; news-карточка до/после pixel-identical. Скриншоты `event-before-*.png`, `event-after-*.png`, `news-before-1440.png`, `news-after-*.png` находятся в `/var/folders/sb/82f4cg9n6z182c5q0k6dn3hr0000gn/T/opencode/`. Браузерная сессия закрыта, пользовательский dev-сервер не остановлен.
- Подтверждённый запрос `календарь мероприятий` сохраняется через `SearchDocument.aliases` на detail. Это поисковая metadata, не URL-алиасы. Последний раздел [search-verification.md](search-verification.md) фиксирует сравнение с footer-вариантом: полезные результаты сохранены, snippets содержат настоящее содержимое, соседние запросы не ухудшились. Этот отчёт ведёт поисковая/родительская ветка работы; прежнее утверждение об индексации нижней ссылки больше не действует.
- Повторные `pnpm typecheck`, `pnpm test` и `pnpm build` прошли: **1313 тестов приложения + 94 shared = 1407**, 344 страницы, 227 документов Pagefind. Сборка завершилась в 20:05:52. Независимое ревью всего текущего diff не нашло ошибок; astro config, CI workflow и deploy-helper возвращены к состоянию до добавления редиректов. Все **15 требований и 51 сценарий** основного spec и архивной delta совпадают; задачи 9.5 и 9.9 закрыты по этим результатам.

## История приёмки подробных страниц — до последних уточнений

Change возобновлён по запросу владельца 16 сентября 2026 года в рамках той же задачи #224 и PR #748. После предъявления обновлённых proposal/design/spec/tasks владелец ответил: «конечно. не стесняйся пользовать субагентов и параллелить где это возможно». Следующие результаты относятся к версии до отказа от URL-алиасов и нижней навигации; актуальный объём и ожидающая приёмка указаны выше.

Согласованные артефакты сохранены в `b39f4d5e`, разрешение отражено в [issue](https://github.com/silentroach/kpshelkovo/issues/224#issuecomment-5700783963). Основная спецификация и delta сверены по всем 15 требованиям и 51 сценарию, CLI повторного архивирования подтвердил отсутствие оставшихся изменений. При архивировании открытыми были только сама операция 8.1 и обновление PR 8.2; после выполнения они закрыты. PR #748 и issue #224 обновлены, задача остаётся открытой до человеческого merge.

### Доработка подробных страниц: результаты

- Все семь мероприятий имеют страницы `/events/YYYY/MM/<slug>/`; краткие дни и месячный список ведут на них. Сохранились прежние day#id якоря, ID записей, семь общих и пять исторических ICS-адресов и UID.
- Все пять ссылок в трёх новостях переведены на единственный `YYYY/MM/<slug>`; проверялись отказ для старого ID, устаревшего адреса и несуществующей связи, коллизии адресов, числовые slug и `list`. Проверявшийся тогда перенос через HTTP 301 отменён владельцем; актуальная проверка прямой смены адреса ожидается по задаче 9.5.
- Общая `MapCardShell.astro` используется в новости и карточке места. Проверены название без координат, координаты без названия, неизвестное место, длинный текст и отмена. Удалены неиспользуемые варианты новостной карточки и прежняя математика смещения карты; URL без смещения закреплён точечным тестом.
- Финальные `pnpm typecheck` и `pnpm test` прошли: **1325 тестов приложения и 94 shared-теста, всего 1419**. `pnpm build` завершился **в 19:15:04**, 344 страницы, 227 документов Pagefind, 7791 слово. После финального упрощения сохраняются те же индексируемые текст и URL. `git diff --check` прошёл.
- Структурная валидация `pnpm openspec:validate` прошла. Независимое correctness- и ponytail-review включило существующие коммиты PR и весь unstaged/untracked diff относительно merge-base с `origin/master` (`83358be5`). Подтверждённых ошибок не осталось; обе найденные возможности упрощения выполнены.

### Поиск

- Baseline снят до правок, snapshot точно совпал с `f7061696`. Update и обычный search-quality после исправлений: **13 passed / 5 failed**, без snapshot mismatches. Все проверки нового корпуса и целевых запросов проходят; семь подробных страниц индексируются, дни/месяцы/списки исключены.
- При первой проверке обнаружены и исправлены потеря пометки отмены в snippets и потеря общих запросов: отмена помещена в H1, поисковый заголовок содержит раздел. Тогда запрос `календарь мероприятий` поддерживала индексируемая нижняя ссылка возврата. Этот вариант заменён поисковой metadata после удаления нижней навигации; актуальные результаты — в последнем разделе поискового отчёта.
- Пять падений — только прежние rank failures `еда` 3 > 2, `детская площадка` 4 > 3, `экскаватор` 2 > 1 из #747. Пороги не ослаблены, новых исключений нет. Полные сравнения и логи — в [search-verification.md](search-verification.md), финальный раздел.

### Историческая проверка HTTP и деплоя

- На nginx 1.30.4 с реальной финальной сборкой проверены **18 HTML-маршрутов**: семь мероприятий, четыре дня, три месяца, три списка и корень. Прямой и согласованный Markdown совпадают; подтверждены MIME, `Vary: Accept`, `Link` и canonical. Локальный транспорт без production TLS/Brotli, прикладные правила взяты из production-конфига.
- JSON проверен через `EventsPublicPayloadSchema` по HTTP; отдельные детали и новости имеют один согласованный Event ID. Навигационные страницы не размножают полные Event. В sitemap **15 canonical URL** раздела без списков и aliases.
- Все 12 ICS доставляются с правильными MIME/attachment, прежние UID совпали; оба экспорта отменённой встречи содержат `CANCELLED`. Пройден путь `/llms.txt` → Markdown календаря → день 19 сентября → оба подробных квиза, источники и ICS.
- **Отменено последующим решением:** прежняя сборка генерировала `apps/www/dist/events-redirects.conf`, временная alias-fixture проверяла 301 на canonical с сохранением query. Генератор и механизм доставки удалены; этот результат не относится к текущему объёму PR.
- **Отменено последующим решением:** прежний вариант скрипта установки включал redirects и дополнительную обработку установки/отката. Для него прошли девять изолированных сценариев и `sh -n`. Helper возвращён к реализации до этой доработки; эти результаты не подтверждают наличие усиления деплоя в текущем PR.
- Доказательства: `/var/folders/sb/82f4cg9n6z182c5q0k6dn3hr0000gn/T/opencode/events-final-delivery/` — `evidence.json`, `acceptance.test.ts`, `rollback-evidence.json`, `check-rollback.py`. Сервер 18439 остановлен.

### Браузер и ручная приёмка

- На готовой сборке просмотрены реальные screenshots при **1440, 390 и 320 px**: шествие, два квиза, краткий день, отменённая встреча, месячный список и исходная новость. Справа карточка места 304×258 px, на телефоне — после заголовка и перед описанием. Переходы, клавиатура, фокус, отмена и отсутствие горизонтального переполнения подтверждены.
- Отдельная изолированная fixture покрыла неизвестное место, название без координат, координаты без названия и длинное название. Проверены читаемость и действия при недоступности iframe; Chromium может показывать значок сломанного документа в декоративной подложке, но текст и ссылка остаются доступны. Нарушений contrast audit нет; один overlay-узел новости дополнительно проверен визуально.
- Материалы: `/var/folders/sb/82f4cg9n6z182c5q0k6dn3hr0000gn/T/opencode/events-ui-1855/` — `report.md`, `march-1440.png`, `march-390.png`, `march-320.png`, `day-focus-320.png`, `quiz-1440.png`, `quiz-320.png`, `cancelled-390.png`, `news-1440.png`, `news-compact-320.png`, `fixture-final-1440.png`, `fixture-final-320.png`. Координирующий агент отдельно просмотрел desktop/mobile мероприятия и новость. Все браузерные сессии и серверы 4873/4874 закрыты.
- Остаётся человеческое визуальное ревью и merge PR #748. Проверки выполнялись в Chromium с размерами телефона, не на физическом iPhone. Production-приёмка до закрытия задачи не была условием этого change.

## История первоначальной проверки

Проверка 16 сентября 2026 года. Реализация покрывает **14 требований и 39 сценариев** [spec](specs/events-calendar/spec.md); открытых дефектов в согласованном объёме по итогам correctness- и ponytail-review не осталось. Владелец принял исключение для прежних поисковых rank failures с отдельным исправлением в #747. В [tasks.md](tasks.md) завершены 25/25 задач; реализация опубликована в [PR #748](https://github.com/silentroach/kpshelkovo/pull/748).

## Разрешение и состояние работы

- [Разрешение реализации в issue #224](https://github.com/silentroach/kpshelkovo/issues/224#issuecomment-5694513573).
- [Решение владельца об исключении, sync/archive и публикации](https://github.com/silentroach/kpshelkovo/issues/224#issuecomment-5695610385); follow-up — [#747](https://github.com/silentroach/kpshelkovo/issues/747).
- Реализация опубликована в ветке `feat/224-events-calendar`, созданной из `plan/224-events-calendar`: [коммит 51ad077b](https://github.com/silentroach/kpshelkovo/commit/51ad077b), [PR #748](https://github.com/silentroach/kpshelkovo/pull/748). PR включает оба плановых коммита и реализацию; ссылки в issue #224 ведут на архив и основной spec.
- Этот отчёт сводит независимую проверку исходников и `dist/www` с подтверждёнными результатами основного, интеграционного, браузерного и редакторского агентов. Согласованные требования и технические решения не менялись; после переноса исправлены только относительные ссылки design на документацию.
- Создан [основной spec](../../../specs/events-calendar/spec.md); на момент первоначального архивирования его Purpose и все 14 требований / 39 сценариев совпадали с delta. Change был перенесён штатной CLI-командой в `openspec/changes/archive/2026-09-16-add-events-calendar/`, включая `.openspec.yaml`. При том переносе открытыми оставались операция 8.1 и следующий за ней PR 8.2; после проверки архива была закрыта 8.1. После возобновления работы delta описывает новый объём, повторная синхронизация выполнена после реализации: теперь основная спецификация содержит 15 требований и 51 сценарий.

## Команды и принятое исключение

- Основной агент: финальный `pnpm typecheck` **прошёл в 12:37 после всех UI-правок**.
- `pnpm test`: **1384 теста прошли** — 1290 в приложении и 94 в shared-пакетах.
- `pnpm build`: **успешно**, 337 страниц; Pagefind содержит 224 документа.
- Финальные `test:search-quality:update` и обычный `test:search-quality`: **13 passed / 5 failed**, без расхождений snapshots. Новые проверки событий проходят; новых регрессий контрольных запросов нет. Baseline, логи команд, URL, порядок, snippets и highlights — в [поисковом отчёте](search-verification.md).
- Пять провалов вызваны тремя прежними rank limits: `еда` — rank 3 > 2; `детская площадка` — rank 4 > 3; `экскаватор` — rank 2 > 1. Assertions не ослаблены. Задача 5.4 выполнена как интеграция и сравнительная проверка поиска; **7.1 принята с явным исключением владельца**. Общий gate остаётся красным, его исправление вынесено в [#747](https://github.com/silentroach/kpshelkovo/issues/747); это не блокирует разрешённое завершение текущего change и не означает успешного прохождения search-quality.
- До архивирования scoped OpenSpec validation и `pnpm openspec:validate` прошли; выполнено ревью всего актуального diff, включая staged, unstaged и новые файлы. После sync/archive основной spec прошёл строгую валидацию. Промежуточная archived-проверка отклоняла единственную открытую задачу публикации PR; после его создания задача закрыта. Точное совпадение основного spec с delta, сохранность `.openspec.yaml` и относительные ссылки архива проверены отдельно.

## Покрытие и закрытые замечания

Финальный `pnpm openspec:validate` после создания PR и закрытия 8.2 прошёл: **5 основных specs и 4 архива**, без ошибок.

- Данные, неизвестные значения, периоды, переходы месяца/года, ночные окончания, пустой набор и московские границы проверены предметными тестами и ревью. Общая проекция используется всеми календарными видами; циклов loaders нет. Упоминания и единственный основной backlink многодневной записи проверены отдельно.
- Мигрированы пять записей, добавлены два будущих киноквиза. Источники, постеры, адресные уточнения и разовые редакторские сценарии — в [редакторском отчёте](editorial-verification.md).
- Закрыты замечания к публичному `site-sections`, переходу к текущему месяцу с сохранением вида, ссылке месячного Markdown на первый видимый день, склейкам текста в snippets и потере отмены в поисковом заголовке. Неиспользуемый barrel удалён. Финальное ревью не выявило новых correctness- или complexity-находок.
- Не представленные реальными записями conditional/date-only/multi-day сценарии проверены тестами и отдельной production-like fixture; синтетические записи в публичный каталог не добавлялись.

## Отрендеренные SEO и публичные данные

- Непосредственно проверены все **11 HTML-страниц** в `dist/www/events`: корень, май/июнь/сентябрь с календарём и списком, дни 9 и 16 мая, 13 июня и 19 сентября. На каждой один H1, корректные период и год, согласованные title/description/Open Graph. Title — 24–43 символа, description — 114–120; количества совпадают с карточками.
- Корень, месяцы и дни имеют self-canonical, списки ссылаются canonical на свой месяц. Внешнего `noindex` нет. Sitemap содержит ровно **8 канонических URL**, без списков и без вымышленных `lastmod`. HTML обнаруживает соответствующий Markdown и корневой llms.
- Непосредственно распакованы Pagefind fragments: из 224 документов ровно **4 страницы дней событий**, без корня, месяцев и списков. Отмена и исправленные пробелы присутствуют в индексируемом тексте.
- JSON проверен через Zod по опубликованной JSON Schema: **7 записей**. Их даты, состояния, источники и известные поля согласованы с видимыми карточками и семью Event JSON-LD на страницах дней. Нет вымышленных цен и окончаний; отменённая встреча имеет `EventCancelled`. Условные два часа остаются только в ICS.
- Проверены **7 общих и 5 прежних ICS**: исторические URL/UID сохранены, интервалы и отмена совпадают. Разовый путь из llms к Markdown 19 сентября, JSON, схеме и каталогу приводит к обеим карточкам с источником, ценами, аудиторией и записью.

## HTTP и браузер

- Интеграционный агент подтвердил успешный `nginx 1.30.4 -t` и реальные HTTP-проверки production-like сборки. Для четырёх семейств HTML-маршрутов получены 200; прямой Markdown и `Accept: text/markdown` дают одинаковое содержимое с правильными MIME, `Vary`, `Link`, cache и CSP. `q=0` оставляет HTML, отсутствующие маршруты дают 404. Проверены JSON/schema/catalog, 7 общих и 5 прежних ICS с UID и отменой, а также полный агентный путь. Это закрывает отмеченный независимым ревью пробел доказательств доставки. Сервер на `14339` штатно остановлен.
- Локальные воспроизводимые материалы HTTP-проверки: `/var/folders/sb/82f4cg9n6z182c5q0k6dn3hr0000gn/T/opencode/events-delivery-check.mjs` и `events-nginx-prepare.mjs` в том же каталоге. Это разовая проверка, не постоянный smoke-тест.
- Браузерный субагент проверил реальные страницы и отдельную production-like fixture при ширинах **1440, 390 и 320 px**: календарь/список/день, пустые даты, пропуски месяцев, отмену, условный набор, неизвестные часы, многодневность, внешнюю карту, источник, запись и ICS. Подтверждены клавиатурная навигация, видимый фокус, текстовые состояния и отсутствие переполнения; браузерные сессии закрыты.
- Скриншоты в том же временном каталоге: `events-desktop-calendar.png`, `events-phone-list.png`, `events-narrow-calendar.png`, `events-phone-keyboard.png`, `events-desktop-map-loaded.png`, `events-fixture-phone-conditional-focus.png`, `events-fixture-phone-date-only.png`, `events-fixture-desktop-external-map.png`, `events-fixture-320px.png`. Независимое ревью дополнительно просмотрело снимки фокуса реального календаря и условной карточки.

## Передача владельцу

Результаты typecheck, тестов, сборки, HTTP, браузера, SEO и предметного ревью подтверждены. Владелец явно разрешил завершение с сохранением красного результата общего search-quality и отдельной задачей #747. Источники повторно проверены **16 сентября в 12:57 МСК**: #290 остаётся последним постом, новых уточнений нет, обе записи киноквизов не изменились. PR #748 опубликован после sync/archive; человеческое ревью и merge остаются за владельцем.

## Визуальная перепроверка после замечаний владельца

### Заполнение сетки соседними датами

- Проверена исходная реализация статуса: `buildMonthGrid` уже строит шесть недель и 42 ячейки. Сетка событий теперь следует тому же правилу, а соседние даты остаются приглушёнными, `aria-hidden`, без ссылок и маркеров. Общая проекция записей и маршрутов не менялась. Правила добавлены в раздел «Компактный календарь месяца» дизайн-гайда и одинаковым текстом в существующее требование доступности основного и архивного specs.
- После правки прошли **42 focused event-view/foundation теста**, `pnpm typecheck`, `pnpm build` (**337 страниц, 224 документа Pagefind**) и `pnpm openspec:validate` (**5 specs, 4 архива**). Тесты покрывают заполнение с переходами декабря/января, февраль из четырёх полных недель и отсутствие активной соседней даты, даже когда у неё есть запись в общей проекции.
- Готовая сборка просмотрена в браузере при 1440, 390 и 320 px: в сентябре ровно 42 ячейки, серое 31 августа и 1–11 октября, шесть строк, без переполнения. Tab пропускает декоративные ячейки и переводит фокус на 19 сентября; полная дата и количество записей доступны скринридеру. Адреса заполнителей 31 августа и 1 октября отвечают 404.
- Просмотренные скриншоты: `events-filler-desktop.png`, `events-filler-390.png`, `events-filler-320.png` в `/var/folders/sb/82f4cg9n6z182c5q0k6dn3hr0000gn/T/opencode/`. Сессия и сервер закрыты; синтетические записи не публиковались.

### Предыдущие визуальные проверки

- Последующее уточнение владельца: окончательное написание месяца — **«Сентябрь 2026 года»**. `formatEventMonth` использует существующий `formatMonth` с `includeYear: false` и добавляет полный год локально для событий. После правки прошли все **13 event-view тестов** и `pnpm build` (**337 страниц, 224 документа Pagefind**). В готовых HTML корня, сентябрьского календаря, списка и breadcrumbs дня проверено полное написание, включая title и доступное имя календаря; прежнее `Сентябрь 2026 г.` отсутствует. Скриншоты ниже сделаны до этого уточнения написания.

- 16 сентября повторно открыт готовый `dist/www` через отдельный статический сервер: `/events/`, июньский календарь и список, день 19 сентября. Снимки просмотрены при **1440×1000, 390×844 и 320×740**; для сравнения открыты `/status/calendar/2026/` и `/news/2026/05/ok-meeting-june/`. Это Chromium с мобильными размерами viewport, не физический iPhone.
- Размеры и ритм календаря соответствуют месяцу в статусном календаре: ширина 352/318/248 px, строки 44 px, маркер события 4 px. Маркер не выглядит выбранным днём; отменённый день зачёркнут и имеет текст отмены в доступном имени и tooltip. Поясняющих сносок и счётчика под заголовком нет.
- Проверены вычисленные tokens: `--color-surface-muted` = `oklch(96.8% .008 135)`, `--color-surface-raised` = `oklch(100% 0 0)`, `--radius-full` = `999px`, `--radius-sm` = `.25rem`. Сегментированный переключатель действительно имеет круглые края и светлую активную поверхность.
- В июне предыдущая стрелка слева, ссылка «Текущий месяц» по центру, следующая стрелка справа при всех трёх ширинах. В сентябре лишней ссылки нет. Переход из июньского списка к текущему месяцу сохраняет `/list/`. Tab/Enter работают, outline переключателя видим и равен 2 px.
- На странице дня источник `GREEN.EVENTS` с Telegram-иконкой стоит рядом с датой, как метаданные новости; на 320 px переносится отдельной строкой. Календарное действие и карта оформлены текстовыми ссылками с иконками, на телефоне аккуратно переносятся. Фокус действия виден. Горизонтального переполнения на проверенных страницах событий нет; новых дефектов, требующих изменения исходников, не найдено.
- Доказательства лежат в `/var/folders/sb/82f4cg9n6z182c5q0k6dn3hr0000gn/T/opencode/`: серии `events-final-desktop-`, `events-final-390-`, `events-final-320-` с окончаниями `root.png`, `june.png`, `day.png`, `status-reference.png`, `news-reference.png`; дополнительно `events-final-390-list.png`, `events-final-390-tabs-focus.png`, `events-final-390-actions-focus.png`. Браузерная сессия и сервер закрыты. Новая сборка для этой перепроверки не запускалась; окончательное визуальное решение остаётся за владельцем.

## Rebase на master от 18 сентября 2026

- База: `f498f3aa`; исходная вершина PR: `3bea76b0`. Сохранены все 11 коммитов ветки.
- Совмещены отдельная коллекция событий из #748 и ссылки на справочник мест из #750. Перенесены `place` и `location_details`, публичные поля места, ссылки карточек и обратные связи. Координаты яблоневого сада берутся из актуальной карточки, а не из устаревшей копии события. Внешние площадки без карточки по-прежнему поддерживаются.
- Генерация ICS использует `@shelkovo/ical` из #758 с исправлениями Apple Calendar и RFC 6868. Поддержаны необходимые календарю интервалы DATE, состояние и текстовое место без координат. Пять опубликованных новостных адресов, UID и время публикации проверены интеграционными тестами; отмена и условные два часа сохранены.
- Прошли `pnpm typecheck`, `pnpm test` (1408 тестов приложения и 117 пакетных), `pnpm openspec:validate` (13 specs и 9 архивов). Сборка содержит 352 страницы и 234 документа Pagefind.
- После пересборки индекса сверены и обновлены поисковые snapshots. Обычный повторный запуск `pnpm exec vitest run -c vitest.search-quality.config.ts` из `apps/www` дал **13 passed / 5 failed**, без несовпадений snapshots. Остались ровно прежние ранговые ошибки #747: `еда` — 3 вместо ≤2, `детская площадка` — 4 вместо ≤3, `экскаватор` — 2 вместо ≤1. Пороги и проверки из master сохранены, включая поиск отключений воды.
- Ручная визуальная проверка и импорт в физическое приложение календаря при rebase заново не проводились; предыдущие результаты выше относятся к указанным там версиям.
