# Tasks

Разрешение на реализацию уточнения получить у владельца после показа артефактов. Работа продолжит ветку `feat/parcel-map-search` и PR #810; перед реализацией сверить фактическое состояние ветки и решение владельца.

## 1. Картографический слой и данные

- [x] 1.1 Убрать зависимость обычного штриха от статуса в `parcel-layer.ts` и производный `muted` из геометрического адаптера и Zod-схемы; обновить ADR-041. Проверить тестами одинаковый штрих участков с разными/неизвестным статусами до выбора и после таймера, отсутствие `status`/`muted` в публичном payload и прежнее выделение выбранного контура.
- [x] 1.2 Сделать у подписи компактную полупрозрачную капсулу под текстом при сохранении области нажатия и доступного имени; проверить DOM-тестом основной номер, активацию кнопки и сохранение одного номера у объединённого участка. Визуальный результат проверить на карте в задаче 2.3, не закреплять оформление snapshot-тестом CSS.
- [x] 1.3 Исправить зависимость разрешения прямого/подтверждённого конфликта от порядка входа. Проверить оба порядка на fixtures: подтверждённый участок остаётся кандидатом, конфликтная запись отражена в отчёте, повторный запуск не удаляет ранее сохранённый участок.
- [x] 1.4 Обрезать внешние пробелы у поддержанного статуса в обеих схемах генплана. Проверить страницы и сохранённые снимки со статусом с пробелами, сохранение отказа на неизвестной формулировке и работу mapper-а.
- [x] 1.5 При непроверенном кадастровом снимке разрешать повтор только при побайтовом совпадении обоих файлов. Проверить через CLI попытку подмены даты/области при прежнем NDJSON и отсутствие записей при отказе; для неизменного повтора сохранить успешный результат.

## 2. Управление и визуальная приёмка

- [x] 2.1 Перенести SVG стопки на иконную кнопку «Слои», добавить пункту «Участки» иконку контура и сохранить поведение списка. Проверить DOM-тестами доступное имя кнопки, `aria-expanded`, `aria-pressed`, работу клавиатуры/Escape, видимый текст пункта и отсутствие запроса при одном открытии; официальный Svelte autofixer вызвать и повторить после исправлений.
- [x] 2.2 Переход по `?p` центрирует внутреннюю точку подписи с тем же zoom, с которого подписи появляются, без подгонки к границам участка. Проверить в тестах основной код, алиас, узкий/раздельный участок, сохранение выделения, очистку URL и reduced motion.
- [x] 2.3 Выполнить `pnpm typecheck`, затронутые тесты и `pnpm build`. На локальной production-сборке с реальным Яндекс SDK проверить desktop/mobile: единые контуры, номера рядом со строениями на светлых и насыщенных фрагментах при zoom 17/18, фокус/клик подписей, переход `?p` к небольшому участку, выбор иконной кнопки и пункта с клавиатуры, фокус после Escape, работу мест и штатных действий; зафиксировать результат в PR.

## 3. Завершение

- [x] 3.1 Проверить все требования и сценарии delta, сделать ревью полного diff, включая новые и незакоммиченные файлы, упростить по `ponytail-review` и выполнить `pnpm openspec:validate`; результат зафиксировать в PR.
- [x] 3.2 После приёмки штатно синхронизировать `parcel-map` и архивировать change, уточнив Purpose основной spec и отобрав долгоживущие требования. Обновить PR #810 и краткую отметку в issue #804 со ссылками на согласование/коммит; проверить, что change и реализация входят в PR, а структурная валидация проходит.
