## 1. Отображение расстояния

- [x] 1.1 Вычислить расстояние от МКАД через существующий `getRing()` в frontmatter страницы поселка и добавить его с общим форматированием и пояснением в настольную карточку и мобильное расположение; проверить `pnpm typecheck` и убедиться по diff, что значение не зависит от признака базового поселка или проверки на положительное расстояние.
- [x] 1.2 По последующему уточнению владельца добавить ровно одну тильду перед расстоянием от Шелково в HTML и Markdown страницы; проверить `pnpm typecheck`, существующие тесты Markdown, `pnpm build` и значение `~2 км` в собранной странице Мелихово Парка.

## 2. Проверки результата

- [x] 2.1 Запустить существующие тесты рейтинга, полного JSON и Markdown в `apps/www/src/compare/lib`, а также форматирования в `packages/format`, затем `pnpm build`; зафиксировать успешные результаты и сверить метрику нового вывода с Markdown и `distance.mkad_km`.
- [x] 2.2 Проверить собранные страницы обычного поселка с длинным адресом и Шелково в браузере при ширине 1280 и 390 CSS-пикселей: расстояние от МКАД видно, на компьютере оно ниже расстояния от Шелково, на телефоне находится в мобильном расположении, переполнений нет, пояснение присутствует. Разовой проверкой рендера с нулевым значением подтвердить вывод `0 км от МКАД`; закрыть браузерную сессию после проверки.
- [x] 2.3 Выполнить `pnpm openspec:validate` и ревью всего актуального diff, включая новые файлы; проверить соответствие сценариям spec и исправить найденные проблемы в пределах change.

## 3. Завершение

- [x] 3.1 После реализации и приёмки синхронизировать основной spec и архивировать change штатным workflow; проверить наличие `openspec/specs/compare-settlement-location/spec.md`, архива и успешный повторный `pnpm openspec:validate`.
- [x] 3.2 При наличии разрешения на commit, push и PR опубликовать результат со ссылкой на issue #744, согласование и проверки; проверить ссылки и `Closes #744` в PR. Если публикация не разрешена, передать локальный результат с явным указанием отсутствующих опубликованных ссылок.

## Результаты проверок

- `pnpm typecheck` и `pnpm build` прошли.
- `pnpm openspec:validate`, точечные проверки Prettier и oxfmt, `git diff --check` прошли. Ревью кода и всех новых артефактов завершено без замечаний; реализация затрагивает Astro-страницу и её Markdown-генератор.
- `pnpm --filter @shelkovo/www test src/compare/lib/rating.test.ts src/compare/lib/full.test.ts src/compare/lib/markdown.test.ts`: 14 тестов прошли.
- `pnpm --filter @shelkovo/format test`: 24 теста прошли.
- Временная проверка через Astro Container отрендерила настоящую страницу с координатами центра Москвы: обе строки содержат `0 км от МКАД`. Проверка прошла, временный тест удалён.
- В браузере проверены `melikhovo-park` и `shelkovo` при ширине 1280 и 390 CSS-пикселей. Порядок, оформление, пояснение и отсутствие переполнений подтверждены; у Шелково отсутствует расстояние до самого себя.
- JSON содержит 58,4 км для Мелихово Парка и 59,3 км для Шелково; HTML и Markdown обоих поселков показывают `~60 км`. Браузерная сессия и preview-сервер закрыты.
- Фоновая Яндекс Карта в локальном preview не загрузилась; блоки расположения доступны и проверены.
- После уточнения о тильде повторно прошли `pnpm typecheck`, четыре теста Markdown, `pnpm build` и валидация OpenSpec. В собранном Markdown Мелихово Парка подтверждена строка `Расстояние от Шелково: ~2 км`.
- Разрешение реализации: [отметка в issue](https://github.com/silentroach/kpshelkovo/issues/744#issuecomment-5684705135). Артефакты и уточнение о тильде согласованы в этой сессии.
- Spec синхронизирован, change архивирован. [Первоначальная локальная передача](https://github.com/silentroach/kpshelkovo/issues/744#issuecomment-5684853691) состоялась до разрешения публикации.
- Перед публикацией по просьбе владельца повторно выполнен `ponytail-review` всего diff: лишней сложности не найдено. Владелец разрешил commit и push; опубликованные ссылки фиксируются в issue #744.
