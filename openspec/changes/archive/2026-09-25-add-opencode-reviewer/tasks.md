# Tasks

## 1. Профиль и протокол reviewer

- [x] 1.1 Добавить `.opencode/agents/reviewer.md` с native V2 frontmatter, `mode: subagent`, `model: openai/gpt-6-sol` и `deny` для действия `edit`; проверить обнаружение профиля в OpenCode после обновления контекста среды.
- [x] 1.2 Описать в prompt границы роли, передачу задачи/change/target/base SHA/результатов проверок и сбор committed, staged, unstaged и untracked; сверить инструкцию со сценариями полного охвата и недоступной базы в delta spec.
- [x] 1.3 Описать основной проход, последующий вызов `ponytail-review` и отчёт с подтверждёнными находками, вопросами и непроверенным; проверить, что порядок и форматы сохраняют границы skill, согласованный объём и запрет одобрять PR.

## 2. Включение в OpenSpec-приёмку

- [x] 2.1 Обновить `rules.tasks` и `operations.apply.guidance` в `openspec/config.yaml`: вызов reviewer, разбор замечаний, повтор после изменения diff и явный fallback. Проверить доставку через `pnpm exec openspec instructions tasks --change add-opencode-reviewer --json` и `pnpm exec openspec instructions apply --change add-opencode-reviewer --json`; убедиться, что обязательность ограничена OpenSpec-путём.
- [x] 2.2 Дополнить `docs/development.md` примером обычного вызова, контекстом, границей `deny edit` и широких инструментов, ролью автора и fallback. Пройти пример из основной сессии и получить отчёт reviewer без правок проекта.

## 3. Проверка поведения на небольших примерах

- [x] 3.1 Подготовить одноразовую Git-fixture по design, проверить эффективные permissions и фактическую блокировку попытки edit/write/patch runtime. Зафиксировать результат блокировки; текстовый отказ модели не считать достаточной проверкой.
- [x] 3.2 Провести reviewer по примеру с несколькими коммитами, staged, unstaged и новым файлом, известными дефектами и обоснованным упрощением; сопоставить отчёт с ожидаемыми местами и последствиями, подтвердить отдельный запуск `ponytail-review`. Проверить также учёт тестов, документации, конфигурации и переданных требований.
- [x] 3.3 Проверить чистый пример и дефект, исправленный последующим слоем diff: в отчёте нет придуманных подтверждённых ошибок, промежуточный дефект не выдаётся за текущий, отсутствие упрощений названо явно. Проверить ответ при отсутствующей или недоступной базе: полный охват не заявлен.
- [x] 3.4 Исправить найденную проблему силами автора и повторно вызвать reviewer; проверить оценку исправления и связанных сценариев. В отдельном пробном вызове основной сессии с недоступным reviewer подтвердить самопроверку обоими проходами и явное указание причины fallback.
- [x] 3.5 Для пробных ревью сопоставить содержимое отслеживаемых и новых файлов, индекс, HEAD и refs до/после, а также журнал действий: reviewer не изменял проект, не коммитил и не публиковал. Записать версию OpenCode и результаты прогонов, затем удалить временную fixture.

## 4. Итоговая приёмка изменения

- [x] 4.1 Выполнить `pnpm openspec:validate`, `pnpm skills:check`, `pnpm test:agent-tooling` и проверку форматирования затронутых файлов; подтвердить успешное завершение и отсутствие непредусмотренных изменений после проверок.
- [x] 4.2 Проверить весь актуальный diff реализации относительно явной базы, включая staged, unstaged и новые файлы: основной проход и `ponytail-review`, по возможности через новый reviewer. Разобрать замечания и повторно проверить сделанные исправления; при недоступности reviewer явно отметить fallback, не подменяя им runtime-приёмку из раздела 3.
- [x] 4.3 Сверить результат с четырьмя требованиями delta spec и всеми согласованными решениями; подготовить сводку проверок для PR, включая фактический охват, причины возможного fallback и оставшуюся ручную приёмку. Повторить отбор долгоживущих требований перед последующими sync/archive по проектному workflow.
