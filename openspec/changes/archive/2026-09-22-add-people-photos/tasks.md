# Tasks

## 1. Разрешение и правила публикации

- [x] 1.1 Получить явное разрешение владельца на реализацию просмотренного change и оставить в issue #800 отметку со ссылкой на коммит согласованных артефактов. Проверка: разрешение относится к актуальному плану и подтверждено сообщением владельца; публикация draft PR сама по себе разрешением не считается.
- [x] 1.2 Добавить ADR о публичных портретах и синхронизировать ADR-021, индекс ADR, `ops/storage/public-media.md` и проектный `public-media-publisher`. Проверка: документы согласованно разрешают портреты, описывают `people/<slug>.jpeg`, замену текущего файла с коротким кешем, подготовку JPEG и проверку публикации; процесс использует существующие инструменты.

## 2. Данные и HTML-карточка

- [x] 2.1 Добавить необязательный `photo` в raw-схему, явные доменные типы и mapper `apps/www/src/lib/people/`. Проверка: короткие тесты покрывают фото с источником, без источника, отсутствие фото и отклонение некорректного URL, размеров или неполного источника; mapper сохраняет валидные данные.
- [x] 2.2 Добавить портрет и источник в `apps/www/src/pages/people/[slug]/index.astro` с локальной адаптивной вёрсткой. Проверка: изображение имеет фактические `width`/`height` и альтернативу с именем, ссылка использует указанный источник, при отсутствии фото или источника соответствующий блок отсутствует; точные CSS-классы и тексты заголовков не фиксируются тестами.
- [x] 2.3 Описать frontmatter и атрибуцию в `apps/www/src/data/people/AGENTS.md`, расположение портрета — в дизайн-гайде. Проверка: пример использует `people/<slug>.jpeg` и соответствует raw-схеме, источник необязателен и не подставляется из организации, размеры отображения не выдаются за размеры файла.

## 3. Публичные представления

- [x] 3.1 Добавить фотографию и ссылку источника в Markdown-карточку через AST API в `view.ts`. Проверка: короткие тесты проверяют URL изображения и источника, порядок блока и отсутствие выдуманной атрибуции или заглушки; полный документ в snapshot не фиксируется.
- [x] 3.2 Добавить явный `photo` в Public DTO, JSON Schema и OpenAPI; актуализировать описание поля в существующих people discovery/agent-документах. Проверка: профиль с фото и без него проходит публичную Zod-валидацию, Schema/OpenAPI описывают необязательные `photo` и `source`, существующие поля и URL сохраняются.
- [x] 3.3 Передать URL фотографии в `Person.image` через `seo.ts`. Проверка: JSON-LD содержит этот URL только у профиля с фото; портрет не подставляется в социальные metadata страницы.

## 4. Портреты и наполнение

- [x] 4.1 Повторно сверить шесть пар «профиль — JPEG» из `design.md`, скачать и подготовить фотографии по согласованному процессу. Проверка: полный кадр визуально сохранён, MIME — `image/jpeg`, размеры не увеличены, встроенные метаданные удалены, для публикуемых файлов известны размер и SHA-256.
- [x] 4.2 Проверить точные S3-ключи `people/<slug>.jpeg`, опубликовать отсутствующие объекты с `Cache-Control: public, max-age=300` и проверить каждый через media-origin; проверить разрешение изображений с этого origin в CSP сайта. Проверка: у каждого URL ответ 200, ожидаемые MIME, размер, SHA-256 и размеры изображения без метаданных, короткий кеш без `immutable`; неожиданные конфликты не перезаписаны, браузерная доставка разрешена CSP.
- [x] 4.3 Добавить проверенные media URL, размеры и источник в `kschemelinin.md`, `gmozgov.md`, `akornyakov.md`, `nmakhrova.md`, `ekurenkova.md`, `ebakharueva.md`. Проверка: все шесть профилей проходят загрузку коллекции и показывают свой портрет; остальные профили остаются валидными без `photo`.

## 5. Приёмка и завершение

- [x] 5.1 Выполнить `pnpm typecheck`, `pnpm test`, `pnpm build` и `pnpm openspec:validate`. Проверка: команды завершаются успешно; разово сверены собранные HTML, Markdown, JSON и JSON-LD затронутых профилей с требованиями, без добавления постоянного обхода сайта в CI.
- [x] 5.2 Проверить локальную сборку в браузере на широком и узком экране: профиль с фотографией/источником, вариант без источника, профиль без фото и недоступный JPEG. Проверка: верное расположение и полный кадр, отсутствие горизонтального переполнения и сдвига при загрузке изображения, рабочие контакты, доступная с клавиатуры ссылка источника, читаемый профиль без JavaScript; приложены снимки широкого и узкого вида, браузерная сессия закрыта.
- [x] 5.3 Провести ревью всего актуального diff и исправить замечания. Проверка: учтены staged, unstaged и новые файлы, каждому требованию и сценарию есть результат проверки, отсутствуют лишние абстракции, бинарные исходники и изменения вне согласованного объёма.
- [x] 5.4 После успешной приёмки синхронизировать `people-photos` с основными specs и архивировать change штатными workflows, затем обновить текущий PR и ссылку в issue. Проверка: повторный `pnpm openspec:validate` успешен, PR содержит реализацию, основные specs, архив, результаты проверок и `Closes #800`; ревью и merge оставлены владельцу.
