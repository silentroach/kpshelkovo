## 1. Модель мест и событий

- [x] 1.1 Добавить optional `show_on_map` и нормализованный `showOnMap`, убрать географический refine посёлка. Проверить schema/mapper тестами значения `true`, `false`, отсутствие поля, мировые диапазоны, место за прежними границами и разные slug с одинаковыми координатами.
- [x] 1.2 Заменить редакционные поля события на `place` и `location_details`, разрешать место через полный базовый dataset без backlinks. Проверить загрузку видимого и скрытого места, отсутствие места, ошибки неизвестного slug, старых inline-полей и уточнения без места; ошибка должна содержать контекст статьи и события.

## 2. Миграция редакционных данных

- [x] 2.1 Создать четыре места по списку из design, сохранив исходные координаты и раздельность ресторана и КПП; всем 13 прежним местам поставить `show_on_map: true`. Проверить Zod-валидацию данных, отсутствие коллизий slug, соответствие фактов источникам и исходный набор видимых slug разовой сверкой.
- [x] 2.2 Перевести пять событий в трёх статьях на ссылки и уточнения из design; посадку связать с `apple-garden`. Проверить отсутствие inline-мест, сохранность времён, slug и URL событий, а также замену координат посадки именно на точку сада.

## 3. Общая карта и карточки мест

- [x] 3.1 Применить единую видимую выборку к JSON карты, ItemList, количеству, fallback-ссылке и Markdown-списку, сохранив полный набор карточок и сущностей. Проверить focused-тестами фильтрацию смешанной и пустой выборок; разово проверить сборочные HTML/Markdown карточки, sitemap и поисковую доступность скрытого места.
- [x] 3.2 Убрать у скрытых мест ссылку общей карты и использовать существующую обработку неизвестного `h`. Проверить видимое, скрытое и несуществующее место, отсутствие скрытого маркера, сохранение viewport и остальных частей URL/history state.
- [x] 3.3 Обеспечить начальный viewport по видимой выборке без локального географического запрета. Проверить пустую выборку, единственную удалённую точку и несколько точек, а также отсутствие влияния скрытых мест на bounds и кластеры.

## 4. Представления событий и связи

- [x] 4.1 Обновить event view и `NewsEventCard.astro`: внутренняя ссылка на место, отдельное уточнение, внешняя карта и существующий iframe по данным сущности. Проверить HTML/поведение для скрытого места и события без места, включая доступность ссылки с клавиатуры.
- [x] 4.2 Обновить публичный DTO и Zod-схему: сохранить прежние типы полей, добавить `place_id`, `place_url`, `location_details`; обновить Event JSON-LD. Проверить контрактные случаи с адресом, без адреса и без места, а также изменение канонических данных при сохранении идентичности события.
- [x] 4.3 Обновить календарь: данные места в `LOCATION`, `GEO` и `X-APPLE-STRUCTURED-LOCATION`, уточнение в `DESCRIPTION`. Проверить сохранение URL/UID, описания и ссылки на статью, корректное escaping и отсутствие географических полей у события без места.
- [x] 4.4 Добавить места событий в source refs новости с дедупликацией по статье и месту. Проверить backlink для ссылки только из события, одну связь при повторениях в событиях и body, а также её вывод существующим Markdown-потребителем.

## 5. Документация и приёмка

- [x] 5.1 Синхронизировать ADR-028 и ADR-012, проектные инструкции событий и описание публичного JSON; проверить связанные discovery-поверхности. Проверить, что примеры соответствуют новому формату и не предлагают inline-места или автоматический показ новых places; сгенерированные upstream-инструкции не редактировать вручную.
- [x] 5.2 Выполнить `pnpm typecheck`, `pnpm test`, `pnpm build` и `pnpm openspec:validate`. Зафиксировать результаты и сопоставить каждый сценарий двух specs с тестом либо разовой проверкой; не превращать разовый обход артефактов в постоянный smoke-набор.
- [x] 5.3 Проверить браузером собранный сайт: прежний набор карты, фокус видимого/скрытого/неизвестного места, карточку скрытого места, ссылку и уточнение события, внешний переход и встроенную карту. Зафиксировать результат; завершить браузерную сессию после проверки.
- [x] 5.4 Провести ревью всего актуального diff, включая новые файлы, staged и unstaged; проверить соответствие согласованному объёму и убрать лишние промежуточные модели или повторные вычисления. Зафиксировать исправления и оставшуюся ручную приёмку.
- [x] 5.5 После реализации и проверок синхронизировать основные specs и архивировать change штатным workflow в той же ветке. Повторно выполнить `pnpm openspec:validate`, обновить ссылку в issue на архив и подготовить PR с результатами проверок; убедиться, что закрытие #749 относится к выполненной реализации, а не к PR планирования.
