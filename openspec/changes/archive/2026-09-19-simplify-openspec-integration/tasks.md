# Обновление и упрощение OpenSpec

## 1. Интеграция

- [x] 1.1 Закрепить `@fission-ai/openspec@1.13.1` в package и lock-файле; проверить версию через `pnpm exec openspec --version` и согласованность установки с frozen lock-файлом.
- [x] 1.2 Сократить генератор до одного штатного `init` с `core`, убрать режим `check` и повторную генерацию в bootstrap; проверить успешную и повторную регенерацию без дополнительных изменений инструкций.
- [x] 1.3 Удалить шаг `openspec:check` из CI и тесты сравнения с эталоном, сохранить короткий тест изоляции профиля; выполнить `pnpm test:agent-tooling` и убедиться, что проверки расхождения рабочей копии не перенесены в другой шаг.
- [x] 1.4 Исправить команды обслуживания и ссылки на перенесённые правила в `AGENTS.md`, `openspec/config.yaml` и `docs/development.md`; проверить отсутствие действующих ссылок на удалённый `openspec/AGENTS.md` и команду `openspec:check` вне истории changes.
- [x] 1.5 По решению владельца установить `minimumReleaseAge: 2880` и проверить `pnpm install --frozen-lockfile` без обхода политики.

## 2. Приёмка

- [x] 2.1 Выполнить `pnpm openspec:validate` и `pnpm skills:check`, проверить форматирование изменённых файлов и совместимость всего generated diff с проектными правилами; исправить найденные проблемы.
- [x] 2.2 Сверить результат со сценариями `agent-tooling`, выполнить ревью всего diff изменения и синхронизировать spec штатным workflow; проверить, что убрано только требование автоматического сравнения инструкций.
- [x] 2.3 Архивировать завершённый change штатным workflow и повторить `pnpm openspec:validate`; убедиться, что итоговая spec и архив отражают выполненные задачи.
