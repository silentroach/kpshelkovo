# Tasks

## 1. Передача согласованного плана и исходные замеры

- [x] 1.1 Перед редактированием кода сверить #845, #841, актуальный change, рабочую ветку и незакоммиченные файлы; найти явное разрешение владельца на реализацию после ревью draft PR и указать ссылку на него в задаче или issue. Без разрешения реализацию не начинать. [Разрешение владельца зафиксировано в #845](https://github.com/silentroach/kpshelkovo/issues/845#issuecomment-5834054631).
- [x] 1.2 Зафиксировать исходные размеры и состав картографических JS из `docs/bundle/` и сетевые запросы представительных страниц; проверить, что сравнение относится к текущей базе, а не только к историческому срезу #845. На HEAD `30f11f4a` `editorial-map.C_r5aN4j.js` — 80 778 B / 24 670 B Brotli (Typograf и YAML по `client.yaml`), `PlaceMap.a79D7T2u.js` — 16 002 B / 5 611 B Brotli. Локальный браузер по текущему `dist/www` запросил `editorial-map` на `/map/`, `/map/burzhuyka/`, `/news/2026/09/rostelecom-survey/` и `/kb/hello/`; обе текстовые страницы также запросили `editorial-map-element`. Данные `docs/bundle/` текущего HEAD совпадают с файлами `dist/www/static/`, не только с историческим срезом issue.

## 2. Подготовленные данные подписей (#845)

- [x] 2.1 Добавить в `@shelkovo/markdown` безопасное форматирование обычного текста по действующим правилам Typograf; проверить тестами кавычки, неразрывные пробелы, буквальные entity, псевдотеги, Markdown и строки без преобразований.
- [x] 2.2 Подготовить подписи точек в независимом display-представлении для HTML Markdown-карт и превью места; проверить сохранность исходной коллекции, скрытых строк, HTML-экранирование, проверку конечного payload до публикации и фактический текст маркера тестами.
- [x] 2.3 Подключить ту же подготовку к `/map/data/places.json` через публичный адаптер без второго поля; проверить тестом, что `properties.iconCaption` подготовлен, исходная запись не изменена, остальные публичные свойства/координаты сохранены, а невалидный конечный payload останавливает сборку.

## 3. Лёгкая клиентская цепочка карты (#845)

- [x] 3.1 Убрать типографику и HTML decoder из общего renderer-а: выводить полученный `iconCaption` только через `textContent`; проверить тестами буквальные символы разметки, отсутствие исполняемых элементов, отсутствие подписи у неподписанной точки и работу всех трёх потребителей.
- [x] 3.2 Проверить фактические остаточные пути Typograf/YAML на `/map/` и остальных страницах; если их удерживает форматирование статического статуса через `places/view`/`status/view`, готовить display-текст при сборке данных карты и убрать тяжёлый клиентский импорт. Подтвердить состав чанков и отсутствие регрессии динамического отображения часов/состояния, включая сопутствующий Luxon-путь #843. Typograf/YAML ушли из карты; Luxon остаётся в ленивом поисковом чанке `lazy`, не в цепочке карт; динамические часы и статусы подтверждены тестами и браузером.

## 4. Код редакционных карт только на материалах с картами (#841)

- [x] 4.1 Условно подключать bundled script карты в шаблонах новостей и KB по уже подготовленному результату Markdown-render-а; проверить на собранных страницах с картой и без неё отсутствие карты/её JS на текстовых страницах и наличие готовых карт на материалах с несколькими вставками.
- [x] 4.2 Проверить тестом и в браузере переход без карты → с картой → без карты → с картой через Astro ClientRouter, работу всех вставок ниже экрана, печать, сообщения об ошибке и освобождение старого экземпляра; исправить способ условного подключения, если Astro всё же включает код карты на страницах без неё. В браузере переход и возврат, второй экземпляр ниже экрана, печать с видимыми лейблами и отказ SDK проверены; уничтожение старых экземпляров и несколько вставок покрывают тесты custom element и Markdown-render-а.

## 5. Документация и общая приёмка

- [x] 5.1 Обновить применимые инструкции по редакционным картам и публичному `iconCaption`, отметить несовместимое изменение поля без изменения исходников/Markdown companion; проверить согласованность с `map-geodata`, `site-maps` и границами #858/#859.
- [x] 5.2 Выполнить `pnpm typecheck`, затронутые тесты и `pnpm build`; затем `pnpm bundle:analyze`, сравнить запросы и суммарный вес JS на `/map/`, месте с геометрией, статье с картой, новости/KB без карты и обычном превью, сохранить обновлённые YAML-отчёты и не считать `rendered` сетевым весом. `pnpm typecheck`, `pnpm test` (www: 1677 тестов), `pnpm build` и `pnpm bundle:analyze` прошли. Суммы JS из файлов, которые запросил браузер (байты / сумма Brotli-артефактов, без внешнего SDK): `/map/` 252 844 / 79 765 → 103 546 / 36 352; превью места с геометрией 119 927 / 38 420 → 41 198 / 14 484; статья с картой 116 567 / 37 107 → 37 789 / 13 129; новость и KB без карты каждая 116 567 / 37 107 → 30 210 / 9 995. Обычное превью без геометрии проверено отдельно в браузере: прежняя загрузка общего map renderer-а ожидаемо остаётся в границах #847, но чанк существенно легче. В `docs/bundle/` сохранены итоговые YAML; `rendered` использовался только для анализа состава, а не как сетевые байты.
- [x] 5.3 Выполнить `pnpm openspec:validate`, сопоставить требования и сценарии delta specs с результатами тестов, браузерной проверки и сборки; не отмечать готовность по одной структурной валидации. Валидация успешна (0 ошибок); исходник, DTO, текст маркера, отсутствие JS на текстовых страницах, навигация, ошибка SDK и печать подтверждены также тестами/браузером.
- [x] 5.4 Перед переводом draft PR в готовый вызвать reviewer для всего актуального diff относительно точного SHA базы, включая staged/unstaged/новые файлы, требования, результаты проверок и ограничения; разобрать и исправить замечания, повторить проверки изменённых мест. При недоступности reviewer провести явную самопроверку регрессий и отдельный ponytail-review с указанием отсутствия независимого ревью. Reviewer проверил полный охват относительно `635b6aecc354f331585507f22ac9205861421e47`, замечаний по регрессиям и лишней сложности нет; независимого запуска тестов и браузера не было.
