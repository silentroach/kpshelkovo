# Tasks

## 1. Передача согласованного плана и исходные замеры

- [x] 1.1 Перед редактированием кода сверить #845, #841, актуальный change, рабочую ветку и незакоммиченные файлы; найти явное разрешение владельца на реализацию после ревью draft PR и указать ссылку на него в задаче или issue. Без разрешения реализацию не начинать. [Разрешение владельца зафиксировано в #845](https://github.com/silentroach/kpshelkovo/issues/845#issuecomment-5834054631).
- [x] 1.2 Зафиксировать исходные размеры и состав картографических JS из `docs/bundle/` и сетевые запросы представительных страниц; проверить, что сравнение относится к текущей базе, а не только к историческому срезу #845. На HEAD `30f11f4a` `editorial-map.C_r5aN4j.js` — 80 778 B / 24 670 B Brotli (Typograf и YAML по `client.yaml`), `PlaceMap.a79D7T2u.js` — 16 002 B / 5 611 B Brotli. Локальный браузер по текущему `dist/www` запросил `editorial-map` на `/map/`, `/map/burzhuyka/`, `/news/2026/09/rostelecom-survey/` и `/kb/hello/`; обе текстовые страницы также запросили `editorial-map-element`. Данные `docs/bundle/` текущего HEAD совпадают с файлами `dist/www/static/`, не только с историческим срезом issue.
- [x] 1.3 Показать владельцу пересмотренные артефакты после отказа от типографики `iconCaption` и получить явное разрешение на изменение уже опубликованной реализации в PR #860; оставить в #845 ссылку на согласованный коммит. Владелец подтвердил пересмотр и реализацию в рабочем диалоге 25 сентября 2026 года; ссылка на коммит будет в #845.

## 2. Дословные данные подписей (#845)

- [x] 2.1 Удалить добавленные для `iconCaption` helper `formatPlainText`, `prepareEditorialGeometry`, их экспорт, документацию и тесты; оставить другие правила и вызовы типографики без изменений.
- [x] 2.2 Передавать авторскую геометрию без текстовой подготовки в HTML Markdown-карт и превью места, сохранив проверку конечного payload. Проверить лейблы с кавычками, HTML-подобным текстом, буквальными entity и Markdown, безопасность вывода через `textContent`, исходник и скрытые поля.
- [x] 2.3 Сохранить исходный `properties.iconCaption` в `/map/data/places.json` без второго поля и без изменения остальных координат и свойств; проверить равенство исходнику и ошибки невалидного конечного payload.

## 3. Лёгкая клиентская цепочка карты (#845)

- [x] 3.1 Убрать типографику и HTML decoder из общего renderer-а: выводить полученный `iconCaption` только через `textContent`; проверить тестами буквальные символы разметки, отсутствие исполняемых элементов, отсутствие подписи у неподписанной точки и работу всех трёх потребителей.
- [x] 3.2 Проверить фактические остаточные пути Typograf/YAML на `/map/` и остальных страницах; если их удерживает форматирование статического статуса через `places/view`/`status/view`, готовить display-текст при сборке данных карты и убрать тяжёлый клиентский импорт. Подтвердить состав чанков и отсутствие регрессии динамического отображения часов/состояния, включая сопутствующий Luxon-путь #843. Typograf/YAML ушли из карты; Luxon остаётся в ленивом поисковом чанке `lazy`, не в цепочке карт; динамические часы и статусы подтверждены тестами и браузером.

## 4. Код редакционных карт только на материалах с картами (#841)

- [x] 4.1 Условно подключать bundled script карты в шаблонах новостей и KB по уже подготовленному результату Markdown-render-а; проверить на собранных страницах с картой и без неё отсутствие карты/её JS на текстовых страницах и наличие готовых карт на материалах с несколькими вставками.
- [x] 4.2 Проверить тестом и в браузере переход без карты → с картой → без карты → с картой через Astro ClientRouter, работу всех вставок ниже экрана, печать, сообщения об ошибке и освобождение старого экземпляра; исправить способ условного подключения, если Astro всё же включает код карты на страницах без неё. В браузере переход и возврат, второй экземпляр ниже экрана, печать с видимыми лейблами и отказ SDK проверены; уничтожение старых экземпляров и несколько вставок покрывают тесты custom element и Markdown-render-а.

## 5. Документация и общая приёмка

- [x] 5.1 Обновить инструкции по редакционным картам и публичному `iconCaption`, снять объявление несовместимости в PR #860 и основных specs, не меняя источники/Markdown companion и границы #858/#859.
- [x] 5.2 Выполнить `pnpm typecheck`, затронутые тесты, `pnpm test` и `pnpm build`; затем `pnpm bundle:analyze`, сравнить состав и суммарную передачу JS на `/map/`, месте с геометрией, статье с картой, новости/KB без карты и обычном превью с исходной базой; сохранить обновлённые YAML-отчёты. Перепроверить браузерные переходы, печать, ошибку SDK, статусы и часы. Не считать `rendered` сетевым весом. Typecheck, полные тесты (www: 1676), сборка через `bundle:analyze`, браузерные сценарии и печать успешны; JS-отчёты `docs/bundle/` по составу и весу не изменились относительно `c7857ac8`, исходные сетевые замеры относительно `30f11f4a` остаются верны.
- [x] 5.3 Выполнить `pnpm openspec:validate` и сопоставить сценарии delta specs с тестами, браузером и сборкой. Основной spec синхронизирован с дословным `iconCaption`, delta и основные specs проходят валидацию; исходник, HTML и JSON проверены тестами, карты и загрузка — браузером.
- [x] 5.4 Вызвать reviewer для всего актуального diff относительно точного SHA базы, включая staged/unstaged/новые файлы; исправить замечания и повторить подходящие проверки. Reviewer проверил `origin/master` от `635b6aecc354f331585507f22ac9205861421e47` до текущего рабочего дерева, замечаний по регрессиям и лишней сложности нет; тесты и браузер он отдельно не запускал.
- [x] 5.5 Синхронизировать основные specs, снова архивировать change в текущей ветке, повторить структурную валидацию и ревью всего итогового diff; обновить PR #860 результатами и отправить на человеческое ревью. Основные specs синхронизированы, архив возвращён в ту же ветку; второй reviewer проверил полный итоговый diff и не нашёл замечаний. PR body обновлён, готовность к ревью выставляется после публикации коммита; финальная OpenSpec-валидация повторяется после этой отметки.
