# Tasks

## 1. Анализатор и исходный baseline

- [x] 1.1 Сверить issue [#838](https://github.com/silentroach/kpshelkovo/issues/838), change и разрешение на первую реализацию с состоянием ветки. [Разрешение и коммит артефактов](https://github.com/silentroach/kpshelkovo/issues/838#issuecomment-5818952193).
- [x] 1.2 Подключить `rollup-plugin-visualizer` только к opt-in клиентской сборке Astro и добавить `pnpm bundle:analyze` с YAML-составом, размерами готовых JS/CSS и локальным HTML; проверить запуск, отсутствие серверных модулей и абсолютных путей, ненулевые размеры крупных зависимостей.
- [x] 1.3 Сохранить исходные YAML и HTML, повторить анализ на одинаковых исходниках и проверить воспроизводимость; убедиться, что отчёты не публикуются с сайтом и обычная сборка не обновляет снимки.

## 2. Инструкция и итоговая приёмка

- [x] 2.1 Описать `pnpm bundle:analyze`, пути отчётов и сравнение YAML при смене хешей в `docs/development.md`; проверить инструкцию на действующей команде.
- [x] 2.2 Добавить короткое правило в `AGENTS.md` для изменений клиентского графа и сборки; проверить, что оно ведёт к инструкции и отчётам без дублирования команды в нескольких местах.
- [x] 2.3 Выполнить `pnpm typecheck`, `pnpm test` и `pnpm bundle:analyze`; сверить новые YAML со снимком после первого подключения анализатора и убедиться, что после отката оптимизаций в итоговом diff остался только инструмент.
- [x] 2.4 Выполнить `pnpm openspec:validate` и проверить, что change без delta spec описывает только инструмент, а результаты локальной сборки не попадают в публичный сайт.
- [x] 2.5 Провести ревью всего diff ветки и рабочей копии, включая lock-файл и отчёты, с `ponytail-review`; исправить замечания и зафиксировать результаты проверок и оставшуюся ручную приёмку перед штатным архивированием и PR.
