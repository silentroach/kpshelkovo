# Tasks

## 1. Исходная проверка

- [x] 1.1 По `site-search` на актуальной локальной production-сборке просмотреть `SHR-L43`, `SHR-L44`, `L43`, совпадающий между частями короткий номер, неполный код, цифры и соседние текстовые запросы. Зафиксировать полезные URL, порядок и число результатов, холодные сетевые запросы поиска, размеры словаря и Pagefind для сравнения после правки; проверить свежесть индекса.

## 2. Статический индекс

- [x] 2.1 Перевести production build и `search:prepare` с CLI на один скрипт Node API Pagefind: индексировать HTML с прежним root selector, добавить по одной записи на участок из проверенного `dist/site/map/data/parcels.json`, записать индекс в прежние пути и сохранить компрессию/dev-snapshot. Проверить построение обеими командами, ссылки и metadata на примерах основного кода и алиаса, строгую остановку при повреждённом feed или ошибке Pagefind, отсутствие геометрии в записи; выполнить typecheck затронутого кода и удалить неиспользуемый CLI-конфиг.

## 3. Клиенты поиска и карты

- [x] 3.1 Перенести поиск номеров в существующий Pagefind-клиент: точный фильтр по полному/короткому коду и алиасам, исключение участков из текстовой выдачи, сортировка и один результат на участок, сохранение `?p=` в ссылке, общего лимита/total, догрузки, отмены устаревшего запроса и ошибок. Удалить `site-client.ts` и подключить клиент к прежнему диалогу. Проверить короткими тестами клиента и DOM точные/отрицательные совпадения, разные части, текстовые запросы, переход, retry и гонки запросов; выполнить typecheck.
- [x] 3.2 Переключить прямую ссылку карты на разрешение основного кода и алиаса по уже загружаемому `/map/data/parcels.json`, без Pagefind и словаря. Проверить тестами `PlaceMap` один запрос геометрии для известной ссылки, отсутствие запросов до ручного включения слоя, неизвестный номер с выключенным слоем, повтор после ошибки, сохранение `p` до успеха и фокус/очистку URL после него; выполнить официальный Svelte autofixer и повторить после исправлений.
- [x] 3.3 После перевода обоих клиентов удалить `/map/data/parcel-search.json`, search-adapter, схемы, типы и URL-helper, а также устаревшие тесты словаря. Проверить поиском ссылок, отсутствием файла в итоговом `dist/www/map/data/` и сборкой, что никакой runtime больше его не запрашивает.

## 4. Приёмка и завершение

- [x] 4.1 На обновлённой локальной production-сборке с настоящим индексом вручную сравнить запросы из 1.1; проверить, что обычные страницы не вытеснены, поиск не загружает геометрию и ссылки по алиасам работают. Измерить холодный трафик и размер индекса до/после в сопоставимых условиях, проверить переходы по `?p=` и ошибки карты в браузере; записать фактические результаты в PR без новой постоянной матрицы контента.
- [x] 4.2 Обновить ADR-041 и связанные актуальные инструкции о поисковом словаре, сохранив исторические записи прежнего change. Выполнить `pnpm typecheck`, `pnpm test`, `pnpm --filter @shelkovo/www test:search-quality`, при необходимости итоговый `pnpm build` и `pnpm openspec:validate`; сверить действующие `search`/`parcel-map` с поведением, провести ревью полного diff с `ponytail-review` и исправить замечания.
- [ ] 4.3 После проверок архивировать change штатным OpenSpec workflow без delta specs и обновить существующий PR #810 и ссылку из #804 на архив; проверить, что код и документы остались в ветке `feat/parcel-map-search`, указаны результаты проверок, байты и оставшаяся ручная приёмка.
