# Tasks

## 1. Подготовка реализации

- [ ] 1.1 В отдельной сессии прочитать change, отметку в #851 и фактическое состояние `perf/show-maps-progressively`; получить явное разрешение реализации после чтения артефактов и зафиксировать его в issue со ссылкой на коммит согласованного плана. Проверка: разрешение подтверждено, рабочая копия и исходный SHA известны.
- [ ] 1.2 До изменения кода снять baseline по методике design.md для превью и обзорной карты на мобильном и десктопе при первом и повторном открытии, проверить остальные renderer-ы и сохранить исходные bundle-отчёты через `pnpm bundle:analyze`. Проверка: записаны URL, условия, время видимости/готовности действия и сопоставимые результаты не менее трёх прогонов для представительных случаев.

## 2. Постепенный показ превью и независимые действия

- [ ] 2.1 В `MapPreview` убрать скрытие canvas и ожидание всех тайлов, заменить сплошной fallback места компактной ссылкой и отделить сообщение ошибки. Проверка: регрессионный тест допускает видимость карты с незавершёнными тайлами и незагруженным контролом; реальный браузер показывает доступную сцену без перекрытия.
- [ ] 2.2 Передавать действие от fallback к доступной штатной кнопке независимо от тайлов, сохранив фокус и самостоятельные ссылки поселка. Проверка: тесты покрывают готовый контрол при медленных тайлах, медленный контрол, сфокусированный fallback и отказ импорта; браузер подтверждает одну отдельную кнопку события без перекрытий, включая промежуточную автоматическую кнопку SDK.

## 3. Остальные renderer-ы без перекрывающего ожидания

- [ ] 3.1 Убрать загрузочную панель и скрытие canvas у `EditorialMapElement`, сохранив обработку ошибки и раннюю инициализацию всех схем. Проверка: тест с задержанным контролом, проверка ошибок и удаления экземпляра; в браузере несколько схем ниже экрана загружаются без прокрутки и печатаются после готовности с подложкой, объектами, атрибуцией и подписями.
- [ ] 3.2 Убрать перекрывающую загрузочную панель `PlaceMap` и независимое от неё ожидание контрола перед добавлением подготовленных маркеров/геометрии. Проверка: тесты задержанного и отклонённого импорта, уничтожения карты во время ожидания; в браузере работают кластеры, выделение по URL, переходы и слой участков без возврата общего лоудера.
- [ ] 3.3 Убрать панель и спиннер `SettlementMap`, добавлять маркеры независимо от загрузки штатного контрола и сохранить необходимые состояния управления. Проверка: тесты поздней готовности/отказа/повторной загрузки, маркеров и попапов; в браузере работают фильтры, повторный показ и восстановление после отказа, область резервирования места оболочки сохраняет размеры.

## 4. Документация и общая приёмка

- [ ] 4.1 Актуализировать пояснения ADR-039 и разделы о загрузке и действиях карт в дизайн-гайде. Проверка: правила больше не связывают видимость сцены или замену fallback с готовностью всей подложки и совпадают с delta specs.
- [ ] 4.2 Выполнить `pnpm openspec:validate`, `pnpm typecheck`, `pnpm test` и сборку с обновлением отчётов через `pnpm bundle:analyze`; сравнить состав и размеры с baseline. Проверка: команды проходят, изменения бандлов объяснены и актуальные YAML сохранены вместе с реализацией.
- [ ] 4.3 Повторить браузерные замеры в условиях baseline и пройти сценарии обоих delta specs на всех перечисленных в proposal поверхностях: медленные тайлы и отдельно контрол, мобильный/десктоп, первое/повторное открытие, Astro-навигация, ошибки, no-JS, указатель/клавиатура, атрибуция, прокрутка и отсутствие сдвигов; скрытая шапка ждёт показа. Проверка: записаны результаты, медианы/диапазоны представительных прогонов и оставшаяся ручная приёмка; к #851 подготовлено короткое резюме подтверждённых причин и дальнейших шагов без объявления полной загрузки ускоренной по одному факту удаления лоудера.
- [ ] 4.4 Вызвать `reviewer` для всего актуального diff, включая новые, staged и unstaged файлы, требования change и результаты проверок; разобрать замечания, исправить подтверждённые и повторить ревью после правок. Проверка: замечания закрыты или обоснованно отклонены; при недоступности reviewer выполнены и явно отмечены самостоятельный основной проход и отдельный проход `ponytail-review`. После sync/archive итоговое ревью повторяется по проектному workflow перед переводом того же draft PR в готовый к ревью.
