## Статус приёмки

По прямому указанию владельца 19 сентября change остаётся активным: доработки продолжатся. Архивирование отложено до отдельного разрешения; публикация текущего этапа не означает завершение всего change.

Change возобновлён 19 сентября после утверждения общей плашки для новости и мероприятия. Ранее завершённые 35 задач остаются историей; общая плашка и её проверки перечислены в разделе 10, согласованная уборка после ревью — в разделе 11. Разрешение реализации получено в текущем обсуждении, прежнее разрешение commit/push и обновления PR #748 сохраняется.

19 сентября ветка перебазирована на `6d17d016`; карты совмещены с ADR-039. По прямому запросу владельца пункт «События» временно закомментирован в обоих вариантах главного меню. Навигационная часть задачи 5.4 остаётся реализованной, но её показ отложен до решения владельца; это явное исключение приёмки данного выпуска.

После просмотра владельцем устранён отдельный вариант карты мероприятия: теперь её оформление и единственное внешнее действие совпадают с новостной картой. Иконка отмены заменена красной зачёркнутой датой. Уточнения отражены в design, основном и delta specs; актуальные проверки — в начале verification.md.

Завершены **35/35 задач** плана реализации от 16 сентября. Разделы 1–7 сохраняют задачи первоначального варианта; раздел 9 фиксирует переход к подробным мероприятиям, кратким дням и их поисковой политике. Это та же задача #224 и [PR #748](https://github.com/silentroach/kpshelkovo/pull/748).

Проверки каждого этапа собраны в [verification.md](verification.md). Старые отметки браузерной и HTTP-приёмки относятся к своим версиям; актуальная приёмка указана в начале отчёта. Человеческое ревью и merge выполняет владелец.

Прежняя задача 7.1 принята с [явным исключением владельца](https://github.com/silentroach/kpshelkovo/issues/224#issuecomment-5695610385) для трёх исходных rank failures в [#747](https://github.com/silentroach/kpshelkovo/issues/747). Это исключение не разрешает новые поисковые регрессии после смены корпуса.

## 1. Передача и исходная проверка

- [x] 1.1 В новой сессии прочитать issue #224, этот change и фактическое состояние рабочей ветки; получить явное разрешение реализации и зафиксировать его в issue со ссылкой на коммит артефактов. Проверка: опубликованная отметка относится к реализации, а не только к planning-only PR.
- [x] 1.2 До изменения поискового корпуса добавить целевые и соседние контрольные запросы в quality-матрицу и выполнить `pnpm --filter @shelkovo/www test:search-quality:update` по `site-search`; сохранить исходные URL, порядок, snippets и highlights для последующего сравнения.

## 2. Общие данные и календарная проекция

- [x] 2.1 Добавить Markdown-коллекцию событий, Zod raw-схему, отдельные readonly domain types, mapper и loader; проверить допустимость даты без часов, явного периода, цены текстом, одного источника и координат внешней площадки, а также отклонение невозможных дат, обратных интервалов, отсутствующего источника и несовместимых `ends_at`/`through`. Запустить точечные тесты и `pnpm typecheck`.
- [x] 2.2 Реализовать единую проекцию событий в дни и непустые месяцы с дедупликацией месячного списка; короткими тестами подтвердить несколько событий в день, ночь, включительный период, переход месяца/года, несмежные даты и сохранение отменённых записей.
- [x] 2.3 Реализовать выбор стартового месяца относительно переданного времени сборки по Москве и переходы между непустыми месяцами; тестами проверить текущий непустой месяц, ближайший будущий, последний прошедший, полностью пустой набор и границу московского месяца.

## 3. Страницы и карточки

- [x] 3.1 Добавить корень, месячные календарь/список и страницы дней, используя одну проекцию и обычные ссылки; на production-сборке проверить маршруты, отсутствие пустых страниц, сохранение месяца при смене вида, сохранение вида при переходе месяца и возврат со дня к месяцу.
- [x] 3.2 Реализовать полные карточки с ценой, возрастом, одним источником, описанием и состоянием, а также опциональной картой по существующему паттерну; проверить рендер неизвестных часов/места, условного набора, отмены, текста записи и вариантов цены без вымышленных значений.
- [x] 3.3 Подключить общий Markdown renderer, типограф и SiteMentionRegistry с backlinks раздела; проверить существующий контракт упоминаний, ошибку неизвестной формы имени и единственный основной backlink многодневной записи. Новые тесты разместить в ближайшей папке `tests/`.

## 4. ICS и миграция новостей

- [x] 4.1 Переиспользовать текущую сериализацию ICS в модуле событий и добавить обработчик `/events/calendar/<id>.ics`; тестами проверить UTF-8 folding/escaping, точный интервал, ночное окончание, экспортные два часа с пояснением, многодневный `VALUE=DATE` с исключительной датой конца, `TENTATIVE`/`CANCELLED` и отсутствие скачивания для одной даты без часов. Убедиться, что fallback не меняет domain/JSON/JSON-LD.
- [x] 4.2 Перевести новости на ссылки к общим событиям с сохранением прежнего новостного slug и compact-функционала; проверить падение на несуществующую ссылку и одинаковое обновление часов/места в новости, странице дня и ICS без циклических loaders.
- [x] 4.3 Мигрировать пять записей из трёх статей, сохранив сведения об организаторах, исполнителях, месте и координатах; зафиксировать прежние ICS URL/UID и проверить их после миграции. Для встречи 13 июня сверить `ok-meeting-recording-ban.md` и проверить отмену в карточках, JSON-LD и прежнем ICS.

## 5. Интеграция и публичные представления

- [x] 5.1 Добавить отдельный Public DTO, полную JSON-выдачу событий и Zod-derived схему; контрактными тестами проверить одну запись на многодневное событие, точность даты/времени, состояние, источник, связи с новостями и реальную доступность ICS без фиктивных значений.
- [x] 5.2 Сохранить совместимые календарные поля прежнего news JSON и документировать чтение новых date-only форм из полной выдачи событий; проверить прежние пять объектов через публичную схему, корректную отмену и полноту HTML-карточки новости при связи с событием без часов.
- [x] 5.3 Добавить Markdown companions через AST API, общий JSON-LD событий, фрагмент Public Surface Registry, корневой каталог API и именованные ссылки `/llms.txt` с пояснением назначения и полноты данных; проверить публичные URL, состояние/источник/период, отсутствие внутренних путей и совпадение ID многодневного Event на разных днях. Разово пройти путь от llms до карточек заданного дня и их источников через Markdown/JSON.
- [x] 5.4 Добавить раздел в desktop/mobile меню, канонические месяцы и дни в sitemap, страницы дней в Pagefind; повторить `pnpm --filter @shelkovo/www test:search-quality:update`, сравнить целевые и контрольные запросы с baseline и проверить исключение повторяющих месячных видов. Запустить `pnpm --filter @shelkovo/www test:search-quality`. Это первоначальный корпус; его замена подробными мероприятиями выполнена в 9.7.
- [x] 5.5 Подключить доставку новых HTML/Markdown/JSON/ICS-поверхностей и `Link` по действующим правилам nginx, сверить CSP карты и затронутые deploy-проверки; проверить конфигурацию и реальные ответы production-like сборки, включая MIME, прямой Markdown, согласование `Accept: text/markdown` и прежние ICS-адреса. Изменения deploy-потока сопровождать синхронной правкой workflow.
- [x] 5.6 Добавить SEO-метаданные по `docs/page-meta.md` и согласованный canonical для корня, месяца, дня и альтернативного списка; разово проверить отрендеренные title/description/H1/Open Graph, robots, sitemap без дублей и вымышленных lastmod, а также точность JSON-LD относительно видимых карточек. Проверить, что Pagefind-исключение месячных видов не превращается во внешний noindex.

## 6. Редакторские правила и первое наполнение

- [x] 6.1 Написать локальную инструкцию событий для ручного запуска агента и обновить точки входа в правилах, news-maker, продуктовых документах и глоссарии; проверить единый маршрут news/status/events, один источник, запись в body, условный набор, адресные уточнения и отсутствие требования создавать отдельную новость на каждый сеанс.
- [x] 6.2 Повторно проверить актуальные будущие анонсы GREEN.EVENTS, необходимые изображения и связанные уточнения; добавить подтверждённые события с конкретными датами. Проверка: у каждой новой записи есть основной URL и проверенные условия, альбомы/повторы не размножены, неопределённые кандидаты перечислены отдельно в передаче редактору, валидация данных и сборка проходят.
- [x] 6.3 Проверить редакторскую инструкцию на примерах #290, #137/#139, #174/#179, #275 и #183 из design; зафиксировать правильное извлечение даты с постера, адресную отмену, сохранение цены при уточнении окончания, разделение занятий и условный набор. Использовать эти примеры для разовой редакторской приёмки и коротких необходимых регрессий, а не для полного исторического импорта.

## 7. Приёмка реализации

- [x] 7.1 Выполнить `pnpm typecheck`, `pnpm test`, `pnpm build` и итоговый `pnpm --filter @shelkovo/www test:search-quality`; зафиксировать результаты и разобрать изменения поисковой выдачи, а не принять snapshots автоматически. Принято с исключением владельца для baseline failures; результаты не объявлены зелёными, исправление отслеживается в #747.
- [x] 7.2 Проверить production-сборку в браузере на desktop и телефоне: компактный календарь, отдельный список, полные карточки дня, пустые даты, пропуски месяцев, отмену, условный набор, внешнюю карту, ссылки источника/записи и скачивание ICS. Подтвердить клавиатурную навигацию, видимый фокус, текстовое различение состояний и отсутствие переполнения; закрыть браузерные сессии.
- [x] 7.3 Сверить весь результат со сценариями spec, включая старые новости и публичные контракты; выполнить `pnpm openspec:validate` и ревью всего актуального diff со staged, unstaged и новыми файлами, исправить дефекты и лишнюю сложность в пределах change.

## 8. Завершение после реализации

- [x] 8.1 После выполнения раздела 9 повторно синхронизировать основной `events-calendar` spec и архивировать этот change штатными workflows; проверить основной spec, архив и `pnpm openspec:validate`. На этапе повторного архивирования 16 сентября сверены 15 требований и 51 сценарий; CLI подтвердил отсутствие оставшихся delta-изменений. Операция публикации 8.2 затем закрыта. После rebase формулировки дополнительно согласованы с основными `news-event-calendar` и `news-event-places`; актуальный состав проверяется по самим specs.
- [x] 8.2 По разрешению на публикацию обновить существующий PR #748 с `Closes #224`, результатами новых проверок и оставшейся человеческой приёмкой; обновить ссылки и краткое резюме в issue #224. Полный diff к `origin/master` проверен; описания PR и issue обновлены. Отдельные issue/change/PR для этой доработки не создавались.

## 9. Доработка до merge: страницы мероприятий и карточка места

- [x] 9.1 Получить явное разрешение реализации пересмотренного proposal/design/spec и подтвердить полномочия на публикацию. Артефакты сохранены в `b39f4d5e`; [отметка в issue #224](https://github.com/silentroach/kpshelkovo/issues/224#issuecomment-5700783963) относится к пересмотренному плану. Разрешение реализации получено в текущем обсуждении, прежнее разрешение на публикацию подтверждено по issue.
- [x] 9.2 Реализовать канонический URL `/events/YYYY/MM/<slug>/` по московскому месяцу начала, отдельный slug без обязательного года и независимость URL от стабильного ID/ICS UID; Zod-проверками и точечными тестами подтвердить запрет числовых slug, коллизий итоговых URL, конфликтов с `/list/`, а также одну страницу многодневной записи. Сохранить прежние ICS-адреса и UID.
- [x] 9.3 Добавить подробные страницы мероприятий и превратить страницы дней в краткие списки; обновить ссылки месяца, новостей и backlinks. На готовой сборке проверить оба киноквиза 19 сентября, переходы день → мероприятие → день/месяц, неизвестные поля, отмену, условный набор, цену и аудиторию без дублирования полных описаний.
- [x] 9.4 Вынести общую app-owned оболочку карты для новостной карточки и карточки места; на подробной странице использовать правую колонку при достаточной ширине и компактный блок после основных сведений на телефоне. Проверить новость `/news/2026/05/victory-day-greenwood/`, длинное название места, координаты без названия, место без координат и полное отсутствие места; карту и её действие не дублировать под описанием. Календарное действие размещать рядом с датой, сохранив правила доступности ICS.
- [x] 9.5 При изменении месяца начала или slug обновлять canonical напрямую и все связанные ссылки в том же изменении. Сохранить стабильные ID записей и DOM-якорей строк дня, опубликованные ICS-адреса/UID. Тесты прямого переноса и ссылок прошли; текущая страница сада отвечает 200, незанятый путь предыдущего месяца — 404 без редиректа. После полной сборки файл редиректов отсутствует.
- [x] 9.6 Перевести Public DTO URL, Markdown companions, JSON-LD, sitemap, Public Surface Registry, каталог и llms на отдельные страницы. Через публичные ссылки пройти путь от llms к полному описанию, источнику и ICS; проверить HTML/Markdown, `Accept: text/markdown`, `Link`, JSON по Zod, единственный Event ID многодневной записи и отсутствие вымышленных фактов. Обновить редакторские и продуктовые инструкции под новые адреса и переносы.
- [x] 9.7 По `site-search` снять baseline до смены корпуса, индексировать подробные мероприятия вместо повторяющих дней, месяцев и списков; повторить production-like сравнение и `pnpm --filter @shelkovo/www test:search-quality:update`, затем `pnpm --filter @shelkovo/www test:search-quality`. Проверить целевые URL, отмену, snippets и соседние запросы; обновить отчёт с фактическими результатами, не распространить прежнее исключение на новые регрессии.
- [x] 9.8 Согласовать дизайн-гайд с карточкой места и проверить сборку в браузере при 1440, 390 и 320 px: полную страницу, краткий день, прежнюю новость, длинный текст, контраст поверх карты, клавиатуру, фокус и отсутствие переполнения. Закрыть браузерные сессии; сохранить результаты и оставшуюся ручную приёмку.
- [x] 9.9 После последних уточнений повторить подходящие проверки типов, тестов, сборки, HTTP и `pnpm openspec:validate`; сверить актуальные сценарии и весь diff. Проверены отсутствие нижней навигации, день начала в breadcrumbs и карта; новостная карточка в pixel-сравнении не изменилась. Typecheck, 1407 тестов и сборка прошли. Независимое ревью не нашло ошибок; поисковое сравнение metadata-варианта зафиксировано в `search-verification.md`. Отменённый генератор редиректов и усиление deploy-helper исключены из итогового результата.
- [x] 9.10 Вместе с задачами 9.2–9.3 перевести все `events[].event` новостей на единственный формат `YYYY/MM/<slug>` и разрешать только текущий canonical-ключ. Мигрировать fixtures и редакторские инструкции; Zod-проверками и интеграционными тестами подтвердить успешное разрешение новой формы, отказ для старого плоского ID/устаревшего адреса и ошибку не обновлённой ссылки после переноса. Проверить прежние новостные ICS-пути и UID после миграции.

## 10. Общая плашка новости и мероприятия

- [x] 10.1 Сохранить согласованные артефакты в ветке и отметить разрешение в issue; снять production search-quality baseline до перестановки содержимого, сохранив вывод и snapshots для сравнения. План — `7c5d3058`, отметка — issuecomment-5743845350. Baseline на `5f332d69`: 12 passed / 6 failed, без изменений snapshots; лог `shared-event-widget-baseline.log` в временном каталоге OpenCode.
- [x] 10.2 Перенести новостную плашку в один общий компонент и подключить его к обоим размещениям. Убрать дубли даты/ICS и отдельную карточку места со страницы мероприятия. Предметными тестами проверить состав плашки, единственный H1, самостоятельный/исторический ICS и DOM-якоря, неизвестное место, отмену, условный набор, ночные и многодневные периоды. Выполнено с последующими уточнениями: название присутствует и в плашке мероприятия, обе ширины 19rem, новостная плашка поднята к заголовку, иконки выровнены, копирайт справа.
- [x] 10.3 Согласовать дизайн-гайд; проверить обе плашки на desktop/mobile, доступность карты и ICS, красную зачёркнутую дату, длинное место и ширину 320 px. Выполнить typecheck, подходящие тесты, сборку и сравнение поиска с baseline, затем ревью всего актуального diff. Прошли typecheck, 1535 тестов, 8 браузерных проверок и сборка; измерения production-preview подтверждают одинаковые ширины и положение. Поиск сохраняет прежние четыре ранговых нарушения без новых acceptance failures; результаты в verification/search-verification.
- [x] 10.4 Синхронизировать основные specs; выполнить `pnpm openspec:validate`, опубликовать текущий этап в PR #748 с актуальной приёмкой и ссылками issue. Опубликован коммит `15e77738`, PR и ссылки issue обновлены. По указанию владельца change оставлен активным для следующих доработок, без архивирования.

Дополнение владельца в ходе реализации: убрать строку над заголовком или перенести её под него, выровнять хлебные крошки. Категория переносится к источнику под H1; обычный «Анонс» убирается, условный набор остаётся в плашке, отмена — в H1 и дате. Breadcrumbs используют стандартный `ui-page-header`, без собственного верхнего padding. Проверка входит в 10.3: сравнение положения с обычной страницей при desktop/mobile.

Ещё одно уточнение владельца: новостные плашки на широком экране выравниваются по правому краю общей области контента и поднимаются до уровня заголовка, как на мероприятии. Проверить соседство с заголовком/body без пустоты между метаданными и текстом, новость только с событием и неизменность полноширинной фотообложки; читаемая ширина текста и мобильное расположение сохраняются.

В обоих размещениях проверить одинаковую ширину `19rem` от `64rem` и полную ширину ниже; иконки времени/места выровнять относительно первой строки, включая переносы названия.

## 11. Уборка после ревью ветки

Владелец 19 сентября разрешил исправить устаревшие документы и выполнить предложенные упрощения: единые helpers событий и проверку поискового корпуса по опубликованному каталогу. Это уточнение текущей реализации в том же change. Архивирование остаётся отложенным.

- [x] 11.1 Обновить `docs/news.md`, proposal/design и согласовать delta/main specs с общей записью мероприятия, единым `EventWidget` и API Яндекс Карт по ADR-039. Убрать отменённые указания про iframe и независимое оформление карт; доступность ICS в `site-maps` связать с правилами событий. Проверка: сверка документов со схемой и компонентами, `pnpm openspec:validate`.
- [x] 11.2 Объединить форматирование периодов в `events/view.ts`, перенести туда helpers месяца плашки и карты, обновить потребителей и предметные тесты. Проверка: точное начало и окончание, ночь, включительный период, неизвестные часы и варианты места; typecheck, тесты и сборка.
- [x] 11.3 Сравнивать поисковые документы событий с Zod-проверенной полной JSON-выдачей вместо списка из семи записей. Сохранить проверки отсутствия дублей и навигационных страниц. Проверка: baseline до изменений, итоговый production search-quality со сравнением URL/snippets/highlights, ревью полного diff и запись результатов. Baseline/update/final: 12 passed / 6 failed с прежними ранговыми нарушениями и неизменными snapshots; проверка корпуса проходит. Результаты записаны в verification/search-verification.

## 12. Ограничение поискового корпуса

- [x] 12.1 Ограничить Pagefind-корпус мероприятий корнем `/events/` и событиями, завершившимися не более 30 дней назад либо ещё не начавшимися/не завершившимися; сохранить полные календарь, JSON, Markdown и sitemap. Проверка: тесты границы окна, production search-quality и проверка отсутствия дней/месяцев/списков в корпусе.
