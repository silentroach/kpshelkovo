# Tasks

## 1. Анализатор и исходный baseline

- [x] 1.1 В отдельной сессии подтвердить разрешение реализации в ветке `perf/trim-client-bundles`, сверить issue [#838](https://github.com/silentroach/kpshelkovo/issues/838) с change и фактическим состоянием ветки; зафиксировать ссылку на коммит согласованных артефактов и отметку разрешения перед изменением кода. [Разрешение группы 1 и коммит артефактов](https://github.com/silentroach/kpshelkovo/issues/838#issuecomment-5818952193).
- [x] 1.2 Подключить `rollup-plugin-visualizer` только к opt-in клиентской сборке Astro и добавить `pnpm bundle:analyze` с YAML-составом, размерами готовых JS/CSS и локальным HTML; проверить успешный запуск на текущем стеке, отсутствие серверных модулей/абсолютных путей и наличие ожидаемых крупных зависимостей с ненулевыми размерами.
- [x] 1.3 До очистки кода сохранить исходные YAML и HTML, повторить анализ на тех же исходниках и проверить воспроизводимость; убедиться, что отчёты не публикуются с сайтом и обычная сборка не обновляет снимки. Зафиксировать исходный результат для сравнения всех следующих этапов.

## 2. Часы работы без редакционных зависимостей

- [ ] 2.1 Разорвать цепочку `opening-hours` → схемы/общий `mentions` → unified/remark лёгкими импортами констант, обновив всех вызывающих; проверить существующие тесты часов работы, включая московский часовой пояс, границы интервалов и расписание после полуночи.
- [ ] 2.2 Запустить `pnpm bundle:analyze`, подтвердить уход Markdown-зависимостей из цепочки часов/превью и сообщить изменение состава и размеров относительно baseline; проверить статус открытости на карточке места и общей карте.

## 3. Подготовленные подписи и данные карт

- [ ] 3.1 Добавить build-time подготовку подписей существующей типографикой и подключить её к Markdown-картам, превью мест и `PlaceMap.places`; убрать клиентский форматировщик и ветку загрузки мест через публичный DTO. Проверить одинаковый текст на трёх поверхностях, корректный пустой список мест и сохранение исходных строк в публичных JSON/Markdown существующими и точечными регрессионными тестами.
- [ ] 3.2 Запустить `pnpm bundle:analyze`, проверить уход Typograf/YAML из клиентских карт и сравнить результат с предыдущим этапом; отдельно измерить изменение HTML `/map/` вместе с исчезнувшим запросом `/map/data/places.json` и клиентским JS.
- [ ] 3.3 Удалить `EditorialMapDataSchema` и повторную Zod-проверку встроенных подготовленных данных; перенести нужные проверки исходников в существующие build-time тесты вместо ослабления их покрытия. Проверить отказ сборки на ошибочных геоданных, безопасный текст подписей и сохранение обработки ошибок/cleanup; повторить анализ и сообщить изменение Zod-зависимостей, сохранив проверку отдельно загружаемого слоя участков.

## 4. Устранение лишнего Luxon

- [ ] 4.1 Отделить `formatPlaceStatus` от build-time view и вычисления года статуса, обновив всех потребителей; проверить прежние подписи статусов тестами и убедиться по новому отчёту, что карта больше не удерживает Luxon через эту цепочку.
- [ ] 4.2 Перевести дату поискового результата на лёгкое форматирование через `Intl` с прежним русским видом и `Europe/Moscow`; проверить дату без времени, timestamp около полуночи, отсутствие даты и открытие результата. Сопоставить видимые даты до/после на production-индексе и по отчёту подтвердить отсутствие Luxon в этой цепочке загрузки поиска.

## 5. Условные ресурсы карт и виджетов

- [ ] 5.1 Подключать регистрацию редакционных карт только при наличии элементов текущей страницы, а общую отрисовку геометрии preview — только при наличии геометрии; проверить несколько карт, прямой вход и Astro-переходы без карты → с картой → без карты → назад, а также событие с фоновой картой без редакционной геометрии.
- [ ] 5.2 Устранить подключение CSS отсутствующих `EventWidget`, `MapPreview` и редакционной карты, сохранив принадлежность стилей, каскад и печать; проверить фактические stylesheet requests и отсутствие сдвигов/неоформленных блоков на новостях и KB с блоками и без них.
- [ ] 5.3 Запустить `pnpm bundle:analyze` и проверить сетевые цепочки затронутых страниц: новость и KB без карт не загружают renderer редакционной карты, а новость без события — его ресурсы. Сообщить изменения JS/CSS относительно предыдущего этапа и исходного состояния.

## 6. Документация и итоговая приёмка

- [ ] 6.1 Описать команду, пути отчётов, чтение Git diff при смене хешей и обновление baseline в `docs/development.md`; добавить короткое правило агенту в `AGENTS.md` и уточнить ADR-039 о подготовке/передаче данных. Проверить соответствие инструкции действующей команде, используя профильные skills для агентских правил и ADR.
- [ ] 6.2 Выполнить `pnpm typecheck`, `pnpm test` и `pnpm --filter @shelkovo/www test:search-quality`; проверить изменённые Svelte-компоненты штатным autofixer. Проверки должны подтверждать поведение карт, подписей, часов и поиска, а не точные имена чанков или снимок всего сайта.
- [ ] 6.3 На локальной production-сборке проверить desktop/mobile, подписи и цифры точек, жесты/resize, фокус, отказ SDK, печать карты ниже экрана и клиентские возвраты; проверить карточку места, общую карту со слоем участков, виджет события и затронутые shared-стили в `/815/compare/` и карточке поселка. Контрольные главная и `/status/` сохраняют рабочий вид и навигацию.
- [ ] 6.4 Выполнить финальный `pnpm bundle:analyze`, сохранить актуальные YAML и предоставить локальный HTML; сверить исходные и итоговые модули, JS/CSS и Brotli, отдельно учесть HTML/data общей карты и объяснить каждое существенное увеличение. Подготовить краткий итог до/после по этапам для PR без обещания точного выигрыша внешних ресурсов.
- [ ] 6.5 Выполнить `pnpm openspec:validate`, сопоставить результат с каждым сценарием delta spec и действующими контрактами карт/часов/поиска; проверить отсутствие разовых условий и дублей в требованиях перед sync/archive.
- [ ] 6.6 Провести ревью всего diff задачи, включая коммиты ветки, staged, unstaged, новые файлы, lock-файл и baseline, с `ponytail-review`; исправить собственные замечания и выполнить затронутые проверки повторно. Зафиксировать выполненные проверки и оставшуюся ручную приёмку для последующего штатного sync/archive и PR.
