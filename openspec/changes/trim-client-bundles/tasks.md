# Tasks

## 1. Анализатор и исходный baseline

- [x] 1.1 В отдельной сессии подтвердить разрешение реализации в ветке `perf/trim-client-bundles`, сверить issue [#838](https://github.com/silentroach/kpshelkovo/issues/838) с change и фактическим состоянием ветки; зафиксировать ссылку на коммит согласованных артефактов и отметку разрешения перед изменением кода. [Разрешение группы 1 и коммит артефактов](https://github.com/silentroach/kpshelkovo/issues/838#issuecomment-5818952193).
- [x] 1.2 Подключить `rollup-plugin-visualizer` только к opt-in клиентской сборке Astro и добавить `pnpm bundle:analyze` с YAML-составом, размерами готовых JS/CSS и локальным HTML; проверить успешный запуск на текущем стеке, отсутствие серверных модулей/абсолютных путей и наличие ожидаемых крупных зависимостей с ненулевыми размерами.
- [x] 1.3 До очистки кода сохранить исходные YAML и HTML, повторить анализ на тех же исходниках и проверить воспроизводимость; убедиться, что отчёты не публикуются с сайтом и обычная сборка не обновляет снимки. Зафиксировать исходный результат для сравнения всех следующих этапов.

## 2. Часы работы без редакционных зависимостей

- [x] 2.1 Разорвать цепочку `opening-hours` → схемы/общий `mentions` → unified/remark лёгкими импортами констант, обновив всех вызывающих; проверить существующие тесты часов работы, включая московский часовой пояс, границы интервалов и расписание после полуночи.
- [x] 2.2 Запустить `pnpm bundle:analyze`, подтвердить уход Markdown-зависимостей из цепочки часов/превью и сообщить изменение состава и размеров относительно baseline; проверить статус открытости на карточке места и общей карте.

## 3. Подготовленные подписи и данные карт

- [ ] 3.1 Добавить build-time подготовку видимых подписей точек существующей типографикой `@shelkovo/markdown` как обычного текста, не меняя исходную геометрию и скрытые описания. Точечным тестом проверить пробелы, кавычки, HTML/Markdown/mention-подобный текст и неизменность исходной строки.
- [ ] 3.2 Передавать подготовленную геометрию в HTML Markdown-карт и убрать клиентский форматировщик подписей; проверить готовый текст и безопасный показ через `textContent`, сохранение исходных строк в публичном Markdown и прежние сценарии редакционной карты.
- [ ] 3.3 Подключить ту же подготовку к геометрии превью места; проверить одинаковую подпись в Markdown-карте и превью, не меняя геометрию и поведение фоновых карт без редакционных точек.
- [ ] 3.4 Передавать выбранные поля мест с подготовленной геометрией в `PlaceMap.places` из `/map/`; удалить клиентские `dataUrl`/`fetchPlaces` и преобразование публичного DTO. Проверить совпадение подписей на всех трёх поверхностях, пустой список без догрузки, работу маркеров и сохранение исходных строк в публичном `/map/data/places.json` существующими и точечными тестами.
- [ ] 3.5 Запустить `pnpm bundle:analyze`, проверить уход Typograf/YAML из клиентских карт и сравнить состав JS/CSS с группой 2; отдельно измерить изменение HTML `/map/` вместе с исчезнувшим запросом `/map/data/places.json` и клиентским JS.
- [ ] 3.6 Удалить `EditorialMapDataSchema` и повторную Zod-проверку встроенной подготовленной геометрии; перенести необходимые проверки исходников в существующие build-time тесты вместо ослабления их покрытия. Проверить отказ сборки на ошибочных геоданных, безопасный текст подписей, обработку ошибок/cleanup и сохранение проверки отдельно загружаемого слоя участков.
- [ ] 3.7 Повторить `pnpm bundle:analyze` после удаления схемы; сравнить состав и размеры с результатом 3.5, сообщить изменение Zod-зависимостей и подтвердить, что отдельно загружаемый слой участков сохраняет свою валидацию.

## 4. Устранение лишнего Luxon

- [ ] 4.1 Отделить `formatPlaceStatus` от build-time view и вычисления года статуса, обновив всех потребителей; проверить прежние подписи статусов тестами и убедиться по новому отчёту, что карта больше не удерживает Luxon через эту цепочку.
- [ ] 4.2 Перевести дату поискового результата на лёгкое форматирование через `Intl` с прежним русским видом и `Europe/Moscow`; проверить дату без времени, timestamp около полуночи и отсутствие даты тестами, сохранив открытие результата. Применить `site-search` перед изменением поискового кода.
- [ ] 4.3 Сопоставить видимые даты до/после на production-индексе и проверить открытие результата; запустить `pnpm bundle:analyze` и подтвердить отсутствие Luxon в цепочке загрузки поиска.

## 5. Условные ресурсы карт и виджетов

- [ ] 5.1 Подключать регистрацию редакционных карт только при наличии элементов текущей страницы; проверить несколько карт, прямой вход и Astro-переходы без карты → с картой → без карты → назад, включая очистку экземпляров карт.
- [ ] 5.2 Подключать отрисовку редакционной геометрии в общем preview только при наличии геометрии; проверить превью места с геометрией и событие с фоновой картой без неё.
- [ ] 5.3 Устранить подключение CSS редакционной карты на страницах без её блоков; проверить фактические stylesheet requests, оформление карт, каскад и печать на новостях и KB с картами и без них.
- [ ] 5.4 Устранить подключение CSS отсутствующих `EventWidget` и `MapPreview`; проверить фактические stylesheet requests, оформление присутствующих блоков и отсутствие сдвигов на новостях с виджетом и без него.
- [ ] 5.5 Запустить `pnpm bundle:analyze` и проверить сетевые цепочки затронутых страниц: новость и KB без карт не загружают renderer редакционной карты, а новость без события — его ресурсы. Сообщить изменения JS/CSS относительно предыдущего этапа и исходного состояния.

## 6. Документация и итоговая приёмка

- [ ] 6.1 Описать `pnpm bundle:analyze`, пути отчётов, чтение Git diff при смене хешей и обновление baseline в `docs/development.md`; проверить инструкцию на действующей команде.
- [ ] 6.2 Уточнить ADR-039 о подготовке подписей и передаче данных картам, сверив решение с реализованными границами; использовать профильный skill для ADR.
- [ ] 6.3 Добавить короткое правило агенту в `AGENTS.md` о проверке клиентского графа и baseline; использовать профильный skill для агентских правил.
- [ ] 6.4 Выполнить `pnpm typecheck`, `pnpm test` и `pnpm --filter @shelkovo/www test:search-quality`; проверить изменённые Svelte-компоненты штатным autofixer. Проверки должны подтверждать поведение карт, подписей, часов и поиска, а не точные имена чанков или снимок всего сайта.
- [ ] 6.5 На локальной production-сборке проверить Markdown-карты на desktop/mobile: подписи и цифры точек, жесты/resize, фокус, отказ SDK, печать карты ниже экрана и клиентские возвраты.
- [ ] 6.6 На локальной production-сборке проверить карточку места и общую `/map/` со слоем участков: геометрию, подписи, действия маркеров, фокус и клиентские возвраты на desktop/mobile.
- [ ] 6.7 На локальной production-сборке проверить виджет события, затронутые shared-стили в `/815/compare/` и карточке поселка; убедиться, что контрольные главная и `/status/` сохраняют рабочий вид и навигацию.
- [ ] 6.8 Выполнить финальный `pnpm bundle:analyze`, сохранить актуальные YAML и предоставить локальный HTML; сверить исходные и итоговые модули, JS/CSS и Brotli.
- [ ] 6.9 Отдельно учесть HTML/data общей карты и объяснить каждое существенное увеличение; подготовить краткий итог до/после по этапам для PR без обещания точного выигрыша внешних ресурсов.
- [ ] 6.10 Выполнить `pnpm openspec:validate`, сопоставить результат с каждым сценарием delta spec и действующими контрактами карт/часов/поиска; проверить отсутствие разовых условий и дублей в требованиях перед sync/archive.
- [ ] 6.11 Провести ревью всего diff задачи, включая коммиты ветки, staged, unstaged, новые файлы, lock-файл и baseline, с `ponytail-review`; исправить собственные замечания и выполнить затронутые проверки повторно. Зафиксировать выполненные проверки и оставшуюся ручную приёмку для последующего штатного sync/archive и PR.
