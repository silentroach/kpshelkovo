## 1. Реализация после разрешения владельца

- [ ] 1.1 Включить штатный mouse drag карты места, переименовать внутренний opt-in в `interactive` и согласовать выбор behaviors для pointerdown/wheel. Проверка: предметные тесты подтверждают основную кнопку мыши, исключение touch, сохранение drag после wheel, неподвижные фоны и снятие слушателей при повторном подключении.
- [ ] 1.2 Согласовать дизайн-гайд и ADR-028/039 с мышиным перетаскиванием. Проверка: документы соответствуют delta specs и сохраняют правило сенсорной прокрутки страницы.

## 2. Приёмка

- [ ] 2.1 Выполнить `pnpm typecheck`, подходящие тесты и `pnpm build`; проверить с настоящим SDK перетаскивание мышью в обе стороны, отпускание внутри/снаружи карты, движение без кнопки, drag после wheel/pinch и mouse → touch. Проверка: записаны фактические результаты desktop/mobile, сохранены географические данные, прокрутка и фоновые карты; браузерная сессия закрыта.
- [ ] 2.2 Проверить весь актуальный diff, синхронизировать основные specs и пересекающийся блок активного `add-events-calendar`; выполнить `pnpm openspec:validate` и `git diff --check`. Проверка: контракты согласованы с результатом, открытых дефектов нет, результаты приёмки записаны.

После приёмки архивировать этот change штатным workflow. Связанный `add-events-calendar` остаётся активным по указанию владельца; публикация следует действующим разрешениям на текущую ветку.
