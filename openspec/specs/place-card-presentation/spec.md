# place-card-presentation Specification

## Purpose

Помогать жителю прочитать сведения о месте, увидеть его расположение и перейти к картам или контактам из понятной адаптивной HTML-карточки.

## Requirements

### Requirement: Карточка различает сведения о месте и географический контекст

HTML-карточка SHALL показывать название, доступное расписание перед редакционным описанием и географическое превью. Описание SHALL использовать `body`, если он заполнен, иначе `summary`; отдельное автоматическое повторение `summary` перед `body` SHALL отсутствовать. Имеющийся адрес SHALL выводиться под превью без лейбла «Адрес». Карточка SHALL сохранять читаемый порядок и доступ к содержимому на узком экране без горизонтального переполнения страницы.

Карта места SHALL иметь рамку, скругление и тень в едином визуальном стиле с компактным виджетом события по дизайн-гайду. Размер географического блока SHALL резервироваться до загрузки карты; рамка SHALL не скрывать штатные действия и атрибуцию.

Логотип и копирайт картографического сервиса SHALL располагаться вместе справа внизу карты и сохранять читаемость, рабочие ссылки и клавиатурный доступ без пересечения со штатным действием карты.

#### Scenario: Место с подробным описанием и адресом

- **WHEN** у места заполнены `body`, `summary`, адрес и расписание
- **THEN** часы расположены перед `body`, адрес находится под превью, а `summary` не добавлен вторым вводным описанием

#### Scenario: Минимальное наполнение

- **WHEN** у места нет `body`, адреса и расписания
- **THEN** описание использует `summary`, а пустые блоки адреса и часов не выводятся

#### Scenario: Узкий экран

- **WHEN** карточка открыта на узком экране с длинным названием или текстом
- **THEN** содержимое и действия переносятся в читаемый вертикальный поток без обрезки и горизонтального переполнения

#### Scenario: Загрузка карты внутри рамки

- **WHEN** карта места переходит из состояния загрузки к готовому виду
- **THEN** размер географического блока и положение адреса сохраняются, карта заполняет рамку
- **AND** штатные действия и атрибуция доступны целиком

#### Scenario: Атрибуция на узкой карте

- **WHEN** готовая карта места показана на узком экране
- **THEN** логотип и копирайт находятся справа внизу внутри рамки, не обрезаются и не перекрываются штатной кнопкой
- **AND** ссылки атрибуции доступны указателем и клавиатурой с видимым фокусом

### Requirement: Действия с местом доступны рядом с названием

HTML-карточка SHALL показывать рядом с названием ссылку-иконку общей карты только для места с `show_on_map: true` и ссылку-иконку «Контакты и отзывы» только при наличии связанного контакта. Переход на общую карту SHALL сохранять фокус `/map/?h=<slug>`, переход к контакту SHALL вести на связанную карточку «Сарафана». Каждое действие SHALL иметь понятное доступное имя, обычный `title`, клавиатурный доступ и видимый фокус.

#### Scenario: Обе ссылки доступны

- **WHEN** место включено в общую карту и имеет связанный контакт
- **THEN** рядом с названием доступны обе ссылки с правильными назначениями и подписями
- **AND** пользователь может последовательно сфокусировать и активировать их с клавиатуры

#### Scenario: Место отсутствует на общей карте и не имеет контакта

- **WHEN** у места нет `show_on_map: true` и связанного контакта
- **THEN** соответствующие действия и пустые кнопки не отображаются

### Requirement: Превью сразу показывает точку и всю геометрию места

После успешной загрузки HTML-карточка SHALL показывать превью своего места независимо от `show_on_map`: точку с его существующей иконкой либо стандартным маркером и все имеющиеся области. Контуры SHALL быть видимы без наведения, выбора или временной подсветки. Начальный охват и охват после изменения размера SHALL включать точку и все области целиком с учётом элементов карты.

Превью SHALL позволять пользователю увеличивать и уменьшать масштаб карты жестом сведения или разведения двух пальцев на сенсорном экране и трекпаде. Обычное колесо мыши, вертикальный свайп одним пальцем и жест прокрутки двумя пальцами на трекпаде SHALL прокручивать страницу без изменения масштаба карты. Жест масштабирования карты SHALL не масштабировать одновременно всю страницу.

Карту SHALL быть возможно перетаскивать с удержанием основной кнопки мыши, включая нажатие кнопки трекпада. Отпускание кнопки SHALL завершать перетаскивание, в том числе за пределами карты; дальнейшее движение указателя без нажатия SHALL не перемещать карту. Сенсорное касание SHALL не включать перетаскивание одним пальцем. Вращение карты SHALL оставаться отключённым. Жесты SHALL менять видимую область, а не координаты места или его контуров.

#### Scenario: Несколько раздельных областей

- **WHEN** место имеет несколько отдельных полигонов
- **THEN** превью одновременно показывает маркер и все контуры без соединения отдельных областей
- **AND** смена ширины страницы сохраняет их видимость

#### Scenario: Место без геометрии

- **WHEN** у места есть координаты, но нет областей
- **THEN** превью показывает точку и локальное окружение без выдуманного контура

#### Scenario: Место не включено в общую карту

- **WHEN** открыта публичная карточка без `show_on_map: true`
- **THEN** её собственное превью доступно и не требует включения места в общую карту

#### Scenario: Прокрутка над превью

- **WHEN** пользователь прокручивает страницу колесом, вертикальным свайпом одним пальцем или жестом прокрутки трекпада над превью
- **THEN** прокручивается страница, а карта не меняет масштаб или положение из-за этого жеста

#### Scenario: Масштабирование двумя пальцами

- **WHEN** пользователь сводит или разводит два пальца над готовой картой места на сенсорном экране или трекпаде
- **THEN** меняется масштаб карты, а размер страницы и рамки остаётся прежним
- **AND** масштабирование сохраняет географические координаты маркера и контуров

#### Scenario: Перетаскивание мышью

- **WHEN** пользователь зажимает основную кнопку мыши над картой места и перемещает указатель
- **THEN** видимая область карты следует за движением указателя, пока кнопка удерживается
- **WHEN** пользователь отпускает кнопку внутри карты или за её пределами и продолжает двигать указатель
- **THEN** эти движения не продолжают перетаскивание карты

#### Scenario: Смена мыши на сенсорное касание

- **WHEN** после перетаскивания мышью пользователь на устройстве с сенсорным экраном проводит одним пальцем над картой
- **THEN** жест прокручивает страницу, а не перемещает карту

### Requirement: Переход во внешнюю карту сохраняется при недоступном превью

Загруженное превью SHALL предоставлять штатное действие перехода в Яндекс Карты без второй дублирующей ссылки под ним. Во время загрузки, при ошибке API или без JavaScript HTML-карточка SHALL сохранять обычную доступную ссылку на исходный `mapUrl` места. Недоступность превью SHALL не скрывать описание, адрес, расписание или остальные ссылки карточки.

#### Scenario: Превью загрузилось

- **WHEN** карта готова и её штатное действие доступно
- **THEN** пользователь может открыть Яндекс Карты из превью, а резервная ссылка не дублирует это действие в видимой странице

#### Scenario: Карта не загрузилась

- **WHEN** загрузка API завершилась ошибкой либо JavaScript недоступен
- **THEN** исходный `mapUrl` доступен как обычная HTML-ссылка, а остальные сведения места можно прочитать

### Requirement: Маркер превью отражает часы работы как общая карта

При наличии расписания маркер превью SHALL использовать ту же оценку открытости по московскому времени, что и общая карта: закрытое место показывается обесцвеченным маркером со значком часов, открытое — цветным без этого значка. Состояние SHALL обновляться при смене рабочего интервала. При отсутствии расписания превью SHALL не обозначать место закрытым. Статусы строительства и планирования SHALL не подменять часы работы.

#### Scenario: Перерыв или окончание рабочего дня

- **WHEN** текущее московское время находится вне рабочего интервала места
- **THEN** маркер обесцвечен и показывает значок часов, как на общей карте
- **AND** при наступлении следующего рабочего интервала маркер возвращается к обычному виду без перезагрузки страницы

#### Scenario: Расписания нет

- **WHEN** у места отсутствует расписание
- **THEN** превью показывает обычный маркер без значка закрытого места

### Requirement: Динамический статус работы остаётся компактным и доступным

При наличии расписания и JavaScript HTML-карточка SHALL показывать зелёный или красный кружок после подписи «Время работы», обычный `title` «Открыто до HH:MM» либо «Сейчас закрыто» и текстовый эквивалент для скринридера. Без JavaScript кружок SHALL быть скрыт, а полное расписание SHALL оставаться доступным. Расчёт времени и отсутствие заглушек для места без расписания определяются `place-opening-hours`.

#### Scenario: Доступный текущий статус

- **WHEN** место открыто в текущем рабочем интервале по московскому времени
- **THEN** кружок зелёный, `title` и доступная подпись содержат время окончания этого интервала

#### Scenario: Статическое расписание

- **WHEN** карточка с расписанием открыта без JavaScript
- **THEN** полная неделя читается без раскрытия блока, а невычисленный кружок скрыт
