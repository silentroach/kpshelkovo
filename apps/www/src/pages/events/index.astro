---
import EventMonthPage from '@/components/events/EventMonthPage.astro';
import { loadEventsBuildData } from '@/lib/events/build';

const { data, now, startMonth } = await loadEventsBuildData();
---

<EventMonthPage calendar={data.calendar} now={now} month={startMonth} root />
