---
import Breadcrumbs from '@shelkovo/ui/Breadcrumbs.astro';

import ReviewCard from '@/components/reviews/ReviewCard.astro';
import BaseLayout from '@/layouts/BaseLayout.astro';
import {
  withHomeBreadcrumbs,
  withHomeSchemaBreadcrumbs,
} from '@/lib/breadcrumbs';
import { loadReviewsData } from '@/lib/reviews/load';
import { reviewsRulesUrl, reviewsUrl } from '@/lib/reviews/routes';
import { reviewsCollectionPageSchema } from '@/lib/reviews/seo';

const data = await loadReviewsData();
const title = 'Отзывы собственников — Шелково Онлайн';
const description =
  'Независимые отзывы текущих собственников о жизни в Шелково: место, застройщик и обслуживание без общей оценки поселка.';
const keywords = [
  'отзывы Шелково',
  'собственники Шелково',
  'КП Шелково отзывы',
  'Шелково Эко Клаб отзывы',
  'Шелково Форест отзывы',
  'Шелково Вилладж отзывы',
  'Шелково Парк отзывы',
  'Шелково Ривер отзывы',
  'ОК Комфорт отзывы',
];
const breadcrumbs = withHomeBreadcrumbs([{ label: 'Отзывы' }]);
const schema = reviewsCollectionPageSchema({
  name: title,
  description,
  url: reviewsUrl(),
  breadcrumbs: withHomeSchemaBreadcrumbs([
    { name: 'Отзывы', url: reviewsUrl() },
  ]),
  items: data.reviews,
});
---

<BaseLayout
  title={title}
  description={description}
  canonical={reviewsUrl()}
  keywords={keywords}
  schema={schema}
>
  <main class="ui-page reviews-page">
    <section class="ui-page-header">
      <div class="ui-page-header-stack">
        <Breadcrumbs items={breadcrumbs} />

        <div>
          <h1 class="ui-page-title">Отзывы собственников</h1>
          <p class="ui-copy reviews-intro">
            Вы собственник участка или дома в Шелково?{' '}
            <a href={reviewsRulesUrl()} class="site-text-link">
              Оставьте свой отзыв!
            </a>
          </p>
        </div>
      </div>
    </section>

    <section class="reviews-content">
      <h2 class="ui-visually-hidden">Список отзывов</h2>
      {
        data.reviews.length > 0 ? (
          <ul class="reviews-list--flush-first reviews-list">
            {data.reviews.map((review) => (
              <li>
                <ReviewCard review={review} />
              </li>
            ))}
          </ul>
        ) : (
          <div class="reviews-empty">
            <p class="ui-copy reviews-muted">
              Если вы собственник участка или дома в Шелково,{' '}
              <a href={reviewsRulesUrl()} class="site-text-link">
                посмотрите, как оставить свой отзыв
              </a>
              .
            </p>
          </div>
        )
      }
    </section>
  </main>
</BaseLayout>

<style>
  .reviews-page {
    padding-block-end: 1.5rem;
  }

  .reviews-intro {
    max-width: 48rem;
    margin-block-start: 0.75rem;
    color: var(--color-text-muted);
  }

  .reviews-content {
    margin-block-start: 2rem;
  }

  .reviews-list > li + li {
    margin-block-start: 1rem;
  }

  .reviews-empty {
    padding: 1.5rem 1.25rem;
  }

  .reviews-muted {
    color: var(--color-text-muted);
  }

  @media (min-width: 48rem) {
    .reviews-page {
      padding-block-end: 2.5rem;
    }

    .reviews-empty {
      padding: 1.75rem 1.5rem;
    }
  }

  @media (min-width: 64rem) {
    .reviews-page {
      padding-block-end: 3rem;
    }
  }
</style>
