# AGENTS.md

Локальные правила для данных раздела `Сарафан`.

- Один Markdown-файл в этой папке равен одному опубликованному контакту.
- Не добавлять реальные контакты без источника от пользователя.
- Frontmatter должен проходить `RawContactSchema`: `title`, `slug`, `category`, `updated_at`, `contacts` обязательны.
- `search_aliases` — необязательный служебный массив только для внутрисайтового поиска. Перед его добавлением обязательно подключать `site-search` и сравнивать production-выдачу до и после. По умолчанию не добавлять. Заполнять только для подтвержденной пользовательской формулировки, по которой подходящий контакт не находится; не повторять текст из `title`, `summary` или body, не придумывать синонимы и опечатки «на всякий случай» и не использовать поле как список SEO-ключей.
- `slug` писать латиницей в lower-case, с цифрами и дефисами.
- `updated_at` хранить как календарную дату `YYYY-MM-DD`.
- В списке контактов не обещать качество услуги: не использовать формулировки `проверенный`, `надежный`, `лучший`, `рекомендуем` от лица сайта.
- В `reviews.summary` не цитировать отзыв дословно: давать короткую редакционную выжимку сути, а ссылку на исходный отзыв хранить в `url`.
- В `reviews.summary` описывать сам опыт и результат без шаблонного «заказчик/заказчица/покупатель отметил». Сохранять смысл источника: не превращать аккуратность в оценку медлительности и не усиливать похвалу.
- Не дублировать в body опыт, который уже показан в `reviews`: отзывы и описание контакта должны дополнять друг друга. Если дополнительных полезных сведений нет, body можно оставить пустым.
- Не добавлять очевидные советы и общие дисклеймеры: «цену уточняйте у исполнителя», «стоимость зависит от объёма», «наличие уточняйте» и подобные приписки без конкретной пользы.
- Оговорки оценивать по контексту, актуальности и последующему опыту автора. Не выносить промежуточное сомнение в отдельное предупреждение, если дальнейшее обсуждение его снимает; существенные нерешённые проблемы не скрывать автоматически из-за положительной итоговой оценки.
- В body встраивать ссылку в уместные слова предложения вместо голого URL или отдельной приписки «Источник», если так сохраняется понятная связь с источником. Ссылки отдельных отзывов хранить в `reviews.url`.
- При добавлении или правке полезного контакта включать vCard через `vcf.enable: true`, если известны имя и фамилия человека или название организации.
- Для человека указывать `vcf.kind: person` и обязательно заполнять оба поля: `vcf.name.given` и `vcf.name.family`. Не включать vCard для человека, если известна только одна часть имени.
- Для организации указывать `vcf.kind: organization` и `vcf.organization` с названием организации.
- Поля внутри `vcf` переопределяют данные основной карточки или добавляют недостающие сведения; незаданные телефон, сайт, адрес, ссылка на карту и координаты генератор берет из карточки.
- Координаты точки хранить в необязательном блоке `location.coordinates` с числовыми полями `lat` и `lng`.
- Полные российские телефоны с префиксом `8` или `+7` публично отображаются как `+7 XXX XXX-XX-XX`; исходную запись в карточке можно не переписывать, форматирование выполняется при рендере.
- В `tel:` и vCard попадает номер без визуальных разделителей. Международный номер считается полным только при явном `+` и 8–15 цифрах: его видимая запись сохраняется. Неполные, маскированные номера и записи с добавочным показываются как в карточке, но не превращаются в `tel:` и не попадают в vCard.
