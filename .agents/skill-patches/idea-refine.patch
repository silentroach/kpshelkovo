diff --git a/.agents/skills/idea-refine/SKILL.md b/.agents/skills/idea-refine/SKILL.md
--- a/.agents/skills/idea-refine/SKILL.md
+++ b/.agents/skills/idea-refine/SKILL.md
@@ -18,9 +18,4 @@
-This skill is primarily an interactive dialogue. Invoke it with an idea, and the agent will guide you through the process.
-
-```bash
-# Optional: Initialize the ideas directory
-bash skills/idea-refine/scripts/idea-refine.sh
-```
+This skill is an interactive dialogue. Follow the "Жизненный цикл изменений" section in root `AGENTS.md`; use the current change and existing project knowledge to frame the discussion.
 
 **Trigger Phrases:**
 - "Help me refine this idea"
@@ -30,11 +25,6 @@
 ## Output
 
-The final output is a markdown one-pager saved to `docs/ideas/[idea-name].md` (after user confirmation), containing:
-- Problem Statement
-- Recommended Direction
-- Key Assumptions
-- MVP Scope
-- Not Doing list
+Present a concise discussion draft covering the problem, direction, assumptions, and proposed scope. Record sources and discussion in the issue. Put agreed requirements, scope, and approach into the current OpenSpec change; when no change exists yet, use the project proposal workflow before implementation. Keep a single authoritative scope in that change.
 
 ## Detailed Instructions
 
@@ -138,5 +128,5 @@
 **The "Not Doing" list is arguably the most valuable part.** Focus is about saying no to good ideas. Make the trade-offs explicit.
 
-Ask the user if they'd like to save this to `docs/ideas/[idea-name].md` (or a location of their choosing). Only save if they confirm.
+Use the draft to confirm the direction, then update the issue or current change according to the ownership rules in `AGENTS.md`; preserve the explicit implementation-approval boundary.
 
 ### Anti-patterns to Avoid
